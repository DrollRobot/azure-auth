{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"azure-auth","text":"<p>Authentication for Microsoft cloud services, so that other packages do not have to deal with it. Create one <code>AuthContext</code>, hand it to the client for the resource you need, and make requests. The client gets, caches and refreshes tokens.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>uv add git+https://github.com/DrollRobot/azure-auth.git\n\n# optional extras: keyvault, broker\nuv add \"azure-auth[keyvault,broker] @ git+https://github.com/DrollRobot/azure-auth.git\"\n</code></pre>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>import asyncio\nfrom azure_auth import AuthContext, GraphClient\n\n\nasync def main() -&gt; None:\n    auth = AuthContext(\"contoso.onmicrosoft.com\", username=\"admin@contoso.com\", cache=\"disk\")\n    async with GraphClient(auth, scopes=[\"User.Read.All\"]) as graph:\n        users = await graph.get_all(\"/users\", params={\"$select\": \"displayName\"})\n    print(len(users))\n\n\nasyncio.run(main())\n</code></pre> <p>The first request opens the browser. With <code>cache=\"disk\"</code> later runs are silent.</p> <p>Continue with the guide, or look things up in the reference.</p>"},{"location":"guide/","title":"Guide","text":""},{"location":"guide/#how-it-fits-together","title":"How it fits together","text":"<p><code>AuthContext</code> holds one tenant, one set of credentials, one token cache and, for user flows, one account. Resource clients hold an <code>AuthContext</code> and ask it for tokens. Nothing talks to the network until the first token is needed.</p> <pre><code>auth = AuthContext(\"contoso.onmicrosoft.com\", username=\"admin@contoso.com\")\ngraph = GraphClient(auth, scopes=[\"User.Read.All\"])\nexchange = ExchangeClient(auth)\n</code></pre>"},{"location":"guide/#flows","title":"Flows","text":"<p>A context runs exactly one flow, decided by its arguments.</p> Flow Arguments User <code>username=</code> (required), optional <code>broker=True</code> App, secret <code>client_id=</code>, <code>client_secret=</code> App, PEM <code>client_id=</code>, <code>certificate_pem=</code>, <code>private_key_pem=</code>, optional <code>certificate_password=</code> App, PFX <code>client_id=</code>, <code>certificate_pfx=</code> (bytes or path), optional <code>certificate_password=</code> App, Windows certificate store <code>client_id=</code>, <code>certificate_thumbprint=</code>, optional <code>certificate_store=\"LocalMachine\"</code> <p>Secrets and key material are only held in memory. Load them from Key Vault or a keyring, not from source or a plain-text file.</p>"},{"location":"guide/#signing-in","title":"Signing in","text":"<p>Signing in is lazy: the first request prompts if it has to. To choose the moment, call <code>await client.login()</code>. Pass <code>force=True</code> to prompt even when a token is cached.</p> <p><code>username</code> selects the cached account and pre-fills the sign-in page. If somebody else signs in, the context raises <code>AuthError</code>, because their tokens would never be found again.</p> <p>A browser sign-in that nobody completes -- the window closed, or left alone -- fails with <code>AuthError</code> after <code>interactive_timeout</code> seconds (default 120) rather than waiting for ever.</p>"},{"location":"guide/#the-windows-broker","title":"The Windows broker","text":"<p><code>broker=True</code> signs in through the Windows Web Account Manager. It needs the <code>broker</code> extra. When the broker is missing or fails, the context falls back to the browser. Pass <code>broker_fallback=False</code> to get <code>BrokerUnavailable</code> instead. A user who cancels the broker prompt is not sent to the browser.</p>"},{"location":"guide/#client-ids-and-scopes","title":"Client ids and scopes","text":"<p>Each client picks its client id in this order:</p> <ol> <li><code>client_id=</code> on the client.</li> <li><code>client_id=</code> on the <code>AuthContext</code>.</li> <li>The first-party default of the client (user flows only).</li> </ol> <p>App flows never use a first-party id; without a client id they raise <code>ValueError</code>.</p> Client Default client id (user flows) Default scope <code>GraphClient</code> Microsoft Graph PowerShell <code>scopes=</code> argument, else <code>https://graph.microsoft.com/.default</code> <code>AzureClient</code>, <code>KeyVaultClient</code>, Azure SDK clients Azure PowerShell <code>https://management.azure.com/.default</code>, <code>https://vault.azure.net/.default</code> <code>ExchangeClient</code> Exchange Online PowerShell <code>https://outlook.office365.com/.default</code> <code>IppsClient</code> Exchange Online PowerShell <code>https://ps.compliance.protection.outlook.com/.default</code> <p>Two things follow from that table:</p> <ul> <li>Each distinct client id needs its own sign-in. Using Graph and Exchange means two   prompts. Browser single sign-on makes the second one quick. Giving the context your own   <code>client_id</code> with all the permissions you need avoids it.</li> <li>Pass <code>scopes=</code> to <code>GraphClient</code> in user flows. The Microsoft Graph PowerShell   application works by dynamic consent. <code>.default</code> only returns scopes that were consented to   in the tenant before, which in a fresh tenant is close to nothing. Short names such as   <code>User.Read.All</code> are qualified for you. App flows always use <code>.default</code>.</li> </ul>"},{"location":"guide/#consent","title":"Consent","text":"<p>Entra shows a consent screen by itself the first time an application is asked for delegated scopes it has not been granted, so in an ordinary interactive sign-in consent happens without this package doing anything. Asking for more scopes later works the same way: the cached token does not match the new scopes, the silent attempt fails, and the browser opens again.</p> <p>The screen decides what is granted, not this package:</p> <ul> <li>A user who may consent for themselves grants it for themselves.</li> <li>An administrator sees a Consent on behalf of your organization checkbox. For a   permission that requires admin consent there is no other form: Entra grants those to the   whole tenant or not at all, so every user in the tenant gets them through that client id.</li> </ul> <p>This package never grants consent on its own and offers no way to do so without that click. Granting consent is a tenant-wide security change, not something a token request should do as a side effect.</p> <p>When a sign-in ends without a token, the error says which of three things happened:</p> <ul> <li>A user who may not consent is shown Need admin approval. Leaving that page produces a   bare <code>access_denied</code> with no error code, and the library raises <code>AuthError</code> naming the   scopes and the tenant and saying what that means.</li> <li>Pressing Cancel on a consent screen produces <code>consent_required</code> with <code>AADSTS65004</code>   (\"User declined to consent\"), and the library raises <code>ConsentRequired</code> carrying the scopes   and the tenant.</li> <li>A browser window that is closed, or never answered, is given up with an <code>AuthError</code> saying   so after <code>interactive_timeout</code> seconds (default 120). Without that limit the wait would be   indefinite.</li> </ul> <p>The library does not reopen the browser on any of them: the first cannot be fixed by asking again, the second is the user's decision, and the third means nobody is there.</p> <p>Sibling contexts from <code>for_tenant()</code> never prompt at all, so they report <code>ConsentRequired</code> and stop. Consent for a managed tenant has to be granted in that tenant.</p>"},{"location":"guide/#token-cache","title":"Token cache","text":"<code>cache=</code> Behaviour <code>\"memory\"</code> (default) Tokens live as long as the process. <code>\"disk\"</code> Tokens are kept in an encrypted file: DPAPI on Windows, Keychain on macOS, libsecret on Linux. <p><code>cache_path=</code> sets the file. The default is a file in the per-user cache directory.</p> <p>There is no unencrypted disk cache. Where the platform cannot encrypt, for example Linux without libsecret, <code>cache=\"disk\"</code> raises <code>CacheEncryptionUnavailable</code>. It never falls back to plain text and never silently switches to the memory cache.</p>"},{"location":"guide/#multi-tenant-access-gdap","title":"Multi-tenant access (GDAP)","text":"<p><code>auth.for_tenant(tenant_id)</code> returns a sibling context for another tenant. It shares the credentials, the cache and the username, and makes no network call, so it is cheap to call in a loop.</p> <pre><code>home = GraphClient(auth, scopes=[\"DelegatedAdminRelationship.Read.All\"])\nawait home.login()\n\nfor tenant_id in await home.list_managed_tenant_ids():\n    graph = GraphClient(auth.for_tenant(tenant_id), scopes=[\"User.Read.All\"])\n    try:\n        users = await graph.get_all(\"/users\")\n    except (ConsentRequired, InteractionRequired) as error:\n        print(f\"skipping {error.tenant_id}: {error}\")\n</code></pre> <ul> <li>A sibling never prompts. When a token cannot be obtained silently it raises   <code>InteractionRequired</code> or <code>ConsentRequired</code>, and the error carries the tenant id. Sign in on   a client of the root context first.</li> <li><code>list_managed_tenant_ids()</code> reads <code>GET /tenantRelationships/delegatedAdminCustomers</code> and   needs <code>DelegatedAdminRelationship.Read.All</code> consented in the home tenant.</li> <li>The application must be consented to in each managed tenant for the scopes you request.</li> <li>In app flows each sibling builds its own client assertion, because the assertion audience   is the tenant's token endpoint.</li> </ul>"},{"location":"guide/#certificates-in-the-windows-certificate-store","title":"Certificates in the Windows certificate store","text":"<p>With <code>certificate_thumbprint=</code> the private key never leaves the key storage provider, so non-exportable and TPM-backed keys work. Only CNG keys are supported, not legacy CryptoAPI keys.</p> <p>The client assertion is signed PS256, which is what Microsoft documents together with the <code>x5t#S256</code> header. Each PSS signature is checked against the certificate before it is sent. If the key provider refuses PSS padding, or signs with the wrong salt length (a TPM 2.0 outside FIPS mode uses the longest salt the key allows), the credential switches to RS256 and stays there. Errors from Entra ID are never retried with the other algorithm.</p> <p>For <code>certificate_store=\"LocalMachine\"</code> the account running the process needs read access to the private key (certlm.msc, certificate, All Tasks, Manage Private Keys).</p> <p>On other platforms use <code>certificate_pem=</code> or <code>certificate_pfx=</code>.</p>"},{"location":"guide/#requests-retries-and-errors","title":"Requests, retries and errors","text":"<p>Every client:</p> <ul> <li>retries 429 and 503, waiting for <code>Retry-After</code> when the service sends it (<code>max_retries=</code>,   <code>max_retry_wait=</code>);</li> <li>answers a 401 that carries a claims challenge (continuous access evaluation) by getting a   new token with those claims, and any other 401 by refreshing the token once;</li> <li>refuses to send its token to another host. A next link or operation URL that points   elsewhere raises <code>ValueError</code>.</li> </ul> <p>Errors:</p> <pre><code>AuthError\n\u251c\u2500\u2500 InteractionRequired(tenant_id, scopes)\n\u251c\u2500\u2500 ConsentRequired(tenant_id, scopes)\n\u251c\u2500\u2500 AccountSelectionRequired\n\u251c\u2500\u2500 CertificateUnavailable\n\u251c\u2500\u2500 CacheEncryptionUnavailable\n\u2514\u2500\u2500 BrokerUnavailable\nResourceError(status, code, request_id, body)\n\u251c\u2500\u2500 GraphError\n\u251c\u2500\u2500 AzureError\n\u2514\u2500\u2500 InvokeCommandError\n</code></pre> <p>MSAL result dictionaries never reach your code.</p>"},{"location":"guide/#microsoft-graph","title":"Microsoft Graph","text":"<pre><code>async with GraphClient(auth, scopes=[\"User.Read.All\"], api_version=\"beta\") as graph:\n    me = await graph.get(\"/me\")\n    users = await graph.get_all(\"/users\", params={\"$top\": 999})  # follows @odata.nextLink\n    async for page in graph.iter_pages(\"/groups\"):\n        ...\n    results = await graph.batch([{\"method\": \"GET\", \"url\": \"/me\"}, ...])  # 20 per call\n</code></pre> <p><code>batch()</code> returns one response per request in request order. Failed items come back with their status; they are not raised.</p>"},{"location":"guide/#azure-resource-manager","title":"Azure Resource Manager","text":"<p>Every call needs the <code>api_version</code> of the resource provider.</p> <pre><code>async with AzureClient(auth) as arm:\n    groups = await arm.get_all(f\"/subscriptions/{sub}/resourcegroups\", api_version=\"2021-04-01\")\n    started = await arm.send(\"PUT\", path, api_version=\"2021-04-01\", json=body)\n    resource = await arm.wait(started)  # polls Azure-AsyncOperation or Location\n</code></pre> <p>For the typed management SDKs no client is needed: pass <code>credential=auth</code> (or <code>auth.aio</code>).</p>"},{"location":"guide/#exchange-online-and-security-compliance","title":"Exchange Online and Security &amp; Compliance","text":"<pre><code>async with ExchangeClient(auth) as exchange:\n    mailboxes = await exchange.run(\"Get-Mailbox\", IncludeInactive=True)\n    print(exchange.last_warnings)\n\nasync with IppsClient(auth) as ipps:\n    labels = await ipps.run(\"Get-Label\")\n</code></pre> <p>Cmdlet parameters are keyword arguments; use <code>True</code> for switch parameters. Results are dictionaries.</p> <p>Both clients use the <code>InvokeCommand</code> REST endpoint behind the ExchangeOnlineManagement PowerShell module. Microsoft does not document this endpoint. The request shape was read from module version 3.10.1:</p> <ul> <li>URL: <code>https://&lt;host&gt;/adminapi/beta/&lt;tenant GUID&gt;/InvokeCommand</code>. The GUID is read from the   token, so a domain name works as <code>tenant_id</code>. <code>api_version=\"v1.0\"</code> is also accepted.</li> <li>Routing header <code>X-AnchorMailbox</code>: <code>UPN:&lt;username&gt;</code> for a user in their own tenant, and the   tenant's system mailbox for app flows and for sibling (GDAP) contexts. Override it with   <code>anchor_mailbox=</code>.</li> <li> <p>Security &amp; Compliance redirects the first call to a regional host. The client follows that   redirect itself, because HTTP libraries drop the <code>Authorization</code> header on a cross-host   redirect, and keeps using the regional host.</p> </li> <li> <p>Paging works by POSTing the same body again to <code>@odata.nextLink</code>. Confirmed against a live   tenant: 13 pages with <code>page_size=2</code>, each page after the first fetched from the link.   <code>run()</code> walks every page, so it returns the whole result set; use <code>iter_pages()</code> to stop   early.</p> </li> </ul> <p><code>ResultSize</code> does nothing here. In the PowerShell module it is a client-side cap applied while consuming pages, not a cmdlet parameter, so <code>InvokeCommand</code> ignores it \u2014 silently, with no warning, in every spelling (<code>10</code>, <code>\"10\"</code>, <code>\"Unlimited\"</code>). <code>run(\"Get-Mailbox\", ResultSize=10)</code> returns every mailbox in the tenant. To limit results, break out of <code>iter_pages()</code> yourself.</p> <p>The <code>X-ResponseFormat</code> and <code>X-CmdletName</code> headers were reconstructed from memory rather than found in the module assemblies. Live calls succeed with them, which shows they are accepted, not that they are required or correct.</p> <p>App flows need the <code>Exchange.ManageAsApp</code> application permission and an Exchange administrator role on the service principal.</p>"},{"location":"guide/#key-vault","title":"Key Vault","text":"<p>Needs the <code>keyvault</code> extra.</p> <pre><code>from azure_auth.clients.keyvault import KeyVaultClient\n\nasync with KeyVaultClient(auth, \"https://contoso.vault.azure.net\") as vault:\n    password = await vault.get_secret(\"service-password\")\n    certificate_der = await vault.get_certificate(\"signing\")\n    signature = await vault.sign(\"signing-key\", \"PS256\", digest)\n</code></pre> <p>This is a thin wrapper over the Azure SDK. For anything else use the SDK clients directly with <code>credential=auth.aio</code>.</p>"},{"location":"guide/#blocking-clients","title":"Blocking clients","text":"<p><code>azure_auth.sync</code> has the same classes and methods without <code>await</code>. They are generated from the async clients, so behaviour is identical.</p> <pre><code>from azure_auth.sync import GraphClient\nfrom azure_auth.sync.keyvault import KeyVaultClient\n\nwith GraphClient(auth, scopes=[\"User.Read.All\"]) as graph:\n    users = graph.get_all(\"/users\")\n</code></pre> <p><code>AuthContext</code> itself is synchronous and is an Azure SDK <code>TokenCredential</code>. <code>auth.aio</code> is the async view and an <code>AsyncTokenCredential</code>.</p>"},{"location":"reference/auth/","title":"Authentication","text":"<p>The authentication context shared by every resource client.</p> <p>An :class:<code>AuthContext</code> holds one tenant, one set of credentials, one token cache and (for user flows) one account. It creates MSAL applications on demand, one per client id, because the Microsoft first-party client ids differ per resource.</p> <p>The context implements the Azure SDK <code>TokenCredential</code> protocol, and its :attr:<code>aio</code> attribute implements <code>AsyncTokenCredential</code>, so any <code>azure-*</code> SDK client accepts it as <code>credential=</code>.</p> <p>Exceptions raised while acquiring tokens.</p> <p>MSAL reports failures as result dictionaries. Those dictionaries never reach callers of this package; :func:<code>error_from_msal_result</code> turns them into the exception hierarchy below.</p> <p>Application credentials and the client assertion used for certificate authentication.</p> <p>Three kinds of credential are supported for app flows:</p> <ul> <li>a client secret,</li> <li>a certificate and private key supplied as PEM or PFX (MSAL signs the assertion),</li> <li>a certificate in the Windows certificate store whose key cannot be exported (this package   signs the assertion through CNG, see :mod:<code>azure_auth.auth.cng</code>).</li> </ul> <p>Secrets and key material are only ever held in memory.</p> <p>Token cache construction.</p> <p>Two caches exist: an in-memory cache that dies with the process, and an encrypted cache on disk. There is deliberately no unencrypted disk cache. When the platform cannot encrypt, asking for a disk cache raises instead of quietly doing something weaker.</p> <p>Sign with a private key held in the Windows certificate store.</p> <p>The key never leaves the key storage provider, so this works for non-exportable and TPM-backed keys. Signing goes through CNG (<code>NCryptSignHash</code>); legacy CryptoAPI keys are not supported.</p> <p>The design follows <code>microsoft/entrabot</code> (MIT licence), <code>src/entrabot/auth/cncrypt_signer.py</code>. That implementation covers <code>CurrentUser\\My</code> and PKCS#1 padding; this one adds <code>LocalMachine\\My</code> and PSS padding.</p> <p>On platforms other than Windows every function raises :class:<code>~azure_auth.auth.errors.CertificateUnavailable</code>.</p> <p>Well-known identifiers for Microsoft cloud resources.</p> <p>The client ids are Microsoft first-party public client applications. They are used by default for user flows so that callers do not have to register an application. App flows (secret or certificate) always need the caller's own application id.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext","title":"<code>AsyncAuthContext</code>","text":"<p>Asynchronous view of an :class:<code>AuthContext</code> (an <code>AsyncTokenCredential</code>).</p> <p>MSAL is synchronous, so token requests run in a worker thread. Tokens that are still fresh are returned without leaving the event loop.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>class AsyncAuthContext:\n    \"\"\"Asynchronous view of an :class:`AuthContext` (an ``AsyncTokenCredential``).\n\n    MSAL is synchronous, so token requests run in a worker thread. Tokens that are still\n    fresh are returned without leaving the event loop.\n    \"\"\"\n\n    def __init__(self, sync_context: AuthContext) -&gt; None:\n        \"\"\"Wrap a synchronous context.\n\n        Args:\n            sync_context: The context that does the work.\n        \"\"\"\n        self._sync = sync_context\n\n    @property\n    def sync(self) -&gt; AuthContext:\n        \"\"\"The synchronous context behind this view.\"\"\"\n        return self._sync\n\n    def for_tenant(self, tenant_id: str) -&gt; AsyncAuthContext:\n        \"\"\"Return the asynchronous view of a sibling context.\n\n        Args:\n            tenant_id: Tenant id (GUID) or verified domain name of the other tenant.\n\n        Returns:\n            The sibling's asynchronous view.\n        \"\"\"\n        return self._sync.for_tenant(tenant_id).aio\n\n    async def get_token(\n        self,\n        *scopes: str,\n        claims: str | None = None,\n        tenant_id: str | None = None,\n        enable_cae: bool = False,\n        **kwargs: Any,\n    ) -&gt; AccessToken:\n        \"\"\"Request an access token (Azure SDK ``AsyncTokenCredential`` protocol).\n\n        Args:\n            *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n            claims: Claims challenge returned by a resource, as a JSON string.\n            tenant_id: Tenant to request the token from.\n            enable_cae: Accepted for protocol compatibility and ignored.\n            **kwargs: Ignored.\n\n        Returns:\n            The token and its expiry time.\n        \"\"\"\n        view = self.for_tenant(tenant_id or self._sync.tenant_id)\n        info = await view.acquire_token(scopes, claims=claims)\n        return AccessToken(info.token, info.expires_on)\n\n    async def get_token_info(\n        self, *scopes: str, options: TokenRequestOptions | None = None\n    ) -&gt; AccessTokenInfo:\n        \"\"\"Request an access token (Azure SDK ``AsyncSupportsTokenInfo`` protocol).\n\n        Args:\n            *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n            options: ``claims`` and ``tenant_id`` are honoured; ``enable_cae`` is ignored.\n\n        Returns:\n            The token with its expiry time and type.\n        \"\"\"\n        options = options or {}\n        view = self.for_tenant(options.get(\"tenant_id\") or self._sync.tenant_id)\n        return await view.acquire_token(scopes, claims=options.get(\"claims\"))\n\n    async def acquire_token(\n        self,\n        scopes: Sequence[str],\n        *,\n        client_id: str | None = None,\n        claims: str | None = None,\n        force_refresh: bool = False,\n    ) -&gt; AccessTokenInfo:\n        \"\"\"Acquire a token; see :meth:`AuthContext.acquire_token`.\n\n        Args:\n            scopes: Scopes to request.\n            client_id: Client id to use.\n            claims: Claims challenge to satisfy, as a JSON string.\n            force_refresh: Skip cached access tokens.\n\n        Returns:\n            The token with its expiry time and type.\n        \"\"\"\n        if not (claims or force_refresh):\n            cached = self._sync.cached_token(scopes, client_id)\n            if cached is not None:\n                return cached\n        return await asyncio.to_thread(\n            self._sync.acquire_token,\n            scopes,\n            client_id=client_id,\n            claims=claims,\n            force_refresh=force_refresh,\n        )\n\n    async def login(self, *, client_id: str, scopes: Sequence[str], force: bool = False) -&gt; None:\n        \"\"\"Sign in now; see :meth:`AuthContext.login`.\n\n        Args:\n            client_id: Client id to sign in to.\n            scopes: Scopes to request.\n            force: Prompt even when a cached token or account exists.\n        \"\"\"\n        await asyncio.to_thread(self._sync.login, client_id=client_id, scopes=scopes, force=force)\n\n    async def close(self) -&gt; None:\n        \"\"\"Do nothing; present for the ``AsyncTokenCredential`` protocol.\"\"\"\n\n    async def __aenter__(self) -&gt; Self:\n        \"\"\"Enter the context manager.\n\n        Returns:\n            This object.\n        \"\"\"\n        return self\n\n    async def __aexit__(\n        self,\n        exc_type: type[BaseException] | None = None,\n        exc_value: BaseException | None = None,\n        traceback: TracebackType | None = None,\n    ) -&gt; None:\n        \"\"\"Leave the context manager.\"\"\"\n        await self.close()\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.sync","title":"<code>sync: AuthContext</code>  <code>property</code>","text":"<p>The synchronous context behind this view.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.__aenter__","title":"<code>__aenter__() -&gt; Self</code>  <code>async</code>","text":"<p>Enter the context manager.</p> <p>Returns:</p> Type Description <code>Self</code> <p>This object.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>async def __aenter__(self) -&gt; Self:\n    \"\"\"Enter the context manager.\n\n    Returns:\n        This object.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.__aexit__","title":"<code>__aexit__(exc_type: type[BaseException] | None = None, exc_value: BaseException | None = None, traceback: TracebackType | None = None) -&gt; None</code>  <code>async</code>","text":"<p>Leave the context manager.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>async def __aexit__(\n    self,\n    exc_type: type[BaseException] | None = None,\n    exc_value: BaseException | None = None,\n    traceback: TracebackType | None = None,\n) -&gt; None:\n    \"\"\"Leave the context manager.\"\"\"\n    await self.close()\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.__init__","title":"<code>__init__(sync_context: AuthContext) -&gt; None</code>","text":"<p>Wrap a synchronous context.</p> <p>Parameters:</p> Name Type Description Default <code>sync_context</code> <code>AuthContext</code> <p>The context that does the work.</p> required Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def __init__(self, sync_context: AuthContext) -&gt; None:\n    \"\"\"Wrap a synchronous context.\n\n    Args:\n        sync_context: The context that does the work.\n    \"\"\"\n    self._sync = sync_context\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.acquire_token","title":"<code>acquire_token(scopes: Sequence[str], *, client_id: str | None = None, claims: str | None = None, force_refresh: bool = False) -&gt; AccessTokenInfo</code>  <code>async</code>","text":"<p>Acquire a token; see :meth:<code>AuthContext.acquire_token</code>.</p> <p>Parameters:</p> Name Type Description Default <code>scopes</code> <code>Sequence[str]</code> <p>Scopes to request.</p> required <code>client_id</code> <code>str | None</code> <p>Client id to use.</p> <code>None</code> <code>claims</code> <code>str | None</code> <p>Claims challenge to satisfy, as a JSON string.</p> <code>None</code> <code>force_refresh</code> <code>bool</code> <p>Skip cached access tokens.</p> <code>False</code> <p>Returns:</p> Type Description <code>AccessTokenInfo</code> <p>The token with its expiry time and type.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>async def acquire_token(\n    self,\n    scopes: Sequence[str],\n    *,\n    client_id: str | None = None,\n    claims: str | None = None,\n    force_refresh: bool = False,\n) -&gt; AccessTokenInfo:\n    \"\"\"Acquire a token; see :meth:`AuthContext.acquire_token`.\n\n    Args:\n        scopes: Scopes to request.\n        client_id: Client id to use.\n        claims: Claims challenge to satisfy, as a JSON string.\n        force_refresh: Skip cached access tokens.\n\n    Returns:\n        The token with its expiry time and type.\n    \"\"\"\n    if not (claims or force_refresh):\n        cached = self._sync.cached_token(scopes, client_id)\n        if cached is not None:\n            return cached\n    return await asyncio.to_thread(\n        self._sync.acquire_token,\n        scopes,\n        client_id=client_id,\n        claims=claims,\n        force_refresh=force_refresh,\n    )\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.close","title":"<code>close() -&gt; None</code>  <code>async</code>","text":"<p>Do nothing; present for the <code>AsyncTokenCredential</code> protocol.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>async def close(self) -&gt; None:\n    \"\"\"Do nothing; present for the ``AsyncTokenCredential`` protocol.\"\"\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.for_tenant","title":"<code>for_tenant(tenant_id: str) -&gt; AsyncAuthContext</code>","text":"<p>Return the asynchronous view of a sibling context.</p> <p>Parameters:</p> Name Type Description Default <code>tenant_id</code> <code>str</code> <p>Tenant id (GUID) or verified domain name of the other tenant.</p> required <p>Returns:</p> Type Description <code>AsyncAuthContext</code> <p>The sibling's asynchronous view.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def for_tenant(self, tenant_id: str) -&gt; AsyncAuthContext:\n    \"\"\"Return the asynchronous view of a sibling context.\n\n    Args:\n        tenant_id: Tenant id (GUID) or verified domain name of the other tenant.\n\n    Returns:\n        The sibling's asynchronous view.\n    \"\"\"\n    return self._sync.for_tenant(tenant_id).aio\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.get_token","title":"<code>get_token(*scopes: str, claims: str | None = None, tenant_id: str | None = None, enable_cae: bool = False, **kwargs: Any) -&gt; AccessToken</code>  <code>async</code>","text":"<p>Request an access token (Azure SDK <code>AsyncTokenCredential</code> protocol).</p> <p>Parameters:</p> Name Type Description Default <code>*scopes</code> <code>str</code> <p>Scopes to request, normally one <code>&lt;resource&gt;/.default</code> value.</p> <code>()</code> <code>claims</code> <code>str | None</code> <p>Claims challenge returned by a resource, as a JSON string.</p> <code>None</code> <code>tenant_id</code> <code>str | None</code> <p>Tenant to request the token from.</p> <code>None</code> <code>enable_cae</code> <code>bool</code> <p>Accepted for protocol compatibility and ignored.</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Ignored.</p> <code>{}</code> <p>Returns:</p> Type Description <code>AccessToken</code> <p>The token and its expiry time.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>async def get_token(\n    self,\n    *scopes: str,\n    claims: str | None = None,\n    tenant_id: str | None = None,\n    enable_cae: bool = False,\n    **kwargs: Any,\n) -&gt; AccessToken:\n    \"\"\"Request an access token (Azure SDK ``AsyncTokenCredential`` protocol).\n\n    Args:\n        *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n        claims: Claims challenge returned by a resource, as a JSON string.\n        tenant_id: Tenant to request the token from.\n        enable_cae: Accepted for protocol compatibility and ignored.\n        **kwargs: Ignored.\n\n    Returns:\n        The token and its expiry time.\n    \"\"\"\n    view = self.for_tenant(tenant_id or self._sync.tenant_id)\n    info = await view.acquire_token(scopes, claims=claims)\n    return AccessToken(info.token, info.expires_on)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.get_token_info","title":"<code>get_token_info(*scopes: str, options: TokenRequestOptions | None = None) -&gt; AccessTokenInfo</code>  <code>async</code>","text":"<p>Request an access token (Azure SDK <code>AsyncSupportsTokenInfo</code> protocol).</p> <p>Parameters:</p> Name Type Description Default <code>*scopes</code> <code>str</code> <p>Scopes to request, normally one <code>&lt;resource&gt;/.default</code> value.</p> <code>()</code> <code>options</code> <code>TokenRequestOptions | None</code> <p><code>claims</code> and <code>tenant_id</code> are honoured; <code>enable_cae</code> is ignored.</p> <code>None</code> <p>Returns:</p> Type Description <code>AccessTokenInfo</code> <p>The token with its expiry time and type.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>async def get_token_info(\n    self, *scopes: str, options: TokenRequestOptions | None = None\n) -&gt; AccessTokenInfo:\n    \"\"\"Request an access token (Azure SDK ``AsyncSupportsTokenInfo`` protocol).\n\n    Args:\n        *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n        options: ``claims`` and ``tenant_id`` are honoured; ``enable_cae`` is ignored.\n\n    Returns:\n        The token with its expiry time and type.\n    \"\"\"\n    options = options or {}\n    view = self.for_tenant(options.get(\"tenant_id\") or self._sync.tenant_id)\n    return await view.acquire_token(scopes, claims=options.get(\"claims\"))\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AsyncAuthContext.login","title":"<code>login(*, client_id: str, scopes: Sequence[str], force: bool = False) -&gt; None</code>  <code>async</code>","text":"<p>Sign in now; see :meth:<code>AuthContext.login</code>.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client id to sign in to.</p> required <code>scopes</code> <code>Sequence[str]</code> <p>Scopes to request.</p> required <code>force</code> <code>bool</code> <p>Prompt even when a cached token or account exists.</p> <code>False</code> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>async def login(self, *, client_id: str, scopes: Sequence[str], force: bool = False) -&gt; None:\n    \"\"\"Sign in now; see :meth:`AuthContext.login`.\n\n    Args:\n        client_id: Client id to sign in to.\n        scopes: Scopes to request.\n        force: Prompt even when a cached token or account exists.\n    \"\"\"\n    await asyncio.to_thread(self._sync.login, client_id=client_id, scopes=scopes, force=force)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext","title":"<code>AuthContext</code>","text":"<p>Credentials, cache and account for one tenant.</p> <p>A context runs either a user flow (<code>username</code> given, no credential) or an app flow (<code>client_id</code> plus a secret or certificate). Authentication is lazy: nothing happens until the first token is requested.</p> Example <p>auth = AuthContext(\"contoso.onmicrosoft.com\", username=\"admin@contoso.com\") token = auth.get_token(\"https://management.azure.com/.default\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>class AuthContext:\n    \"\"\"Credentials, cache and account for one tenant.\n\n    A context runs either a *user flow* (``username`` given, no credential) or an *app flow*\n    (``client_id`` plus a secret or certificate). Authentication is lazy: nothing happens\n    until the first token is requested.\n\n    Example:\n        &gt;&gt;&gt; auth = AuthContext(\"contoso.onmicrosoft.com\", username=\"admin@contoso.com\")\n        &gt;&gt;&gt; token = auth.get_token(\"https://management.azure.com/.default\")  # doctest: +SKIP\n    \"\"\"\n\n    def __init__(\n        self,\n        tenant_id: str,\n        *,\n        client_id: str | None = None,\n        username: str | None = None,\n        client_secret: str | None = None,\n        certificate_thumbprint: str | None = None,\n        certificate_store: cng.StoreLocation = \"CurrentUser\",\n        certificate_pem: str | bytes | None = None,\n        private_key_pem: str | bytes | None = None,\n        certificate_pfx: bytes | str | Path | None = None,\n        certificate_password: str | None = None,\n        cache: CacheKind = \"memory\",\n        cache_path: str | Path | None = None,\n        broker: bool = False,\n        broker_fallback: bool = True,\n        authority_host: str = DEFAULT_AUTHORITY_HOST,\n        interactive_timeout: float = _DEFAULT_INTERACTIVE_TIMEOUT_SECONDS,\n    ) -&gt; None:\n        \"\"\"Configure the context. No network call is made.\n\n        Args:\n            tenant_id: Tenant id (GUID) or verified domain name.\n            client_id: Application (client) id. App flows need one, here or on the resource\n                client. For user flows it overrides the first-party default of every\n                resource client.\n            username: User principal name. Required for user flows; selects the cached\n                account and pre-fills the sign-in page. Not allowed for app flows.\n            client_secret: Client secret for an app flow.\n            certificate_thumbprint: SHA-1 thumbprint of a certificate in the Windows\n                certificate store (``My``). The private key may be non-exportable.\n            certificate_store: ``CurrentUser`` (default) or ``LocalMachine``.\n            certificate_pem: Certificate in PEM format, used with ``private_key_pem``.\n            private_key_pem: Private key in PEM format.\n            certificate_pfx: PKCS#12 archive as bytes or a path.\n            certificate_password: Password for ``private_key_pem`` or ``certificate_pfx``.\n            cache: ``memory`` (default) or ``disk`` for an encrypted cache file.\n            cache_path: Location of the disk cache. Defaults to the per-user cache directory.\n            broker: Use the Windows authentication broker (WAM) for user sign-in. Needs the\n                ``broker`` extra.\n            broker_fallback: When the broker cannot be used, fall back to the browser\n                (default) instead of raising :class:`BrokerUnavailable`.\n            authority_host: Entra ID authority host, for sovereign clouds.\n            interactive_timeout: Seconds a browser sign-in waits for the person before it\n                fails with :class:`AuthError` (default 120). A closed window would otherwise\n                wait for ever.\n\n        Raises:\n            ValueError: If the arguments do not describe exactly one flow.\n            CacheEncryptionUnavailable: If ``cache='disk'`` cannot be encrypted here.\n            CertificateUnavailable: If a PFX archive cannot be read.\n        \"\"\"\n        if not tenant_id:\n            raise ValueError(\"tenant_id is required\")\n        credential = _build_credential(\n            client_secret=client_secret,\n            certificate_thumbprint=certificate_thumbprint,\n            certificate_store=certificate_store,\n            certificate_pem=certificate_pem,\n            private_key_pem=private_key_pem,\n            certificate_pfx=certificate_pfx,\n            certificate_password=certificate_password,\n        )\n        if credential is None and not username:\n            raise ValueError(\"username is required for user flows\")\n        if credential is not None:\n            if username:\n                raise ValueError(\"username is not used for app flows\")\n            if broker:\n                raise ValueError(\"broker is only available for user flows\")\n\n        self._tenant_id = tenant_id\n        self._client_id = client_id\n        self._username = username\n        self._credential = credential\n        self._cache = build_cache(cache, cache_path)\n        self._broker = broker\n        self._broker_fallback = broker_fallback\n        self._authority_host = authority_host.rstrip(\"/\")\n        self._interactive_timeout = interactive_timeout\n        self._interactive_allowed = True\n        self._init_state()\n\n    def _init_state(self) -&gt; None:\n        \"\"\"Create the per-context mutable state.\"\"\"\n        self._apps: dict[tuple[str, bool], Any] = {}\n        self._locks: dict[_TokenKey, threading.Lock] = {}\n        self._tokens: dict[_TokenKey, AccessTokenInfo] = {}\n        self._siblings: dict[str, AuthContext] = {}\n        self._guard = threading.Lock()\n        self._aio: AsyncAuthContext | None = None\n\n    def __repr__(self) -&gt; str:\n        \"\"\"Describe the context without revealing credentials.\n\n        Returns:\n            A short description.\n        \"\"\"\n        flow = \"app\" if self.is_app_flow else \"user\"\n        return f\"AuthContext(tenant_id={self._tenant_id!r}, flow={flow!r})\"\n\n    # ------------------------------------------------------------------ properties\n\n    @property\n    def tenant_id(self) -&gt; str:\n        \"\"\"The tenant this context authenticates against.\"\"\"\n        return self._tenant_id\n\n    @property\n    def client_id(self) -&gt; str | None:\n        \"\"\"The client id given to this context, if any.\"\"\"\n        return self._client_id\n\n    @property\n    def username(self) -&gt; str | None:\n        \"\"\"The user principal name of a user flow, or ``None`` for an app flow.\"\"\"\n        return self._username\n\n    @property\n    def is_app_flow(self) -&gt; bool:\n        \"\"\"Whether this context authenticates as an application.\"\"\"\n        return self._credential is not None\n\n    @property\n    def is_sibling(self) -&gt; bool:\n        \"\"\"Whether this context was created by :meth:`for_tenant` and so never prompts.\"\"\"\n        return not self._interactive_allowed\n\n    @property\n    def authority(self) -&gt; str:\n        \"\"\"The authority URL for this tenant.\"\"\"\n        return f\"{self._authority_host}/{self._tenant_id}\"\n\n    @property\n    def token_endpoint(self) -&gt; str:\n        \"\"\"The v2.0 token endpoint for this tenant.\"\"\"\n        return f\"{self.authority}/oauth2/v2.0/token\"\n\n    @property\n    def aio(self) -&gt; AsyncAuthContext:\n        \"\"\"The asynchronous view of this context (an ``AsyncTokenCredential``).\"\"\"\n        if self._aio is None:\n            self._aio = AsyncAuthContext(self)\n        return self._aio\n\n    # ------------------------------------------------------------------ tenants\n\n    def for_tenant(self, tenant_id: str) -&gt; AuthContext:\n        \"\"\"Return a sibling context for another tenant.\n\n        The sibling shares this context's credentials, cache and username, so a user with\n        GDAP access obtains managed-tenant tokens from the one home-tenant sign-in. No network call\n        is made. A sibling never opens a browser: when a token cannot be obtained silently it\n        raises :class:`InteractionRequired` or :class:`ConsentRequired` carrying the tenant\n        id.\n\n        Args:\n            tenant_id: Tenant id (GUID) or verified domain name of the other tenant.\n\n        Returns:\n            The sibling context. Repeated calls return the same object.\n        \"\"\"\n        if tenant_id.lower() == self._tenant_id.lower():\n            return self\n        with self._guard:\n            sibling = self._siblings.get(tenant_id.lower())\n            if sibling is None:\n                sibling = AuthContext.__new__(AuthContext)\n                sibling._tenant_id = tenant_id\n                sibling._client_id = self._client_id\n                sibling._username = self._username\n                sibling._credential = self._credential\n                sibling._cache = self._cache\n                sibling._broker = self._broker\n                sibling._broker_fallback = self._broker_fallback\n                sibling._authority_host = self._authority_host\n                sibling._interactive_timeout = self._interactive_timeout\n                sibling._interactive_allowed = False\n                sibling._init_state()\n                self._siblings[tenant_id.lower()] = sibling\n            return sibling\n\n    # ------------------------------------------------------------------ public token API\n\n    def get_token(\n        self,\n        *scopes: str,\n        claims: str | None = None,\n        tenant_id: str | None = None,\n        enable_cae: bool = False,\n        **kwargs: Any,\n    ) -&gt; AccessToken:\n        \"\"\"Request an access token (Azure SDK ``TokenCredential`` protocol).\n\n        Args:\n            *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n            claims: Claims challenge returned by a resource, as a JSON string.\n            tenant_id: Tenant to request the token from. A tenant other than this context's\n                is served by :meth:`for_tenant`.\n            enable_cae: Accepted for protocol compatibility and ignored.\n            **kwargs: Ignored.\n\n        Returns:\n            The token and its expiry time.\n        \"\"\"\n        context = self.for_tenant(tenant_id or self._tenant_id)\n        info = context.acquire_token(scopes, claims=claims)\n        return AccessToken(info.token, info.expires_on)\n\n    def get_token_info(\n        self, *scopes: str, options: TokenRequestOptions | None = None\n    ) -&gt; AccessTokenInfo:\n        \"\"\"Request an access token (Azure SDK ``SupportsTokenInfo`` protocol).\n\n        Args:\n            *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n            options: ``claims`` and ``tenant_id`` are honoured; ``enable_cae`` is ignored.\n\n        Returns:\n            The token with its expiry time and type.\n        \"\"\"\n        options = options or {}\n        context = self.for_tenant(options.get(\"tenant_id\") or self._tenant_id)\n        return context.acquire_token(scopes, claims=options.get(\"claims\"))\n\n    def acquire_token(\n        self,\n        scopes: Iterable[str],\n        *,\n        client_id: str | None = None,\n        claims: str | None = None,\n        force_refresh: bool = False,\n    ) -&gt; AccessTokenInfo:\n        \"\"\"Acquire a token, signing in first when that is needed and allowed.\n\n        This is what resource clients call. Concurrent calls for the same client id and\n        scopes are serialised, so a cold start opens one browser window, not many.\n\n        Args:\n            scopes: Scopes to request.\n            client_id: Client id to use. Defaults to the context's client id, then (user\n                flows only) to the Azure PowerShell client id.\n            claims: Claims challenge to satisfy, as a JSON string.\n            force_refresh: Skip cached access tokens.\n\n        Returns:\n            The token with its expiry time and type.\n\n        Raises:\n            InteractionRequired: If the user must sign in and this context may not prompt.\n            ConsentRequired: If consent is missing in the tenant.\n            AuthError: For any other failure.\n        \"\"\"\n        return self._acquire(scopes, client_id, claims, force_refresh, force_interactive=False)\n\n    def login(self, *, client_id: str, scopes: Iterable[str], force: bool = False) -&gt; None:\n        \"\"\"Sign in now instead of at the first request.\n\n        Resource clients call this from their own ``login()`` with their client id and\n        scopes. Calling it is optional, because the first token request signs in anyway.\n\n        Args:\n            client_id: Client id to sign in to.\n            scopes: Scopes to request.\n            force: Prompt even when a cached token or account exists. Ignored by app flows.\n\n        Raises:\n            InteractionRequired: If this is a sibling context and the user must sign in.\n        \"\"\"\n        self._acquire(scopes, client_id, None, force, force_interactive=force)\n\n    # ------------------------------------------------------------------ internals\n\n    def resolve_client_id(self, client_id: str | None = None) -&gt; str:\n        \"\"\"Apply the client id precedence rule for a bare token request.\n\n        Args:\n            client_id: Client id chosen by the caller, if any.\n\n        Returns:\n            ``client_id``, else the context's client id, else (user flows) the Azure\n            PowerShell client id.\n\n        Raises:\n            ValueError: If an app flow has no client id.\n        \"\"\"\n        resolved = client_id or self._client_id\n        if resolved:\n            return resolved\n        if self.is_app_flow:\n            raise ValueError(\"App flows need a client_id; first-party client ids cannot be used\")\n        return AZURE_POWERSHELL_CLIENT_ID\n\n    def cached_token(self, scopes: Iterable[str], client_id: str | None) -&gt; AccessTokenInfo | None:\n        \"\"\"Return a still-fresh token from the in-process memo, without locking.\n\n        Args:\n            scopes: Scopes the token was requested for.\n            client_id: Client id the token was requested with.\n\n        Returns:\n            The token, or ``None`` when there is none or it is close to expiry.\n        \"\"\"\n        key = (self.resolve_client_id(client_id), frozenset(scopes))\n        info = self._tokens.get(key)\n        if info is not None and info.expires_on - time.time() &gt; _REFRESH_MARGIN_SECONDS:\n            return info\n        return None\n\n    def _acquire(\n        self,\n        scopes: Iterable[str],\n        client_id: str | None,\n        claims: str | None,\n        force_refresh: bool,\n        *,\n        force_interactive: bool,\n    ) -&gt; AccessTokenInfo:\n        \"\"\"Acquire a token under the single-flight lock for its key.\n\n        Args:\n            scopes: Scopes to request.\n            client_id: Client id chosen by the caller, if any.\n            claims: Claims challenge to satisfy.\n            force_refresh: Skip cached access tokens.\n            force_interactive: Prompt even when a silent token is available.\n\n        Returns:\n            The token with its expiry time and type.\n        \"\"\"\n        scope_list = list(dict.fromkeys(scopes))\n        if not scope_list:\n            raise ValueError(\"At least one scope is required\")\n        resolved = self.resolve_client_id(client_id)\n        key: _TokenKey = (resolved, frozenset(scope_list))\n        with self._guard:\n            lock = self._locks.setdefault(key, threading.Lock())\n        with lock:\n            if not (claims or force_refresh or force_interactive):\n                cached = self.cached_token(scope_list, resolved)\n                if cached is not None:\n                    return cached\n            if self.is_app_flow:\n                result = self._acquire_for_app(resolved, scope_list, claims, force_refresh)\n            else:\n                result = self._acquire_for_user(\n                    resolved, scope_list, claims, force_refresh, force_interactive\n                )\n            info = AccessTokenInfo(\n                str(result[\"access_token\"]),\n                int(time.time()) + int(result.get(\"expires_in\", 0)),\n                token_type=str(result.get(\"token_type\", \"Bearer\")),\n            )\n            self._tokens[key] = info\n            return info\n\n    def _app(self, client_id: str, *, use_broker: bool = False) -&gt; Any:\n        \"\"\"Return the memoised MSAL application for a client id.\n\n        Args:\n            client_id: Client id of the application.\n            use_broker: Whether the application should use the Windows broker.\n\n        Returns:\n            An ``msal.PublicClientApplication`` or ``msal.ConfidentialClientApplication``.\n        \"\"\"\n        key = (client_id, use_broker)\n        app = self._apps.get(key)\n        if app is None:\n            if self._credential is not None:\n                app = msal.ConfidentialClientApplication(\n                    client_id,\n                    client_credential=self._credential.msal_client_credential(\n                        client_id=client_id, token_endpoint=self.token_endpoint\n                    ),\n                    authority=self.authority,\n                    token_cache=self._cache,\n                )\n            elif use_broker:\n                app = msal.PublicClientApplication(\n                    client_id,\n                    authority=self.authority,\n                    token_cache=self._cache,\n                    enable_broker_on_windows=True,\n                )\n            else:\n                app = msal.PublicClientApplication(\n                    client_id, authority=self.authority, token_cache=self._cache\n                )\n            with self._guard:\n                app = self._apps.setdefault(key, app)\n        return app\n\n    def _acquire_for_app(\n        self, client_id: str, scopes: list[str], claims: str | None, force_refresh: bool\n    ) -&gt; dict[str, Any]:\n        \"\"\"Run the client credentials flow.\n\n        Args:\n            client_id: Client id of the application.\n            scopes: Scopes to request; ``&lt;resource&gt;/.default`` for this flow.\n            claims: Claims challenge to satisfy.\n            force_refresh: Drop cached tokens for this client first.\n\n        Returns:\n            The successful MSAL result.\n        \"\"\"\n        app = self._app(client_id)\n        if force_refresh:\n            app.remove_tokens_for_client()\n        result = app.acquire_token_for_client(scopes, claims_challenge=claims)\n        if \"access_token\" not in result:\n            raise error_from_msal_result(result, tenant_id=self._tenant_id, scopes=scopes)\n        return dict(result)\n\n    def _broker_usable(self) -&gt; bool:\n        \"\"\"Decide whether to use the Windows broker for this context.\n\n        Returns:\n            ``True`` when the broker was requested and can be used.\n\n        Raises:\n            BrokerUnavailable: If the broker was requested, cannot be used, and\n                ``broker_fallback`` is off.\n        \"\"\"\n        if not self._broker:\n            return False\n        if _broker_installed():\n            return True\n        if not self._broker_fallback:\n            raise BrokerUnavailable(\n                \"The authentication broker needs Windows and the 'broker' extra \"\n                \"(pip install azure-auth[broker])\"\n            )\n        _LOGGER.warning(\"Authentication broker unavailable; falling back to the browser\")\n        return False\n\n    def _acquire_for_user(\n        self,\n        client_id: str,\n        scopes: list[str],\n        claims: str | None,\n        force_refresh: bool,\n        force_interactive: bool,\n    ) -&gt; dict[str, Any]:\n        \"\"\"Get a user token: silently if possible, interactively if allowed.\n\n        Args:\n            client_id: Client id of the public client application.\n            scopes: Scopes to request.\n            claims: Claims challenge to satisfy.\n            force_refresh: Skip cached access tokens.\n            force_interactive: Prompt even when a silent token is available.\n\n        Returns:\n            The successful MSAL result.\n        \"\"\"\n        use_broker = self._broker_usable()\n        app = self._app(client_id, use_broker=use_broker)\n        result: dict[str, Any] | None = None\n        if not force_interactive:\n            accounts = app.get_accounts(username=self._username)\n            if len(accounts) &gt; 1:\n                raise AccountSelectionRequired(\n                    f\"{len(accounts)} cached accounts match {self._username!r}\"\n                )\n            if accounts:\n                result = app.acquire_token_silent_with_error(\n                    scopes,\n                    account=accounts[0],\n                    claims_challenge=claims,\n                    force_refresh=force_refresh,\n                )\n            if result and \"access_token\" in result:\n                return dict(result)\n\n        if not self._interactive_allowed:\n            error = error_from_msal_result(result, tenant_id=self._tenant_id, scopes=scopes)\n            if isinstance(error, InteractionRequired):\n                raise InteractionRequired(\n                    f\"{error} Sibling contexts never prompt; call login() on a client of the \"\n                    \"root context first.\",\n                    tenant_id=self._tenant_id,\n                    scopes=scopes,\n                )\n            raise error\n\n        # Entra puts up its own consent screen when an application has not been granted the\n        # scopes being asked for, so consent is handled by the ordinary sign-in and needs\n        # nothing from this package. A retry with prompt=consent was tried here and removed:\n        # measured against a live tenant on 2026-09-20, a user who may not consent is shown\n        # \"Need admin approval\" and leaving that page returns a bare access_denied, which no\n        # retry can fix. Pressing Cancel on the consent screen returns consent_required with\n        # AADSTS65004 (measured 2026-09-25), which is the user's decision. Reopening the\n        # browser on either would be wrong, and the retry could never fire in the case it was\n        # written for.\n        result = self._interactive(client_id, scopes, claims, use_broker=use_broker)\n        if \"access_token\" not in result:\n            raise error_from_msal_result(result, tenant_id=self._tenant_id, scopes=scopes)\n        self._check_signed_in_user(result)\n        return result\n\n    def _interactive(\n        self, client_id: str, scopes: list[str], claims: str | None, *, use_broker: bool\n    ) -&gt; dict[str, Any]:\n        \"\"\"Prompt the user, through the broker when enabled, else in the browser.\n\n        Args:\n            client_id: Client id of the public client application.\n            scopes: Scopes to request.\n            claims: Claims challenge to satisfy.\n            use_broker: Whether to try the Windows broker first.\n\n        Returns:\n            The MSAL result, successful or not.\n\n        Raises:\n            BrokerUnavailable: If the broker fails and ``broker_fallback`` is off.\n        \"\"\"\n        if use_broker:\n            broker_app = self._app(client_id, use_broker=True)\n            try:\n                result = broker_app.acquire_token_interactive(\n                    scopes,\n                    login_hint=self._username,\n                    claims_challenge=claims,\n                    parent_window_handle=broker_app.CONSOLE_WINDOW_HANDLE,\n                )\n            except Exception as exc:\n                if not self._broker_fallback:\n                    raise BrokerUnavailable(f\"The authentication broker failed: {exc}\") from exc\n                _LOGGER.warning(\"Authentication broker failed (%s); using the browser\", exc)\n            else:\n                if \"access_token\" in result or result.get(\"error\") != \"broker_error\":\n                    return dict(result)\n                if not self._broker_fallback:\n                    raise BrokerUnavailable(\n                        f\"The authentication broker failed: {result.get('error_description')}\"\n                    )\n                _LOGGER.warning(\"Authentication broker failed; using the browser\")\n\n        app = self._app(client_id, use_broker=False)\n        try:\n            result = app.acquire_token_interactive(\n                scopes,\n                login_hint=self._username,\n                claims_challenge=claims,\n                timeout=self._interactive_timeout,\n            )\n        except BrowserInteractionTimeoutError as exc:\n            raise AuthError(\n                f\"The browser sign-in was not completed within {self._interactive_timeout:g} \"\n                \"seconds. The window was probably closed, or nobody was there to answer it.\"\n            ) from exc\n        return dict(result)\n\n    def _check_signed_in_user(self, result: dict[str, Any]) -&gt; None:\n        \"\"\"Make sure the person who signed in is the configured user.\n\n        Args:\n            result: A successful MSAL result.\n\n        Raises:\n            AuthError: If somebody else signed in. Their tokens would never be found again,\n                because cached accounts are selected by username.\n        \"\"\"\n        signed_in = (result.get(\"id_token_claims\") or {}).get(\"preferred_username\")\n        if signed_in and self._username and signed_in.lower() != self._username.lower():\n            raise AuthError(\n                f\"Signed in as {signed_in!r}, but this context is configured for {self._username!r}\"\n            )\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.aio","title":"<code>aio: AsyncAuthContext</code>  <code>property</code>","text":"<p>The asynchronous view of this context (an <code>AsyncTokenCredential</code>).</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.authority","title":"<code>authority: str</code>  <code>property</code>","text":"<p>The authority URL for this tenant.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.client_id","title":"<code>client_id: str | None</code>  <code>property</code>","text":"<p>The client id given to this context, if any.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.is_app_flow","title":"<code>is_app_flow: bool</code>  <code>property</code>","text":"<p>Whether this context authenticates as an application.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.is_sibling","title":"<code>is_sibling: bool</code>  <code>property</code>","text":"<p>Whether this context was created by :meth:<code>for_tenant</code> and so never prompts.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.tenant_id","title":"<code>tenant_id: str</code>  <code>property</code>","text":"<p>The tenant this context authenticates against.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.token_endpoint","title":"<code>token_endpoint: str</code>  <code>property</code>","text":"<p>The v2.0 token endpoint for this tenant.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.username","title":"<code>username: str | None</code>  <code>property</code>","text":"<p>The user principal name of a user flow, or <code>None</code> for an app flow.</p>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.__init__","title":"<code>__init__(tenant_id: str, *, client_id: str | None = None, username: str | None = None, client_secret: str | None = None, certificate_thumbprint: str | None = None, certificate_store: cng.StoreLocation = 'CurrentUser', certificate_pem: str | bytes | None = None, private_key_pem: str | bytes | None = None, certificate_pfx: bytes | str | Path | None = None, certificate_password: str | None = None, cache: CacheKind = 'memory', cache_path: str | Path | None = None, broker: bool = False, broker_fallback: bool = True, authority_host: str = DEFAULT_AUTHORITY_HOST, interactive_timeout: float = _DEFAULT_INTERACTIVE_TIMEOUT_SECONDS) -&gt; None</code>","text":"<p>Configure the context. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>tenant_id</code> <code>str</code> <p>Tenant id (GUID) or verified domain name.</p> required <code>client_id</code> <code>str | None</code> <p>Application (client) id. App flows need one, here or on the resource client. For user flows it overrides the first-party default of every resource client.</p> <code>None</code> <code>username</code> <code>str | None</code> <p>User principal name. Required for user flows; selects the cached account and pre-fills the sign-in page. Not allowed for app flows.</p> <code>None</code> <code>client_secret</code> <code>str | None</code> <p>Client secret for an app flow.</p> <code>None</code> <code>certificate_thumbprint</code> <code>str | None</code> <p>SHA-1 thumbprint of a certificate in the Windows certificate store (<code>My</code>). The private key may be non-exportable.</p> <code>None</code> <code>certificate_store</code> <code>StoreLocation</code> <p><code>CurrentUser</code> (default) or <code>LocalMachine</code>.</p> <code>'CurrentUser'</code> <code>certificate_pem</code> <code>str | bytes | None</code> <p>Certificate in PEM format, used with <code>private_key_pem</code>.</p> <code>None</code> <code>private_key_pem</code> <code>str | bytes | None</code> <p>Private key in PEM format.</p> <code>None</code> <code>certificate_pfx</code> <code>bytes | str | Path | None</code> <p>PKCS#12 archive as bytes or a path.</p> <code>None</code> <code>certificate_password</code> <code>str | None</code> <p>Password for <code>private_key_pem</code> or <code>certificate_pfx</code>.</p> <code>None</code> <code>cache</code> <code>CacheKind</code> <p><code>memory</code> (default) or <code>disk</code> for an encrypted cache file.</p> <code>'memory'</code> <code>cache_path</code> <code>str | Path | None</code> <p>Location of the disk cache. Defaults to the per-user cache directory.</p> <code>None</code> <code>broker</code> <code>bool</code> <p>Use the Windows authentication broker (WAM) for user sign-in. Needs the <code>broker</code> extra.</p> <code>False</code> <code>broker_fallback</code> <code>bool</code> <p>When the broker cannot be used, fall back to the browser (default) instead of raising :class:<code>BrokerUnavailable</code>.</p> <code>True</code> <code>authority_host</code> <code>str</code> <p>Entra ID authority host, for sovereign clouds.</p> <code>DEFAULT_AUTHORITY_HOST</code> <code>interactive_timeout</code> <code>float</code> <p>Seconds a browser sign-in waits for the person before it fails with :class:<code>AuthError</code> (default 120). A closed window would otherwise wait for ever.</p> <code>_DEFAULT_INTERACTIVE_TIMEOUT_SECONDS</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the arguments do not describe exactly one flow.</p> <code>CacheEncryptionUnavailable</code> <p>If <code>cache='disk'</code> cannot be encrypted here.</p> <code>CertificateUnavailable</code> <p>If a PFX archive cannot be read.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def __init__(\n    self,\n    tenant_id: str,\n    *,\n    client_id: str | None = None,\n    username: str | None = None,\n    client_secret: str | None = None,\n    certificate_thumbprint: str | None = None,\n    certificate_store: cng.StoreLocation = \"CurrentUser\",\n    certificate_pem: str | bytes | None = None,\n    private_key_pem: str | bytes | None = None,\n    certificate_pfx: bytes | str | Path | None = None,\n    certificate_password: str | None = None,\n    cache: CacheKind = \"memory\",\n    cache_path: str | Path | None = None,\n    broker: bool = False,\n    broker_fallback: bool = True,\n    authority_host: str = DEFAULT_AUTHORITY_HOST,\n    interactive_timeout: float = _DEFAULT_INTERACTIVE_TIMEOUT_SECONDS,\n) -&gt; None:\n    \"\"\"Configure the context. No network call is made.\n\n    Args:\n        tenant_id: Tenant id (GUID) or verified domain name.\n        client_id: Application (client) id. App flows need one, here or on the resource\n            client. For user flows it overrides the first-party default of every\n            resource client.\n        username: User principal name. Required for user flows; selects the cached\n            account and pre-fills the sign-in page. Not allowed for app flows.\n        client_secret: Client secret for an app flow.\n        certificate_thumbprint: SHA-1 thumbprint of a certificate in the Windows\n            certificate store (``My``). The private key may be non-exportable.\n        certificate_store: ``CurrentUser`` (default) or ``LocalMachine``.\n        certificate_pem: Certificate in PEM format, used with ``private_key_pem``.\n        private_key_pem: Private key in PEM format.\n        certificate_pfx: PKCS#12 archive as bytes or a path.\n        certificate_password: Password for ``private_key_pem`` or ``certificate_pfx``.\n        cache: ``memory`` (default) or ``disk`` for an encrypted cache file.\n        cache_path: Location of the disk cache. Defaults to the per-user cache directory.\n        broker: Use the Windows authentication broker (WAM) for user sign-in. Needs the\n            ``broker`` extra.\n        broker_fallback: When the broker cannot be used, fall back to the browser\n            (default) instead of raising :class:`BrokerUnavailable`.\n        authority_host: Entra ID authority host, for sovereign clouds.\n        interactive_timeout: Seconds a browser sign-in waits for the person before it\n            fails with :class:`AuthError` (default 120). A closed window would otherwise\n            wait for ever.\n\n    Raises:\n        ValueError: If the arguments do not describe exactly one flow.\n        CacheEncryptionUnavailable: If ``cache='disk'`` cannot be encrypted here.\n        CertificateUnavailable: If a PFX archive cannot be read.\n    \"\"\"\n    if not tenant_id:\n        raise ValueError(\"tenant_id is required\")\n    credential = _build_credential(\n        client_secret=client_secret,\n        certificate_thumbprint=certificate_thumbprint,\n        certificate_store=certificate_store,\n        certificate_pem=certificate_pem,\n        private_key_pem=private_key_pem,\n        certificate_pfx=certificate_pfx,\n        certificate_password=certificate_password,\n    )\n    if credential is None and not username:\n        raise ValueError(\"username is required for user flows\")\n    if credential is not None:\n        if username:\n            raise ValueError(\"username is not used for app flows\")\n        if broker:\n            raise ValueError(\"broker is only available for user flows\")\n\n    self._tenant_id = tenant_id\n    self._client_id = client_id\n    self._username = username\n    self._credential = credential\n    self._cache = build_cache(cache, cache_path)\n    self._broker = broker\n    self._broker_fallback = broker_fallback\n    self._authority_host = authority_host.rstrip(\"/\")\n    self._interactive_timeout = interactive_timeout\n    self._interactive_allowed = True\n    self._init_state()\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.__repr__","title":"<code>__repr__() -&gt; str</code>","text":"<p>Describe the context without revealing credentials.</p> <p>Returns:</p> Type Description <code>str</code> <p>A short description.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Describe the context without revealing credentials.\n\n    Returns:\n        A short description.\n    \"\"\"\n    flow = \"app\" if self.is_app_flow else \"user\"\n    return f\"AuthContext(tenant_id={self._tenant_id!r}, flow={flow!r})\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.acquire_token","title":"<code>acquire_token(scopes: Iterable[str], *, client_id: str | None = None, claims: str | None = None, force_refresh: bool = False) -&gt; AccessTokenInfo</code>","text":"<p>Acquire a token, signing in first when that is needed and allowed.</p> <p>This is what resource clients call. Concurrent calls for the same client id and scopes are serialised, so a cold start opens one browser window, not many.</p> <p>Parameters:</p> Name Type Description Default <code>scopes</code> <code>Iterable[str]</code> <p>Scopes to request.</p> required <code>client_id</code> <code>str | None</code> <p>Client id to use. Defaults to the context's client id, then (user flows only) to the Azure PowerShell client id.</p> <code>None</code> <code>claims</code> <code>str | None</code> <p>Claims challenge to satisfy, as a JSON string.</p> <code>None</code> <code>force_refresh</code> <code>bool</code> <p>Skip cached access tokens.</p> <code>False</code> <p>Returns:</p> Type Description <code>AccessTokenInfo</code> <p>The token with its expiry time and type.</p> <p>Raises:</p> Type Description <code>InteractionRequired</code> <p>If the user must sign in and this context may not prompt.</p> <code>ConsentRequired</code> <p>If consent is missing in the tenant.</p> <code>AuthError</code> <p>For any other failure.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def acquire_token(\n    self,\n    scopes: Iterable[str],\n    *,\n    client_id: str | None = None,\n    claims: str | None = None,\n    force_refresh: bool = False,\n) -&gt; AccessTokenInfo:\n    \"\"\"Acquire a token, signing in first when that is needed and allowed.\n\n    This is what resource clients call. Concurrent calls for the same client id and\n    scopes are serialised, so a cold start opens one browser window, not many.\n\n    Args:\n        scopes: Scopes to request.\n        client_id: Client id to use. Defaults to the context's client id, then (user\n            flows only) to the Azure PowerShell client id.\n        claims: Claims challenge to satisfy, as a JSON string.\n        force_refresh: Skip cached access tokens.\n\n    Returns:\n        The token with its expiry time and type.\n\n    Raises:\n        InteractionRequired: If the user must sign in and this context may not prompt.\n        ConsentRequired: If consent is missing in the tenant.\n        AuthError: For any other failure.\n    \"\"\"\n    return self._acquire(scopes, client_id, claims, force_refresh, force_interactive=False)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.cached_token","title":"<code>cached_token(scopes: Iterable[str], client_id: str | None) -&gt; AccessTokenInfo | None</code>","text":"<p>Return a still-fresh token from the in-process memo, without locking.</p> <p>Parameters:</p> Name Type Description Default <code>scopes</code> <code>Iterable[str]</code> <p>Scopes the token was requested for.</p> required <code>client_id</code> <code>str | None</code> <p>Client id the token was requested with.</p> required <p>Returns:</p> Type Description <code>AccessTokenInfo | None</code> <p>The token, or <code>None</code> when there is none or it is close to expiry.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def cached_token(self, scopes: Iterable[str], client_id: str | None) -&gt; AccessTokenInfo | None:\n    \"\"\"Return a still-fresh token from the in-process memo, without locking.\n\n    Args:\n        scopes: Scopes the token was requested for.\n        client_id: Client id the token was requested with.\n\n    Returns:\n        The token, or ``None`` when there is none or it is close to expiry.\n    \"\"\"\n    key = (self.resolve_client_id(client_id), frozenset(scopes))\n    info = self._tokens.get(key)\n    if info is not None and info.expires_on - time.time() &gt; _REFRESH_MARGIN_SECONDS:\n        return info\n    return None\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.for_tenant","title":"<code>for_tenant(tenant_id: str) -&gt; AuthContext</code>","text":"<p>Return a sibling context for another tenant.</p> <p>The sibling shares this context's credentials, cache and username, so a user with GDAP access obtains managed-tenant tokens from the one home-tenant sign-in. No network call is made. A sibling never opens a browser: when a token cannot be obtained silently it raises :class:<code>InteractionRequired</code> or :class:<code>ConsentRequired</code> carrying the tenant id.</p> <p>Parameters:</p> Name Type Description Default <code>tenant_id</code> <code>str</code> <p>Tenant id (GUID) or verified domain name of the other tenant.</p> required <p>Returns:</p> Type Description <code>AuthContext</code> <p>The sibling context. Repeated calls return the same object.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def for_tenant(self, tenant_id: str) -&gt; AuthContext:\n    \"\"\"Return a sibling context for another tenant.\n\n    The sibling shares this context's credentials, cache and username, so a user with\n    GDAP access obtains managed-tenant tokens from the one home-tenant sign-in. No network call\n    is made. A sibling never opens a browser: when a token cannot be obtained silently it\n    raises :class:`InteractionRequired` or :class:`ConsentRequired` carrying the tenant\n    id.\n\n    Args:\n        tenant_id: Tenant id (GUID) or verified domain name of the other tenant.\n\n    Returns:\n        The sibling context. Repeated calls return the same object.\n    \"\"\"\n    if tenant_id.lower() == self._tenant_id.lower():\n        return self\n    with self._guard:\n        sibling = self._siblings.get(tenant_id.lower())\n        if sibling is None:\n            sibling = AuthContext.__new__(AuthContext)\n            sibling._tenant_id = tenant_id\n            sibling._client_id = self._client_id\n            sibling._username = self._username\n            sibling._credential = self._credential\n            sibling._cache = self._cache\n            sibling._broker = self._broker\n            sibling._broker_fallback = self._broker_fallback\n            sibling._authority_host = self._authority_host\n            sibling._interactive_timeout = self._interactive_timeout\n            sibling._interactive_allowed = False\n            sibling._init_state()\n            self._siblings[tenant_id.lower()] = sibling\n        return sibling\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.get_token","title":"<code>get_token(*scopes: str, claims: str | None = None, tenant_id: str | None = None, enable_cae: bool = False, **kwargs: Any) -&gt; AccessToken</code>","text":"<p>Request an access token (Azure SDK <code>TokenCredential</code> protocol).</p> <p>Parameters:</p> Name Type Description Default <code>*scopes</code> <code>str</code> <p>Scopes to request, normally one <code>&lt;resource&gt;/.default</code> value.</p> <code>()</code> <code>claims</code> <code>str | None</code> <p>Claims challenge returned by a resource, as a JSON string.</p> <code>None</code> <code>tenant_id</code> <code>str | None</code> <p>Tenant to request the token from. A tenant other than this context's is served by :meth:<code>for_tenant</code>.</p> <code>None</code> <code>enable_cae</code> <code>bool</code> <p>Accepted for protocol compatibility and ignored.</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Ignored.</p> <code>{}</code> <p>Returns:</p> Type Description <code>AccessToken</code> <p>The token and its expiry time.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def get_token(\n    self,\n    *scopes: str,\n    claims: str | None = None,\n    tenant_id: str | None = None,\n    enable_cae: bool = False,\n    **kwargs: Any,\n) -&gt; AccessToken:\n    \"\"\"Request an access token (Azure SDK ``TokenCredential`` protocol).\n\n    Args:\n        *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n        claims: Claims challenge returned by a resource, as a JSON string.\n        tenant_id: Tenant to request the token from. A tenant other than this context's\n            is served by :meth:`for_tenant`.\n        enable_cae: Accepted for protocol compatibility and ignored.\n        **kwargs: Ignored.\n\n    Returns:\n        The token and its expiry time.\n    \"\"\"\n    context = self.for_tenant(tenant_id or self._tenant_id)\n    info = context.acquire_token(scopes, claims=claims)\n    return AccessToken(info.token, info.expires_on)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.get_token_info","title":"<code>get_token_info(*scopes: str, options: TokenRequestOptions | None = None) -&gt; AccessTokenInfo</code>","text":"<p>Request an access token (Azure SDK <code>SupportsTokenInfo</code> protocol).</p> <p>Parameters:</p> Name Type Description Default <code>*scopes</code> <code>str</code> <p>Scopes to request, normally one <code>&lt;resource&gt;/.default</code> value.</p> <code>()</code> <code>options</code> <code>TokenRequestOptions | None</code> <p><code>claims</code> and <code>tenant_id</code> are honoured; <code>enable_cae</code> is ignored.</p> <code>None</code> <p>Returns:</p> Type Description <code>AccessTokenInfo</code> <p>The token with its expiry time and type.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def get_token_info(\n    self, *scopes: str, options: TokenRequestOptions | None = None\n) -&gt; AccessTokenInfo:\n    \"\"\"Request an access token (Azure SDK ``SupportsTokenInfo`` protocol).\n\n    Args:\n        *scopes: Scopes to request, normally one ``&lt;resource&gt;/.default`` value.\n        options: ``claims`` and ``tenant_id`` are honoured; ``enable_cae`` is ignored.\n\n    Returns:\n        The token with its expiry time and type.\n    \"\"\"\n    options = options or {}\n    context = self.for_tenant(options.get(\"tenant_id\") or self._tenant_id)\n    return context.acquire_token(scopes, claims=options.get(\"claims\"))\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.login","title":"<code>login(*, client_id: str, scopes: Iterable[str], force: bool = False) -&gt; None</code>","text":"<p>Sign in now instead of at the first request.</p> <p>Resource clients call this from their own <code>login()</code> with their client id and scopes. Calling it is optional, because the first token request signs in anyway.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client id to sign in to.</p> required <code>scopes</code> <code>Iterable[str]</code> <p>Scopes to request.</p> required <code>force</code> <code>bool</code> <p>Prompt even when a cached token or account exists. Ignored by app flows.</p> <code>False</code> <p>Raises:</p> Type Description <code>InteractionRequired</code> <p>If this is a sibling context and the user must sign in.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def login(self, *, client_id: str, scopes: Iterable[str], force: bool = False) -&gt; None:\n    \"\"\"Sign in now instead of at the first request.\n\n    Resource clients call this from their own ``login()`` with their client id and\n    scopes. Calling it is optional, because the first token request signs in anyway.\n\n    Args:\n        client_id: Client id to sign in to.\n        scopes: Scopes to request.\n        force: Prompt even when a cached token or account exists. Ignored by app flows.\n\n    Raises:\n        InteractionRequired: If this is a sibling context and the user must sign in.\n    \"\"\"\n    self._acquire(scopes, client_id, None, force, force_interactive=force)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.context.AuthContext.resolve_client_id","title":"<code>resolve_client_id(client_id: str | None = None) -&gt; str</code>","text":"<p>Apply the client id precedence rule for a bare token request.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str | None</code> <p>Client id chosen by the caller, if any.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p><code>client_id</code>, else the context's client id, else (user flows) the Azure</p> <code>str</code> <p>PowerShell client id.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an app flow has no client id.</p> Source code in <code>src/azure_auth/auth/context.py</code> <pre><code>def resolve_client_id(self, client_id: str | None = None) -&gt; str:\n    \"\"\"Apply the client id precedence rule for a bare token request.\n\n    Args:\n        client_id: Client id chosen by the caller, if any.\n\n    Returns:\n        ``client_id``, else the context's client id, else (user flows) the Azure\n        PowerShell client id.\n\n    Raises:\n        ValueError: If an app flow has no client id.\n    \"\"\"\n    resolved = client_id or self._client_id\n    if resolved:\n        return resolved\n    if self.is_app_flow:\n        raise ValueError(\"App flows need a client_id; first-party client ids cannot be used\")\n    return AZURE_POWERSHELL_CLIENT_ID\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.AccountSelectionRequired","title":"<code>AccountSelectionRequired</code>","text":"<p>               Bases: <code>AuthError</code></p> <p>More than one cached account matches the configured username.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>class AccountSelectionRequired(AuthError):\n    \"\"\"More than one cached account matches the configured username.\"\"\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.AuthError","title":"<code>AuthError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for every authentication failure raised by this package.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>class AuthError(Exception):\n    \"\"\"Base class for every authentication failure raised by this package.\"\"\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.BrokerUnavailable","title":"<code>BrokerUnavailable</code>","text":"<p>               Bases: <code>AuthError</code></p> <p>The authentication broker was required but cannot be used.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>class BrokerUnavailable(AuthError):\n    \"\"\"The authentication broker was required but cannot be used.\"\"\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.CacheEncryptionUnavailable","title":"<code>CacheEncryptionUnavailable</code>","text":"<p>               Bases: <code>AuthError</code></p> <p>An encrypted disk cache was requested but this platform cannot encrypt it.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>class CacheEncryptionUnavailable(AuthError):\n    \"\"\"An encrypted disk cache was requested but this platform cannot encrypt it.\"\"\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.CertificateUnavailable","title":"<code>CertificateUnavailable</code>","text":"<p>               Bases: <code>AuthError</code></p> <p>The configured certificate or its private key cannot be used.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>class CertificateUnavailable(AuthError):\n    \"\"\"The configured certificate or its private key cannot be used.\"\"\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.ConsentRequired","title":"<code>ConsentRequired</code>","text":"<p>               Bases: <code>AuthError</code></p> <p>The application lacks consent for the requested scopes in the tenant.</p> <p>Attributes:</p> Name Type Description <code>tenant_id</code> <p>Tenant in which consent is missing.</p> <code>scopes</code> <p>Scopes that were requested.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>class ConsentRequired(AuthError):\n    \"\"\"The application lacks consent for the requested scopes in the tenant.\n\n    Attributes:\n        tenant_id: Tenant in which consent is missing.\n        scopes: Scopes that were requested.\n    \"\"\"\n\n    def __init__(self, message: str, *, tenant_id: str, scopes: Sequence[str] = ()) -&gt; None:\n        \"\"\"Create the error.\n\n        Args:\n            message: Human readable description.\n            tenant_id: Tenant in which consent is missing.\n            scopes: Scopes that were requested.\n        \"\"\"\n        super().__init__(message)\n        self.tenant_id = tenant_id\n        self.scopes = tuple(scopes)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.ConsentRequired.__init__","title":"<code>__init__(message: str, *, tenant_id: str, scopes: Sequence[str] = ()) -&gt; None</code>","text":"<p>Create the error.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Human readable description.</p> required <code>tenant_id</code> <code>str</code> <p>Tenant in which consent is missing.</p> required <code>scopes</code> <code>Sequence[str]</code> <p>Scopes that were requested.</p> <code>()</code> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>def __init__(self, message: str, *, tenant_id: str, scopes: Sequence[str] = ()) -&gt; None:\n    \"\"\"Create the error.\n\n    Args:\n        message: Human readable description.\n        tenant_id: Tenant in which consent is missing.\n        scopes: Scopes that were requested.\n    \"\"\"\n    super().__init__(message)\n    self.tenant_id = tenant_id\n    self.scopes = tuple(scopes)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.InteractionRequired","title":"<code>InteractionRequired</code>","text":"<p>               Bases: <code>AuthError</code></p> <p>A token could not be obtained silently and the user must sign in.</p> <p>Attributes:</p> Name Type Description <code>tenant_id</code> <p>Tenant the token was requested from.</p> <code>scopes</code> <p>Scopes that were requested.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>class InteractionRequired(AuthError):\n    \"\"\"A token could not be obtained silently and the user must sign in.\n\n    Attributes:\n        tenant_id: Tenant the token was requested from.\n        scopes: Scopes that were requested.\n    \"\"\"\n\n    def __init__(self, message: str, *, tenant_id: str, scopes: Sequence[str] = ()) -&gt; None:\n        \"\"\"Create the error.\n\n        Args:\n            message: Human readable description.\n            tenant_id: Tenant the token was requested from.\n            scopes: Scopes that were requested.\n        \"\"\"\n        super().__init__(message)\n        self.tenant_id = tenant_id\n        self.scopes = tuple(scopes)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.InteractionRequired.__init__","title":"<code>__init__(message: str, *, tenant_id: str, scopes: Sequence[str] = ()) -&gt; None</code>","text":"<p>Create the error.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Human readable description.</p> required <code>tenant_id</code> <code>str</code> <p>Tenant the token was requested from.</p> required <code>scopes</code> <code>Sequence[str]</code> <p>Scopes that were requested.</p> <code>()</code> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>def __init__(self, message: str, *, tenant_id: str, scopes: Sequence[str] = ()) -&gt; None:\n    \"\"\"Create the error.\n\n    Args:\n        message: Human readable description.\n        tenant_id: Tenant the token was requested from.\n        scopes: Scopes that were requested.\n    \"\"\"\n    super().__init__(message)\n    self.tenant_id = tenant_id\n    self.scopes = tuple(scopes)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.errors.error_from_msal_result","title":"<code>error_from_msal_result(result: Mapping[str, Any] | None, *, tenant_id: str, scopes: Sequence[str]) -&gt; AuthError</code>","text":"<p>Translate a failed MSAL result into an exception.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>Mapping[str, Any] | None</code> <p>The dictionary MSAL returned, or <code>None</code> when MSAL found nothing to return.</p> required <code>tenant_id</code> <code>str</code> <p>Tenant the token was requested from.</p> required <code>scopes</code> <code>Sequence[str]</code> <p>Scopes that were requested.</p> required <p>Returns:</p> Type Description <code>AuthError</code> <p>The exception to raise. The MSAL dictionary itself is never exposed.</p> Source code in <code>src/azure_auth/auth/errors.py</code> <pre><code>def error_from_msal_result(\n    result: Mapping[str, Any] | None,\n    *,\n    tenant_id: str,\n    scopes: Sequence[str],\n) -&gt; AuthError:\n    \"\"\"Translate a failed MSAL result into an exception.\n\n    Args:\n        result: The dictionary MSAL returned, or ``None`` when MSAL found nothing to return.\n        tenant_id: Tenant the token was requested from.\n        scopes: Scopes that were requested.\n\n    Returns:\n        The exception to raise. The MSAL dictionary itself is never exposed.\n    \"\"\"\n    if not result:\n        return InteractionRequired(\n            f\"No cached token for tenant {tenant_id}; the user must sign in.\",\n            tenant_id=tenant_id,\n            scopes=scopes,\n        )\n\n    error = str(result.get(\"error\", \"unknown_error\"))\n    description = str(result.get(\"error_description\", \"\")).strip()\n    markers = {error, str(result.get(\"suberror\", \"\")), str(result.get(\"classification\", \"\"))}\n    codes = {int(code) for code in result.get(\"error_codes\", ()) if str(code).isdigit()}\n    message = f\"{error}: {description}\" if description else error\n\n    if markers &amp; _CONSENT_MARKERS or codes &amp; _CONSENT_ERROR_CODES:\n        return ConsentRequired(message, tenant_id=tenant_id, scopes=scopes)\n    if error in _INTERACTION_ERRORS:\n        return InteractionRequired(message, tenant_id=tenant_id, scopes=scopes)\n    if error in _DECLINED_ERRORS:\n        return AuthError(\n            f\"{message}. No token was issued for {' '.join(scopes) or '(no scopes)'} in tenant \"\n            f\"{tenant_id}. Either the sign-in was cancelled, or the account is not allowed to \"\n            \"consent to these scopes and was shown 'Need admin approval' -- in which case an \"\n            \"administrator has to grant them before this account can use them.\"\n        )\n    return AuthError(message)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.CertStoreCredential","title":"<code>CertStoreCredential</code>","text":"<p>A certificate in the Windows certificate store with a non-exportable key.</p> <p>Assertions are signed PS256, because Microsoft documents PS256 together with the <code>x5t#S256</code> header. The credential switches to RS256, and stays there, when the key storage provider refuses PSS padding or returns a PSS signature that is not PS256. Both are detected here, before anything is sent. An error from Entra ID is never retried with the other algorithm: AADSTS700027 also means the certificate is not registered on the application, so a retry would mistake a configuration error for an algorithm problem.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>class CertStoreCredential:\n    \"\"\"A certificate in the Windows certificate store with a non-exportable key.\n\n    Assertions are signed PS256, because Microsoft documents PS256 together with the\n    ``x5t#S256`` header. The credential switches to RS256, and stays there, when the key\n    storage provider refuses PSS padding or returns a PSS signature that is not PS256. Both\n    are detected here, before anything is sent. An error from Entra ID is never retried with\n    the other algorithm: AADSTS700027 also means the certificate is not registered on the\n    application, so a retry would mistake a configuration error for an algorithm problem.\n    \"\"\"\n\n    def __init__(self, thumbprint: str, store_location: cng.StoreLocation = \"CurrentUser\") -&gt; None:\n        \"\"\"Remember which certificate to use. Nothing is opened yet.\n\n        Args:\n            thumbprint: SHA-1 thumbprint of the certificate.\n            store_location: ``CurrentUser`` or ``LocalMachine``; the ``My`` store is used.\n        \"\"\"\n        self.thumbprint = cng.normalise_thumbprint(thumbprint)\n        self.store_location: cng.StoreLocation = store_location\n        self._padding: cng.Padding = \"pss\"\n        self._certificate_der: bytes | None = None\n        self._public_key: rsa.RSAPublicKey | None = None\n\n    def __repr__(self) -&gt; str:\n        \"\"\"Describe the credential without touching the key.\n\n        Returns:\n            A short description.\n        \"\"\"\n        return (\n            f\"CertStoreCredential(thumbprint={self.thumbprint!r}, \"\n            f\"store_location={self.store_location!r}, algorithm={self.algorithm!r})\"\n        )\n\n    @property\n    def algorithm(self) -&gt; str:\n        \"\"\"The JWT algorithm currently in use: ``PS256`` or ``RS256``.\"\"\"\n        return \"PS256\" if self._padding == \"pss\" else \"RS256\"\n\n    def _load_certificate(self) -&gt; tuple[bytes, rsa.RSAPublicKey]:\n        \"\"\"Read the certificate once, keeping its public key for checking signatures.\n\n        Returns:\n            The DER-encoded certificate and its RSA public key.\n\n        Raises:\n            CertificateUnavailable: If the certificate cannot be found or holds no RSA key.\n        \"\"\"\n        if self._certificate_der is None or self._public_key is None:\n            der = cng.load_certificate_der(self.thumbprint, self.store_location)\n            public_key = x509.load_der_x509_certificate(der).public_key()\n            if not isinstance(public_key, rsa.RSAPublicKey):\n                raise CertificateUnavailable(\n                    f\"certificate {self.thumbprint} does not hold an RSA key\"\n                )\n            self._certificate_der, self._public_key = der, public_key\n        return self._certificate_der, self._public_key\n\n    def _sign(self, digest: bytes, padding: cng.Padding) -&gt; bytes:\n        \"\"\"Sign a digest, and check that a PSS signature really is PS256.\n\n        A TPM 2.0 that is not in FIPS mode signs PSS with the longest salt the key allows,\n        whatever salt length was requested, and the key storage provider still reports\n        success. PS256 requires a salt as long as the digest, so Entra ID would reject the\n        assertion. Verifying against the certificate catches that before it is sent. PKCS #1\n        v1.5 has no parameter a provider could get wrong, so RS256 signatures are not checked.\n\n        Args:\n            digest: The SHA-256 digest to sign.\n            padding: ``pss`` for PS256 or ``pkcs1`` for RS256.\n\n        Returns:\n            The raw signature.\n\n        Raises:\n            SignatureFailed: If the key storage provider refuses to sign with ``padding``.\n            _InvalidPssSignature: If a PSS signature does not verify as PS256.\n        \"\"\"\n        signature = cng.sign_digest(\n            self.thumbprint, digest, padding=padding, store_location=self.store_location\n        )\n        if padding == \"pss\":\n            _certificate_der, public_key = self._load_certificate()\n            try:\n                public_key.verify(signature, digest, _PS256_PADDING, Prehashed(hashes.SHA256()))\n            except InvalidSignature:\n                raise _InvalidPssSignature from None\n        return signature\n\n    def _build(self, client_id: str, token_endpoint: str, padding: cng.Padding) -&gt; str:\n        \"\"\"Build and sign an assertion with one padding.\n\n        The padding is passed in rather than read from the credential, so the ``alg``\n        header always matches the signature even if another thread switches to RS256.\n\n        Args:\n            client_id: Application (client) id.\n            token_endpoint: Tenant-specific token endpoint; becomes the ``aud`` claim.\n            padding: ``pss`` for PS256 or ``pkcs1`` for RS256.\n\n        Returns:\n            The signed JWT.\n        \"\"\"\n        certificate_der, _public_key = self._load_certificate()\n        return build_client_assertion(\n            client_id=client_id,\n            token_endpoint=token_endpoint,\n            certificate_der=certificate_der,\n            algorithm=\"PS256\" if padding == \"pss\" else \"RS256\",\n            sign=lambda digest: self._sign(digest, padding),\n        )\n\n    def build_assertion(self, *, client_id: str, token_endpoint: str) -&gt; str:\n        \"\"\"Build a fresh client assertion for one tenant.\n\n        The ``alg`` header is part of what gets signed, so when PS256 fails the whole\n        assertion is rebuilt as RS256.\n\n        Args:\n            client_id: Application (client) id.\n            token_endpoint: Tenant-specific token endpoint; becomes the ``aud`` claim.\n\n        Returns:\n            The signed JWT.\n\n        Raises:\n            CertificateUnavailable: If the certificate cannot be found, holds no RSA key, or\n                cannot sign even with PKCS #1 v1.5 padding.\n        \"\"\"\n        if self._padding == \"pss\":\n            try:\n                return self._build(client_id, token_endpoint, \"pss\")\n            except cng.SignatureFailed, _InvalidPssSignature:\n                self._padding = \"pkcs1\"\n        return self._build(client_id, token_endpoint, \"pkcs1\")\n\n    def msal_client_credential(self, *, client_id: str, token_endpoint: str) -&gt; Any:\n        \"\"\"Return the value MSAL expects as ``client_credential``.\n\n        The assertion callable is bound to one token endpoint, because the ``aud`` claim is\n        tenant specific. MSAL calls it each time it sends a token request.\n\n        Args:\n            client_id: Application (client) id.\n            token_endpoint: Tenant-specific token endpoint.\n\n        Returns:\n            A dictionary holding the assertion callable.\n        \"\"\"\n\n        def assertion() -&gt; str:\n            return self.build_assertion(client_id=client_id, token_endpoint=token_endpoint)\n\n        return {\"client_assertion\": assertion}\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.CertStoreCredential.algorithm","title":"<code>algorithm: str</code>  <code>property</code>","text":"<p>The JWT algorithm currently in use: <code>PS256</code> or <code>RS256</code>.</p>"},{"location":"reference/auth/#azure_auth.auth.credentials.CertStoreCredential.__init__","title":"<code>__init__(thumbprint: str, store_location: cng.StoreLocation = 'CurrentUser') -&gt; None</code>","text":"<p>Remember which certificate to use. Nothing is opened yet.</p> <p>Parameters:</p> Name Type Description Default <code>thumbprint</code> <code>str</code> <p>SHA-1 thumbprint of the certificate.</p> required <code>store_location</code> <code>StoreLocation</code> <p><code>CurrentUser</code> or <code>LocalMachine</code>; the <code>My</code> store is used.</p> <code>'CurrentUser'</code> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>def __init__(self, thumbprint: str, store_location: cng.StoreLocation = \"CurrentUser\") -&gt; None:\n    \"\"\"Remember which certificate to use. Nothing is opened yet.\n\n    Args:\n        thumbprint: SHA-1 thumbprint of the certificate.\n        store_location: ``CurrentUser`` or ``LocalMachine``; the ``My`` store is used.\n    \"\"\"\n    self.thumbprint = cng.normalise_thumbprint(thumbprint)\n    self.store_location: cng.StoreLocation = store_location\n    self._padding: cng.Padding = \"pss\"\n    self._certificate_der: bytes | None = None\n    self._public_key: rsa.RSAPublicKey | None = None\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.CertStoreCredential.__repr__","title":"<code>__repr__() -&gt; str</code>","text":"<p>Describe the credential without touching the key.</p> <p>Returns:</p> Type Description <code>str</code> <p>A short description.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Describe the credential without touching the key.\n\n    Returns:\n        A short description.\n    \"\"\"\n    return (\n        f\"CertStoreCredential(thumbprint={self.thumbprint!r}, \"\n        f\"store_location={self.store_location!r}, algorithm={self.algorithm!r})\"\n    )\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.CertStoreCredential.build_assertion","title":"<code>build_assertion(*, client_id: str, token_endpoint: str) -&gt; str</code>","text":"<p>Build a fresh client assertion for one tenant.</p> <p>The <code>alg</code> header is part of what gets signed, so when PS256 fails the whole assertion is rebuilt as RS256.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Application (client) id.</p> required <code>token_endpoint</code> <code>str</code> <p>Tenant-specific token endpoint; becomes the <code>aud</code> claim.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The signed JWT.</p> <p>Raises:</p> Type Description <code>CertificateUnavailable</code> <p>If the certificate cannot be found, holds no RSA key, or cannot sign even with PKCS #1 v1.5 padding.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>def build_assertion(self, *, client_id: str, token_endpoint: str) -&gt; str:\n    \"\"\"Build a fresh client assertion for one tenant.\n\n    The ``alg`` header is part of what gets signed, so when PS256 fails the whole\n    assertion is rebuilt as RS256.\n\n    Args:\n        client_id: Application (client) id.\n        token_endpoint: Tenant-specific token endpoint; becomes the ``aud`` claim.\n\n    Returns:\n        The signed JWT.\n\n    Raises:\n        CertificateUnavailable: If the certificate cannot be found, holds no RSA key, or\n            cannot sign even with PKCS #1 v1.5 padding.\n    \"\"\"\n    if self._padding == \"pss\":\n        try:\n            return self._build(client_id, token_endpoint, \"pss\")\n        except cng.SignatureFailed, _InvalidPssSignature:\n            self._padding = \"pkcs1\"\n    return self._build(client_id, token_endpoint, \"pkcs1\")\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.CertStoreCredential.msal_client_credential","title":"<code>msal_client_credential(*, client_id: str, token_endpoint: str) -&gt; Any</code>","text":"<p>Return the value MSAL expects as <code>client_credential</code>.</p> <p>The assertion callable is bound to one token endpoint, because the <code>aud</code> claim is tenant specific. MSAL calls it each time it sends a token request.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Application (client) id.</p> required <code>token_endpoint</code> <code>str</code> <p>Tenant-specific token endpoint.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A dictionary holding the assertion callable.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>def msal_client_credential(self, *, client_id: str, token_endpoint: str) -&gt; Any:\n    \"\"\"Return the value MSAL expects as ``client_credential``.\n\n    The assertion callable is bound to one token endpoint, because the ``aud`` claim is\n    tenant specific. MSAL calls it each time it sends a token request.\n\n    Args:\n        client_id: Application (client) id.\n        token_endpoint: Tenant-specific token endpoint.\n\n    Returns:\n        A dictionary holding the assertion callable.\n    \"\"\"\n\n    def assertion() -&gt; str:\n        return self.build_assertion(client_id=client_id, token_endpoint=token_endpoint)\n\n    return {\"client_assertion\": assertion}\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.PemCertificateCredential","title":"<code>PemCertificateCredential</code>  <code>dataclass</code>","text":"<p>A certificate with an exportable private key, held as PEM text.</p> <p>Attributes:</p> Name Type Description <code>certificate_pem</code> <code>str</code> <p>The certificate in PEM format.</p> <code>private_key_pem</code> <code>str</code> <p>The private key in PEM format.</p> <code>passphrase</code> <code>str | None</code> <p>Passphrase protecting <code>private_key_pem</code>, if it is encrypted.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>@dataclass(frozen=True)\nclass PemCertificateCredential:\n    \"\"\"A certificate with an exportable private key, held as PEM text.\n\n    Attributes:\n        certificate_pem: The certificate in PEM format.\n        private_key_pem: The private key in PEM format.\n        passphrase: Passphrase protecting ``private_key_pem``, if it is encrypted.\n    \"\"\"\n\n    certificate_pem: str\n    private_key_pem: str = field(repr=False)\n    passphrase: str | None = field(default=None, repr=False)\n\n    @classmethod\n    def from_pfx(cls, pfx: bytes | str | Path, password: str | None) -&gt; PemCertificateCredential:\n        \"\"\"Build the credential from a PKCS#12 (PFX) archive.\n\n        Args:\n            pfx: The archive bytes, or a path to the file.\n            password: Password protecting the archive, if any.\n\n        Returns:\n            The credential, with certificate and key converted to PEM in memory.\n\n        Raises:\n            CertificateUnavailable: If the archive cannot be read or lacks a key or\n                certificate.\n        \"\"\"\n        data = pfx if isinstance(pfx, bytes) else Path(pfx).read_bytes()\n        try:\n            key, certificate, _chain = pkcs12.load_key_and_certificates(\n                data, password.encode(\"utf-8\") if password else None\n            )\n        except ValueError as exc:\n            raise CertificateUnavailable(f\"Cannot read the PFX archive: {exc}\") from exc\n        if key is None or certificate is None:\n            raise CertificateUnavailable(\"The PFX archive has no private key or no certificate\")\n        return cls(\n            certificate_pem=certificate.public_bytes(serialization.Encoding.PEM).decode(\"ascii\"),\n            private_key_pem=key.private_bytes(\n                serialization.Encoding.PEM,\n                serialization.PrivateFormat.PKCS8,\n                serialization.NoEncryption(),\n            ).decode(\"ascii\"),\n        )\n\n    def msal_client_credential(self, *, client_id: str, token_endpoint: str) -&gt; Any:\n        \"\"\"Return the value MSAL expects as ``client_credential``.\n\n        MSAL derives the SHA-256 thumbprint from ``public_certificate`` and signs the\n        assertion itself.\n\n        Args:\n            client_id: Application (client) id. Unused; MSAL builds the assertion.\n            token_endpoint: Tenant-specific token endpoint. Unused; MSAL builds the assertion.\n\n        Returns:\n            The credential dictionary.\n        \"\"\"\n        credential = {\n            \"private_key\": self.private_key_pem,\n            \"public_certificate\": self.certificate_pem,\n        }\n        if self.passphrase:\n            credential[\"passphrase\"] = self.passphrase\n        return credential\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.PemCertificateCredential.from_pfx","title":"<code>from_pfx(pfx: bytes | str | Path, password: str | None) -&gt; PemCertificateCredential</code>  <code>classmethod</code>","text":"<p>Build the credential from a PKCS#12 (PFX) archive.</p> <p>Parameters:</p> Name Type Description Default <code>pfx</code> <code>bytes | str | Path</code> <p>The archive bytes, or a path to the file.</p> required <code>password</code> <code>str | None</code> <p>Password protecting the archive, if any.</p> required <p>Returns:</p> Type Description <code>PemCertificateCredential</code> <p>The credential, with certificate and key converted to PEM in memory.</p> <p>Raises:</p> Type Description <code>CertificateUnavailable</code> <p>If the archive cannot be read or lacks a key or certificate.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>@classmethod\ndef from_pfx(cls, pfx: bytes | str | Path, password: str | None) -&gt; PemCertificateCredential:\n    \"\"\"Build the credential from a PKCS#12 (PFX) archive.\n\n    Args:\n        pfx: The archive bytes, or a path to the file.\n        password: Password protecting the archive, if any.\n\n    Returns:\n        The credential, with certificate and key converted to PEM in memory.\n\n    Raises:\n        CertificateUnavailable: If the archive cannot be read or lacks a key or\n            certificate.\n    \"\"\"\n    data = pfx if isinstance(pfx, bytes) else Path(pfx).read_bytes()\n    try:\n        key, certificate, _chain = pkcs12.load_key_and_certificates(\n            data, password.encode(\"utf-8\") if password else None\n        )\n    except ValueError as exc:\n        raise CertificateUnavailable(f\"Cannot read the PFX archive: {exc}\") from exc\n    if key is None or certificate is None:\n        raise CertificateUnavailable(\"The PFX archive has no private key or no certificate\")\n    return cls(\n        certificate_pem=certificate.public_bytes(serialization.Encoding.PEM).decode(\"ascii\"),\n        private_key_pem=key.private_bytes(\n            serialization.Encoding.PEM,\n            serialization.PrivateFormat.PKCS8,\n            serialization.NoEncryption(),\n        ).decode(\"ascii\"),\n    )\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.PemCertificateCredential.msal_client_credential","title":"<code>msal_client_credential(*, client_id: str, token_endpoint: str) -&gt; Any</code>","text":"<p>Return the value MSAL expects as <code>client_credential</code>.</p> <p>MSAL derives the SHA-256 thumbprint from <code>public_certificate</code> and signs the assertion itself.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Application (client) id. Unused; MSAL builds the assertion.</p> required <code>token_endpoint</code> <code>str</code> <p>Tenant-specific token endpoint. Unused; MSAL builds the assertion.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The credential dictionary.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>def msal_client_credential(self, *, client_id: str, token_endpoint: str) -&gt; Any:\n    \"\"\"Return the value MSAL expects as ``client_credential``.\n\n    MSAL derives the SHA-256 thumbprint from ``public_certificate`` and signs the\n    assertion itself.\n\n    Args:\n        client_id: Application (client) id. Unused; MSAL builds the assertion.\n        token_endpoint: Tenant-specific token endpoint. Unused; MSAL builds the assertion.\n\n    Returns:\n        The credential dictionary.\n    \"\"\"\n    credential = {\n        \"private_key\": self.private_key_pem,\n        \"public_certificate\": self.certificate_pem,\n    }\n    if self.passphrase:\n        credential[\"passphrase\"] = self.passphrase\n    return credential\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.SecretCredential","title":"<code>SecretCredential</code>  <code>dataclass</code>","text":"<p>A client secret.</p> <p>Attributes:</p> Name Type Description <code>secret</code> <code>str</code> <p>The secret value.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>@dataclass(frozen=True)\nclass SecretCredential:\n    \"\"\"A client secret.\n\n    Attributes:\n        secret: The secret value.\n    \"\"\"\n\n    secret: str = field(repr=False)\n\n    def msal_client_credential(self, *, client_id: str, token_endpoint: str) -&gt; Any:\n        \"\"\"Return the value MSAL expects as ``client_credential``.\n\n        Args:\n            client_id: Application (client) id. Unused for secrets.\n            token_endpoint: Tenant-specific token endpoint. Unused for secrets.\n\n        Returns:\n            The client secret.\n        \"\"\"\n        return self.secret\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.SecretCredential.msal_client_credential","title":"<code>msal_client_credential(*, client_id: str, token_endpoint: str) -&gt; Any</code>","text":"<p>Return the value MSAL expects as <code>client_credential</code>.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Application (client) id. Unused for secrets.</p> required <code>token_endpoint</code> <code>str</code> <p>Tenant-specific token endpoint. Unused for secrets.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The client secret.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>def msal_client_credential(self, *, client_id: str, token_endpoint: str) -&gt; Any:\n    \"\"\"Return the value MSAL expects as ``client_credential``.\n\n    Args:\n        client_id: Application (client) id. Unused for secrets.\n        token_endpoint: Tenant-specific token endpoint. Unused for secrets.\n\n    Returns:\n        The client secret.\n    \"\"\"\n    return self.secret\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.credentials.build_client_assertion","title":"<code>build_client_assertion(*, client_id: str, token_endpoint: str, certificate_der: bytes, algorithm: str, sign: Callable[[bytes], bytes]) -&gt; str</code>","text":"<p>Build the signed JWT that proves possession of a certificate's private key.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Application (client) id; becomes <code>iss</code> and <code>sub</code>.</p> required <code>token_endpoint</code> <code>str</code> <p>Tenant-specific token endpoint; becomes <code>aud</code>.</p> required <code>certificate_der</code> <code>bytes</code> <p>DER encoding of the certificate; hashed into <code>x5t#S256</code>.</p> required <code>algorithm</code> <code>str</code> <p><code>PS256</code> or <code>RS256</code>; must match what <code>sign</code> produces.</p> required <code>sign</code> <code>Callable[[bytes], bytes]</code> <p>Callable that signs a SHA-256 digest and returns the raw signature.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The compact-serialised JWT.</p> Source code in <code>src/azure_auth/auth/credentials.py</code> <pre><code>def build_client_assertion(\n    *,\n    client_id: str,\n    token_endpoint: str,\n    certificate_der: bytes,\n    algorithm: str,\n    sign: Callable[[bytes], bytes],\n) -&gt; str:\n    \"\"\"Build the signed JWT that proves possession of a certificate's private key.\n\n    Args:\n        client_id: Application (client) id; becomes ``iss`` and ``sub``.\n        token_endpoint: Tenant-specific token endpoint; becomes ``aud``.\n        certificate_der: DER encoding of the certificate; hashed into ``x5t#S256``.\n        algorithm: ``PS256`` or ``RS256``; must match what ``sign`` produces.\n        sign: Callable that signs a SHA-256 digest and returns the raw signature.\n\n    Returns:\n        The compact-serialised JWT.\n    \"\"\"\n    now = int(time.time())\n    header = {\n        \"alg\": algorithm,\n        \"typ\": \"JWT\",\n        \"x5t#S256\": _b64url(hashlib.sha256(certificate_der).digest()),\n    }\n    claims = {\n        \"aud\": token_endpoint,\n        \"iss\": client_id,\n        \"sub\": client_id,\n        \"jti\": str(uuid.uuid4()),\n        \"iat\": now,\n        \"nbf\": now,\n        \"exp\": now + ASSERTION_LIFETIME_SECONDS,\n    }\n    signing_input = \".\".join(\n        _b64url(json.dumps(part, separators=(\",\", \":\")).encode(\"utf-8\"))\n        for part in (header, claims)\n    )\n    signature = sign(hashlib.sha256(signing_input.encode(\"ascii\")).digest())\n    return f\"{signing_input}.{_b64url(signature)}\"\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.cache.build_cache","title":"<code>build_cache(kind: CacheKind, cache_path: str | Path | None = None) -&gt; Any</code>","text":"<p>Create the MSAL token cache for an :class:<code>~azure_auth.AuthContext</code>.</p> <p>Parameters:</p> Name Type Description Default <code>kind</code> <code>CacheKind</code> <p><code>memory</code> for a process-local cache, <code>disk</code> for an encrypted file.</p> required <code>cache_path</code> <code>str | Path | None</code> <p>File to use for the disk cache. Defaults to :func:<code>default_cache_path</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>An <code>msal.TokenCache</code> (or subclass) instance.</p> <p>Raises:</p> Type Description <code>CacheEncryptionUnavailable</code> <p>If <code>kind</code> is <code>disk</code> and the platform cannot encrypt the file (for example Linux without libsecret).</p> <code>ValueError</code> <p>If <code>kind</code> is not a known cache kind.</p> Source code in <code>src/azure_auth/auth/cache.py</code> <pre><code>def build_cache(kind: CacheKind, cache_path: str | Path | None = None) -&gt; Any:\n    \"\"\"Create the MSAL token cache for an :class:`~azure_auth.AuthContext`.\n\n    Args:\n        kind: ``memory`` for a process-local cache, ``disk`` for an encrypted file.\n        cache_path: File to use for the disk cache. Defaults to :func:`default_cache_path`.\n\n    Returns:\n        An ``msal.TokenCache`` (or subclass) instance.\n\n    Raises:\n        CacheEncryptionUnavailable: If ``kind`` is ``disk`` and the platform cannot encrypt\n            the file (for example Linux without libsecret).\n        ValueError: If ``kind`` is not a known cache kind.\n    \"\"\"\n    if kind == \"memory\":\n        return msal.TokenCache()\n    if kind != \"disk\":\n        raise ValueError(f\"cache must be 'memory' or 'disk', not {kind!r}\")\n\n    path = Path(cache_path) if cache_path is not None else default_cache_path()\n    path.parent.mkdir(parents=True, exist_ok=True)\n    try:\n        persistence = msal_extensions.build_encrypted_persistence(str(path))\n    except Exception as exc:\n        raise CacheEncryptionUnavailable(\n            f\"Cannot create an encrypted token cache on this platform: {exc}. \"\n            \"Use cache='memory' instead.\"\n        ) from exc\n    if not getattr(persistence, \"is_encrypted\", False):\n        raise CacheEncryptionUnavailable(\n            \"The token cache persistence on this platform is not encrypted. \"\n            \"Use cache='memory' instead.\"\n        )\n    return msal_extensions.PersistedTokenCache(persistence)\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.cache.default_cache_path","title":"<code>default_cache_path() -&gt; Path</code>","text":"<p>Return the default location of the encrypted disk cache.</p> <p>Returns:</p> Type Description <code>Path</code> <p>A file path under the per-user cache directory of this platform.</p> Source code in <code>src/azure_auth/auth/cache.py</code> <pre><code>def default_cache_path() -&gt; Path:\n    \"\"\"Return the default location of the encrypted disk cache.\n\n    Returns:\n        A file path under the per-user cache directory of this platform.\n    \"\"\"\n    return Path(platformdirs.user_cache_dir(_APP_NAME, appauthor=False)) / _CACHE_FILE_NAME\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.cng.SignatureFailed","title":"<code>SignatureFailed</code>","text":"<p>               Bases: <code>CertificateUnavailable</code></p> <p>The key storage provider refused to sign with the requested padding.</p> <p>Attributes:</p> Name Type Description <code>status</code> <p>The <code>SECURITY_STATUS</code> returned by <code>NCryptSignHash</code>.</p> Source code in <code>src/azure_auth/auth/cng.py</code> <pre><code>class SignatureFailed(CertificateUnavailable):\n    \"\"\"The key storage provider refused to sign with the requested padding.\n\n    Attributes:\n        status: The ``SECURITY_STATUS`` returned by ``NCryptSignHash``.\n    \"\"\"\n\n    def __init__(self, message: str, *, status: int) -&gt; None:\n        \"\"\"Create the error.\n\n        Args:\n            message: Human readable description.\n            status: The ``SECURITY_STATUS`` returned by ``NCryptSignHash``.\n        \"\"\"\n        super().__init__(message)\n        self.status = status\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.cng.SignatureFailed.__init__","title":"<code>__init__(message: str, *, status: int) -&gt; None</code>","text":"<p>Create the error.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Human readable description.</p> required <code>status</code> <code>int</code> <p>The <code>SECURITY_STATUS</code> returned by <code>NCryptSignHash</code>.</p> required Source code in <code>src/azure_auth/auth/cng.py</code> <pre><code>def __init__(self, message: str, *, status: int) -&gt; None:\n    \"\"\"Create the error.\n\n    Args:\n        message: Human readable description.\n        status: The ``SECURITY_STATUS`` returned by ``NCryptSignHash``.\n    \"\"\"\n    super().__init__(message)\n    self.status = status\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.cng.load_certificate_der","title":"<code>load_certificate_der(thumbprint: str, store_location: StoreLocation = 'CurrentUser') -&gt; bytes</code>","text":"<p>Raise, because the Windows certificate store only exists on Windows.</p> <p>Parameters:</p> Name Type Description Default <code>thumbprint</code> <code>str</code> <p>SHA-1 thumbprint of the certificate.</p> required <code>store_location</code> <code>StoreLocation</code> <p><code>CurrentUser</code> or <code>LocalMachine</code>.</p> <code>'CurrentUser'</code> <p>Raises:</p> Type Description <code>CertificateUnavailable</code> <p>Always.</p> Source code in <code>src/azure_auth/auth/cng.py</code> <pre><code>def load_certificate_der(\n    thumbprint: str, store_location: StoreLocation = \"CurrentUser\"\n) -&gt; bytes:\n    \"\"\"Raise, because the Windows certificate store only exists on Windows.\n\n    Args:\n        thumbprint: SHA-1 thumbprint of the certificate.\n        store_location: ``CurrentUser`` or ``LocalMachine``.\n\n    Raises:\n        CertificateUnavailable: Always.\n    \"\"\"\n    raise CertificateUnavailable(\n        \"certificate_thumbprint needs the Windows certificate store; \"\n        \"use certificate_pem or certificate_pfx on this platform\"\n    )\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.cng.normalise_thumbprint","title":"<code>normalise_thumbprint(thumbprint: str) -&gt; str</code>","text":"<p>Return a SHA-1 thumbprint as 40 upper-case hex characters.</p> <p>Parameters:</p> Name Type Description Default <code>thumbprint</code> <code>str</code> <p>Thumbprint as shown by Windows; spaces and colons are ignored.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The cleaned thumbprint.</p> <p>Raises:</p> Type Description <code>CertificateUnavailable</code> <p>If the value is not a SHA-1 thumbprint.</p> Source code in <code>src/azure_auth/auth/cng.py</code> <pre><code>def normalise_thumbprint(thumbprint: str) -&gt; str:\n    \"\"\"Return a SHA-1 thumbprint as 40 upper-case hex characters.\n\n    Args:\n        thumbprint: Thumbprint as shown by Windows; spaces and colons are ignored.\n\n    Returns:\n        The cleaned thumbprint.\n\n    Raises:\n        CertificateUnavailable: If the value is not a SHA-1 thumbprint.\n    \"\"\"\n    cleaned = thumbprint.replace(\" \", \"\").replace(\":\", \"\").upper()\n    if len(cleaned) != _SHA1_HEX_LENGTH or any(c not in \"0123456789ABCDEF\" for c in cleaned):\n        raise CertificateUnavailable(\n            \"certificate_thumbprint must be a SHA-1 thumbprint (40 hexadecimal characters)\"\n        )\n    return cleaned\n</code></pre>"},{"location":"reference/auth/#azure_auth.auth.cng.sign_digest","title":"<code>sign_digest(thumbprint: str, digest: bytes, *, padding: Padding, store_location: StoreLocation = 'CurrentUser') -&gt; bytes</code>","text":"<p>Raise, because the Windows certificate store only exists on Windows.</p> <p>Parameters:</p> Name Type Description Default <code>thumbprint</code> <code>str</code> <p>SHA-1 thumbprint of the certificate.</p> required <code>digest</code> <code>bytes</code> <p>The 32-byte SHA-256 digest to sign.</p> required <code>padding</code> <code>Padding</code> <p><code>pss</code> or <code>pkcs1</code>.</p> required <code>store_location</code> <code>StoreLocation</code> <p><code>CurrentUser</code> or <code>LocalMachine</code>.</p> <code>'CurrentUser'</code> <p>Raises:</p> Type Description <code>CertificateUnavailable</code> <p>Always.</p> Source code in <code>src/azure_auth/auth/cng.py</code> <pre><code>def sign_digest(\n    thumbprint: str,\n    digest: bytes,\n    *,\n    padding: Padding,\n    store_location: StoreLocation = \"CurrentUser\",\n) -&gt; bytes:\n    \"\"\"Raise, because the Windows certificate store only exists on Windows.\n\n    Args:\n        thumbprint: SHA-1 thumbprint of the certificate.\n        digest: The 32-byte SHA-256 digest to sign.\n        padding: ``pss`` or ``pkcs1``.\n        store_location: ``CurrentUser`` or ``LocalMachine``.\n\n    Raises:\n        CertificateUnavailable: Always.\n    \"\"\"\n    raise CertificateUnavailable(\n        \"certificate_thumbprint needs the Windows certificate store; \"\n        \"use certificate_pem or certificate_pfx on this platform\"\n    )\n</code></pre>"},{"location":"reference/clients/","title":"Clients","text":"<p>Shared behaviour of every resource client: tokens, retries and errors.</p> <p>Thin client for Microsoft Graph.</p> <p>Thin client for Azure Resource Manager.</p> <p>For the typed management SDKs (<code>azure-mgmt-*</code>) no client is needed: pass the :class:<code>~azure_auth.AuthContext</code> (or its <code>aio</code> view) as <code>credential=</code>.</p> <p>Cmdlet invocation over the Exchange admin REST API.</p> <p>Exchange Online and Security &amp; Compliance PowerShell both talk to an undocumented REST endpoint, <code>/adminapi/&lt;version&gt;/&lt;tenant id&gt;/InvokeCommand</code>. The request shape used here was read from the ExchangeOnlineManagement module (3.10.1). Because the endpoint is undocumented, everything that knows about it lives in this module.</p> <p>Client for Exchange Online cmdlets.</p> <p>Client for Security &amp; Compliance (IPPS) cmdlets.</p> <p>Thin wrapper over the Azure Key Vault SDK clients.</p> <p>The Azure SDK does the work; this wrapper only wires an :class:<code>~azure_auth.AuthContext</code> into it and exposes the handful of calls that authentication code tends to need. For anything else, use the SDK clients directly with <code>credential=auth.aio</code>.</p> <p>Needs the <code>keyvault</code> extra: <code>pip install azure-auth[keyvault]</code>.</p> <p>Exceptions raised when a resource rejects a request.</p>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient","title":"<code>ResourceClient</code>","text":"<p>Base class: an authenticated HTTP client for one resource.</p> <p>Subclasses set the class attributes and add resource-specific verbs.</p> <p>Attributes:</p> Name Type Description <code>DEFAULT_CLIENT_ID</code> <code>str</code> <p>First-party client id used for user flows when neither the client nor the context names one.</p> <code>RESOURCE</code> <code>str</code> <p>Resource identifier; <code>&lt;RESOURCE&gt;/.default</code> is the default scope.</p> <code>ERROR_CLASS</code> <code>type[ResourceError]</code> <p>Exception type raised for error responses.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>class ResourceClient:\n    \"\"\"Base class: an authenticated HTTP client for one resource.\n\n    Subclasses set the class attributes and add resource-specific verbs.\n\n    Attributes:\n        DEFAULT_CLIENT_ID: First-party client id used for user flows when neither the client\n            nor the context names one.\n        RESOURCE: Resource identifier; ``&lt;RESOURCE&gt;/.default`` is the default scope.\n        ERROR_CLASS: Exception type raised for error responses.\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str]\n    RESOURCE: ClassVar[str]\n    ERROR_CLASS: ClassVar[type[ResourceError]] = ResourceError\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        base_url: str,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        timeout: float = 100.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.AsyncBaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            base_url: Base URL that relative request paths are joined to.\n            client_id: Client id for this client. Defaults to the context's client id, then\n                (user flows only) to ``DEFAULT_CLIENT_ID``.\n            scopes: Delegated scopes to request in a user flow; short names are qualified\n                with the resource. Defaults to ``&lt;RESOURCE&gt;/.default``. App flows always use\n                ``.default``.\n            timeout: Timeout for each HTTP request, in seconds.\n            max_retries: How often a throttled request (429, 503) is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n\n        Raises:\n            ValueError: If an app flow has no client id, or is given delegated scopes.\n        \"\"\"\n        self._auth = auth\n        self._tokens = auth.aio\n        self._client_id = self._resolve_client_id(client_id)\n        self._scopes = self._resolve_scopes(scopes)\n        self._base_url = httpx.URL(base_url.rstrip(\"/\") + \"/\")\n        self._max_retries = max_retries\n        self._max_retry_wait = max_retry_wait\n        self._http = httpx.AsyncClient(timeout=timeout, transport=transport)\n\n    # ------------------------------------------------------------------ configuration\n\n    @property\n    def auth(self) -&gt; AuthContext:\n        \"\"\"The authentication context behind this client.\"\"\"\n        return self._auth\n\n    @property\n    def client_id(self) -&gt; str:\n        \"\"\"The client id this client requests tokens with.\"\"\"\n        return self._client_id\n\n    @property\n    def scopes(self) -&gt; tuple[str, ...]:\n        \"\"\"The scopes this client requests.\"\"\"\n        return tuple(self._scopes)\n\n    @property\n    def base_url(self) -&gt; str:\n        \"\"\"The base URL that relative request paths are joined to.\"\"\"\n        return str(self._base_url).rstrip(\"/\")\n\n    def _resolve_client_id(self, client_id: str | None) -&gt; str:\n        \"\"\"Apply the client id precedence rule.\n\n        Args:\n            client_id: Client id given to this client, if any.\n\n        Returns:\n            The client's id, else the context's, else (user flows) the resource default.\n        \"\"\"\n        resolved = client_id or self._auth.client_id\n        if resolved:\n            return resolved\n        if self._auth.is_app_flow:\n            raise ValueError(\n                \"App flows need a client_id on the AuthContext or on the client; \"\n                \"first-party client ids cannot be used with a secret or certificate\"\n            )\n        return self.DEFAULT_CLIENT_ID\n\n    def _resolve_scopes(self, scopes: Sequence[str] | None) -&gt; list[str]:\n        \"\"\"Work out which scopes to request.\n\n        Args:\n            scopes: Scopes given to this client, if any.\n\n        Returns:\n            Fully qualified scopes.\n        \"\"\"\n        default = [f\"{self.RESOURCE}/.default\"]\n        # MSAL adds the OpenID Connect scopes itself and rejects them when they are passed in.\n        wanted = [scope for scope in scopes or () if scope not in _RESERVED_SCOPES]\n        if not wanted:\n            return default\n        qualified = [\n            scope if \"://\" in scope or scope == \"email\" else f\"{self.RESOURCE}/{scope}\"\n            for scope in wanted\n        ]\n        if self._auth.is_app_flow and qualified != default:\n            raise ValueError(f\"App flows always request {default[0]}; do not pass scopes\")\n        return qualified\n\n    # ------------------------------------------------------------------ lifecycle\n\n    async def login(self, *, force: bool = False) -&gt; None:\n        \"\"\"Sign in now instead of at the first request.\n\n        Args:\n            force: Prompt even when a cached token or account exists.\n        \"\"\"\n        await self._tokens.login(client_id=self._client_id, scopes=self._scopes, force=force)\n\n    async def aclose(self) -&gt; None:\n        \"\"\"Close the underlying HTTP connections.\"\"\"\n        await self._http.aclose()\n\n    async def __aenter__(self) -&gt; Self:\n        \"\"\"Enter the context manager.\n\n        Returns:\n            This client.\n        \"\"\"\n        return self\n\n    async def __aexit__(\n        self,\n        exc_type: type[BaseException] | None = None,\n        exc_value: BaseException | None = None,\n        traceback: TracebackType | None = None,\n    ) -&gt; None:\n        \"\"\"Close the client when leaving the context manager.\"\"\"\n        await self.aclose()\n\n    # ------------------------------------------------------------------ requests\n\n    def _url(self, url: str) -&gt; httpx.URL:\n        \"\"\"Resolve a request URL and make sure the token may be sent to it.\n\n        Args:\n            url: A path relative to the base URL, or an absolute URL such as a next link.\n\n        Returns:\n            The absolute URL.\n\n        Raises:\n            ValueError: If the URL points at a host this client does not trust.\n        \"\"\"\n        target = self._base_url.join(url.lstrip(\"/\") if \"://\" not in url else url)\n        trusted = target.scheme == \"https\" and self._is_trusted_host(target.host)\n        # Credentials in the URL would make httpx replace the bearer token with basic auth.\n        if not trusted or target.userinfo:\n            raise ValueError(f\"Refusing to send a token for {self.RESOURCE} to {target}\")\n        return target\n\n    def _is_trusted_host(self, host: str) -&gt; bool:\n        \"\"\"Decide whether a host may receive this client's token.\n\n        Args:\n            host: Host name of the request URL.\n\n        Returns:\n            ``True`` when the host is the base URL's host.\n        \"\"\"\n        return host.lower() == self._base_url.host.lower()\n\n    async def request(\n        self,\n        method: str,\n        url: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        json: Any = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; httpx.Response:\n        \"\"\"Send an authenticated request and return the successful response.\n\n        Throttled requests (429, 503) are retried, honouring ``Retry-After``. A 401 is\n        retried once: with the claims from the challenge when the resource sent one, else\n        with a freshly acquired token.\n\n        Args:\n            method: HTTP method.\n            url: Path relative to the base URL, or an absolute URL on the same host.\n            params: Query parameters.\n            json: Request body, serialised as JSON.\n            headers: Extra request headers.\n\n        Returns:\n            The response, whose status is below 400.\n\n        Raises:\n            ResourceError: (the subclass in ``ERROR_CLASS``) for an error response.\n        \"\"\"\n        target = self._url(url)\n        retries = 0\n        challenged = False\n        claims: str | None = None\n        force_refresh = False\n        while True:\n            token = await self._tokens.acquire_token(\n                self._scopes, client_id=self._client_id, claims=claims, force_refresh=force_refresh\n            )\n            claims, force_refresh = None, False\n            response = await self._http.request(\n                method,\n                target,\n                params=params,\n                json=json,\n                headers={**(headers or {}), \"Authorization\": f\"Bearer {token.token}\"},\n            )\n            if response.status_code == httpx.codes.UNAUTHORIZED and not challenged:\n                challenged = True\n                claims = claims_from_challenge(response.headers.get(\"WWW-Authenticate\"))\n                force_refresh = claims is None\n                continue\n            if response.status_code in _RETRY_STATUSES and retries &lt; self._max_retries:\n                retries += 1\n                await asyncio.sleep(retry_delay(response, retries, self._max_retry_wait))\n                continue\n            if response.status_code &gt;= httpx.codes.BAD_REQUEST:\n                raise self._error(response)\n            return response\n\n    def _error(self, response: httpx.Response) -&gt; ResourceError:\n        \"\"\"Build the exception for an error response.\n\n        Args:\n            response: The error response.\n\n        Returns:\n            An instance of ``ERROR_CLASS``.\n        \"\"\"\n        body: Any\n        try:\n            body = response.json()\n        except ValueError:\n            body = response.text\n        error = body.get(\"error\") if isinstance(body, dict) else None\n        code = message = None\n        if isinstance(error, dict):\n            code, message = error.get(\"code\"), error.get(\"message\")\n        elif isinstance(error, str):\n            code, message = error, body.get(\"error_description\")\n        request_id = next(\n            (response.headers[name] for name in _REQUEST_ID_HEADERS if name in response.headers),\n            None,\n        )\n        summary = f\"{response.request.method} {response.request.url} -&gt; {response.status_code}\"\n        detail = \": \".join(str(part) for part in (code, message) if part)\n        return self.ERROR_CLASS(\n            f\"{summary} {detail}\".rstrip(),\n            status=response.status_code,\n            code=str(code) if code else None,\n            request_id=request_id,\n            body=body,\n        )\n\n    @staticmethod\n    def _json(response: httpx.Response) -&gt; Any:\n        \"\"\"Decode a response body.\n\n        Args:\n            response: A successful response.\n\n        Returns:\n            The parsed JSON, or ``None`` when the response has no body.\n        \"\"\"\n        return response.json() if response.content else None\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.auth","title":"<code>auth: AuthContext</code>  <code>property</code>","text":"<p>The authentication context behind this client.</p>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.base_url","title":"<code>base_url: str</code>  <code>property</code>","text":"<p>The base URL that relative request paths are joined to.</p>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.client_id","title":"<code>client_id: str</code>  <code>property</code>","text":"<p>The client id this client requests tokens with.</p>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.scopes","title":"<code>scopes: tuple[str, ...]</code>  <code>property</code>","text":"<p>The scopes this client requests.</p>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.__aenter__","title":"<code>__aenter__() -&gt; Self</code>  <code>async</code>","text":"<p>Enter the context manager.</p> <p>Returns:</p> Type Description <code>Self</code> <p>This client.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>async def __aenter__(self) -&gt; Self:\n    \"\"\"Enter the context manager.\n\n    Returns:\n        This client.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.__aexit__","title":"<code>__aexit__(exc_type: type[BaseException] | None = None, exc_value: BaseException | None = None, traceback: TracebackType | None = None) -&gt; None</code>  <code>async</code>","text":"<p>Close the client when leaving the context manager.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>async def __aexit__(\n    self,\n    exc_type: type[BaseException] | None = None,\n    exc_value: BaseException | None = None,\n    traceback: TracebackType | None = None,\n) -&gt; None:\n    \"\"\"Close the client when leaving the context manager.\"\"\"\n    await self.aclose()\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.__init__","title":"<code>__init__(auth: AuthContext, *, base_url: str, client_id: str | None = None, scopes: Sequence[str] | None = None, timeout: float = 100.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.AsyncBaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>base_url</code> <code>str</code> <p>Base URL that relative request paths are joined to.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client. Defaults to the context's client id, then (user flows only) to <code>DEFAULT_CLIENT_ID</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Delegated scopes to request in a user flow; short names are qualified with the resource. Defaults to <code>&lt;RESOURCE&gt;/.default</code>. App flows always use <code>.default</code>.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds.</p> <code>100.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request (429, 503) is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>AsyncBaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an app flow has no client id, or is given delegated scopes.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    base_url: str,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    timeout: float = 100.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.AsyncBaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        base_url: Base URL that relative request paths are joined to.\n        client_id: Client id for this client. Defaults to the context's client id, then\n            (user flows only) to ``DEFAULT_CLIENT_ID``.\n        scopes: Delegated scopes to request in a user flow; short names are qualified\n            with the resource. Defaults to ``&lt;RESOURCE&gt;/.default``. App flows always use\n            ``.default``.\n        timeout: Timeout for each HTTP request, in seconds.\n        max_retries: How often a throttled request (429, 503) is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n\n    Raises:\n        ValueError: If an app flow has no client id, or is given delegated scopes.\n    \"\"\"\n    self._auth = auth\n    self._tokens = auth.aio\n    self._client_id = self._resolve_client_id(client_id)\n    self._scopes = self._resolve_scopes(scopes)\n    self._base_url = httpx.URL(base_url.rstrip(\"/\") + \"/\")\n    self._max_retries = max_retries\n    self._max_retry_wait = max_retry_wait\n    self._http = httpx.AsyncClient(timeout=timeout, transport=transport)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.aclose","title":"<code>aclose() -&gt; None</code>  <code>async</code>","text":"<p>Close the underlying HTTP connections.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying HTTP connections.\"\"\"\n    await self._http.aclose()\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.login","title":"<code>login(*, force: bool = False) -&gt; None</code>  <code>async</code>","text":"<p>Sign in now instead of at the first request.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>Prompt even when a cached token or account exists.</p> <code>False</code> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>async def login(self, *, force: bool = False) -&gt; None:\n    \"\"\"Sign in now instead of at the first request.\n\n    Args:\n        force: Prompt even when a cached token or account exists.\n    \"\"\"\n    await self._tokens.login(client_id=self._client_id, scopes=self._scopes, force=force)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.ResourceClient.request","title":"<code>request(method: str, url: str, *, params: Mapping[str, Any] | None = None, json: Any = None, headers: Mapping[str, str] | None = None) -&gt; httpx.Response</code>  <code>async</code>","text":"<p>Send an authenticated request and return the successful response.</p> <p>Throttled requests (429, 503) are retried, honouring <code>Retry-After</code>. A 401 is retried once: with the claims from the challenge when the resource sent one, else with a freshly acquired token.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>HTTP method.</p> required <code>url</code> <code>str</code> <p>Path relative to the base URL, or an absolute URL on the same host.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>json</code> <code>Any</code> <p>Request body, serialised as JSON.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Response</code> <p>The response, whose status is below 400.</p> <p>Raises:</p> Type Description <code>ResourceError</code> <p>(the subclass in <code>ERROR_CLASS</code>) for an error response.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>async def request(\n    self,\n    method: str,\n    url: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    json: Any = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; httpx.Response:\n    \"\"\"Send an authenticated request and return the successful response.\n\n    Throttled requests (429, 503) are retried, honouring ``Retry-After``. A 401 is\n    retried once: with the claims from the challenge when the resource sent one, else\n    with a freshly acquired token.\n\n    Args:\n        method: HTTP method.\n        url: Path relative to the base URL, or an absolute URL on the same host.\n        params: Query parameters.\n        json: Request body, serialised as JSON.\n        headers: Extra request headers.\n\n    Returns:\n        The response, whose status is below 400.\n\n    Raises:\n        ResourceError: (the subclass in ``ERROR_CLASS``) for an error response.\n    \"\"\"\n    target = self._url(url)\n    retries = 0\n    challenged = False\n    claims: str | None = None\n    force_refresh = False\n    while True:\n        token = await self._tokens.acquire_token(\n            self._scopes, client_id=self._client_id, claims=claims, force_refresh=force_refresh\n        )\n        claims, force_refresh = None, False\n        response = await self._http.request(\n            method,\n            target,\n            params=params,\n            json=json,\n            headers={**(headers or {}), \"Authorization\": f\"Bearer {token.token}\"},\n        )\n        if response.status_code == httpx.codes.UNAUTHORIZED and not challenged:\n            challenged = True\n            claims = claims_from_challenge(response.headers.get(\"WWW-Authenticate\"))\n            force_refresh = claims is None\n            continue\n        if response.status_code in _RETRY_STATUSES and retries &lt; self._max_retries:\n            retries += 1\n            await asyncio.sleep(retry_delay(response, retries, self._max_retry_wait))\n            continue\n        if response.status_code &gt;= httpx.codes.BAD_REQUEST:\n            raise self._error(response)\n        return response\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.claims_from_challenge","title":"<code>claims_from_challenge(header: str | None) -&gt; str | None</code>","text":"<p>Extract the claims challenge from a <code>WWW-Authenticate</code> header.</p> <p>Resources that support continuous access evaluation answer 401 with a base64-encoded claims value. MSAL wants it decoded.</p> <p>Parameters:</p> Name Type Description Default <code>header</code> <code>str | None</code> <p>The <code>WWW-Authenticate</code> header value, if any.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>The claims as a JSON string, or <code>None</code> when the header carries no usable claims.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>def claims_from_challenge(header: str | None) -&gt; str | None:\n    \"\"\"Extract the claims challenge from a ``WWW-Authenticate`` header.\n\n    Resources that support continuous access evaluation answer 401 with a base64-encoded\n    claims value. MSAL wants it decoded.\n\n    Args:\n        header: The ``WWW-Authenticate`` header value, if any.\n\n    Returns:\n        The claims as a JSON string, or ``None`` when the header carries no usable claims.\n    \"\"\"\n    match = _CLAIMS_PATTERN.search(header or \"\")\n    if match is None:\n        return None\n    encoded = match.group(1)\n    try:\n        decoded = base64.urlsafe_b64decode(encoded + \"=\" * (-len(encoded) % 4))\n        return decoded.decode(\"utf-8\") or None\n    except binascii.Error, UnicodeDecodeError:\n        return None\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.base.retry_delay","title":"<code>retry_delay(response: httpx.Response, attempt: int, max_wait: float) -&gt; float</code>","text":"<p>Decide how long to wait before retrying a throttled request.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>The throttled response; its <code>Retry-After</code> header wins when present.</p> required <code>attempt</code> <code>int</code> <p>Number of the retry about to be made, starting at 1.</p> required <code>max_wait</code> <code>float</code> <p>Upper bound for the delay, in seconds.</p> required <p>Returns:</p> Type Description <code>float</code> <p>The delay in seconds.</p> Source code in <code>src/azure_auth/clients/base.py</code> <pre><code>def retry_delay(response: httpx.Response, attempt: int, max_wait: float) -&gt; float:\n    \"\"\"Decide how long to wait before retrying a throttled request.\n\n    Args:\n        response: The throttled response; its ``Retry-After`` header wins when present.\n        attempt: Number of the retry about to be made, starting at 1.\n        max_wait: Upper bound for the delay, in seconds.\n\n    Returns:\n        The delay in seconds.\n    \"\"\"\n    header = response.headers.get(\"Retry-After\", \"\").strip()\n    delay = min(2.0**attempt, _MAX_BACKOFF_SECONDS)\n    if header:\n        try:\n            delay = float(header)\n        except ValueError:\n            try:\n                when = email.utils.parsedate_to_datetime(header)\n            except ValueError:\n                pass\n            else:\n                if when.tzinfo is None:  # \"-0000\" parses as a naive UTC time\n                    when = when.replace(tzinfo=datetime.UTC)\n                delay = (when - datetime.datetime.now(datetime.UTC)).total_seconds()\n    return max(0.0, min(delay, max_wait))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient","title":"<code>GraphClient</code>","text":"<p>               Bases: <code>ResourceClient</code></p> <p>Authenticated requests to Microsoft Graph.</p> <p>User flows default to the Microsoft Graph PowerShell client id. That application works by dynamic consent, so pass the delegated <code>scopes</code> you need; without them the token only carries what was consented to in the tenant before. App flows always use <code>https://graph.microsoft.com/.default</code>.</p> Example <p>graph = GraphClient(auth, scopes=[\"User.Read.All\"])  # doctest: +SKIP users = await graph.get_all(\"/users\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>class GraphClient(ResourceClient):\n    \"\"\"Authenticated requests to Microsoft Graph.\n\n    User flows default to the Microsoft Graph PowerShell client id. That application works\n    by dynamic consent, so pass the delegated ``scopes`` you need; without them the token\n    only carries what was consented to in the tenant before. App flows always use\n    ``https://graph.microsoft.com/.default``.\n\n    Example:\n        &gt;&gt;&gt; graph = GraphClient(auth, scopes=[\"User.Read.All\"])  # doctest: +SKIP\n        &gt;&gt;&gt; users = await graph.get_all(\"/users\")  # doctest: +SKIP\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str] = GRAPH_CLI_CLIENT_ID\n    RESOURCE: ClassVar[str] = GRAPH_RESOURCE\n    ERROR_CLASS: ClassVar[type[ResourceError]] = GraphError\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        api_version: Literal[\"v1.0\", \"beta\"] = \"v1.0\",\n        timeout: float = 100.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.AsyncBaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            client_id: Client id for this client; see :class:`ResourceClient`.\n            scopes: Delegated scopes for a user flow, such as ``[\"User.Read.All\"]``.\n            api_version: ``v1.0`` (default) or ``beta``.\n            timeout: Timeout for each HTTP request, in seconds.\n            max_retries: How often a throttled request is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n        \"\"\"\n        super().__init__(\n            auth,\n            base_url=f\"{GRAPH_RESOURCE}/{api_version}\",\n            client_id=client_id,\n            scopes=scopes,\n            timeout=timeout,\n            max_retries=max_retries,\n            max_retry_wait=max_retry_wait,\n            transport=transport,\n        )\n\n    async def get(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a GET request.\n\n        Args:\n            path: Path such as ``/users``, or an absolute Graph URL.\n            params: Query parameters such as ``{\"$top\": 5}``.\n            headers: Extra request headers such as ``ConsistencyLevel``.\n\n        Returns:\n            The parsed JSON body.\n        \"\"\"\n        return self._json(await self.request(\"GET\", path, params=params, headers=headers))\n\n    async def post(\n        self,\n        path: str,\n        json: Any = None,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a POST request.\n\n        Args:\n            path: Path such as ``/users``.\n            json: Request body.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        response = await self.request(\"POST\", path, params=params, json=json, headers=headers)\n        return self._json(response)\n\n    async def patch(\n        self,\n        path: str,\n        json: Any = None,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a PATCH request.\n\n        Args:\n            path: Path such as ``/users/{id}``.\n            json: Request body.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        response = await self.request(\"PATCH\", path, params=params, json=json, headers=headers)\n        return self._json(response)\n\n    async def put(\n        self,\n        path: str,\n        json: Any = None,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a PUT request.\n\n        Args:\n            path: Path of the resource.\n            json: Request body.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        response = await self.request(\"PUT\", path, params=params, json=json, headers=headers)\n        return self._json(response)\n\n    async def delete(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a DELETE request.\n\n        Args:\n            path: Path of the resource.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(await self.request(\"DELETE\", path, params=params, headers=headers))\n\n    async def iter_pages(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; AsyncIterator[dict[str, Any]]:\n        \"\"\"Yield every page of a collection, following ``@odata.nextLink``.\n\n        Args:\n            path: Path of the collection.\n            params: Query parameters for the first page; next links carry their own.\n            headers: Extra request headers, sent with every page.\n\n        Yields:\n            Each page as returned by Graph.\n        \"\"\"\n        url: str | None = path\n        while url:\n            page = await self.get(url, params=params, headers=headers)\n            yield page\n            url, params = page.get(\"@odata.nextLink\"), None\n\n    async def get_all(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; list[Any]:\n        \"\"\"Return every item of a collection, following ``@odata.nextLink``.\n\n        Args:\n            path: Path of the collection.\n            params: Query parameters for the first page.\n            headers: Extra request headers, sent with every page.\n\n        Returns:\n            The items of all pages, in order.\n        \"\"\"\n        items: list[Any] = []\n        async for page in self.iter_pages(path, params=params, headers=headers):\n            items.extend(page.get(\"value\", []))\n        return items\n\n    async def batch(self, requests: Sequence[Mapping[str, Any]]) -&gt; list[dict[str, Any]]:\n        \"\"\"Send requests through ``$batch``, twenty per call.\n\n        Args:\n            requests: Batch request objects with ``method`` and ``url`` and optionally\n                ``id``, ``body`` and ``headers``. Missing ids are filled in. A request with\n                a body and no content type is sent as JSON.\n\n        Returns:\n            One response object per request, in the order of ``requests``. Failed requests\n            are returned with their error status, not raised.\n        \"\"\"\n        prepared: list[dict[str, Any]] = []\n        for index, request in enumerate(requests):\n            item = dict(request)\n            item[\"id\"] = str(item.get(\"id\", index))\n            if \"body\" in item and \"headers\" not in item:\n                item[\"headers\"] = {\"Content-Type\": \"application/json\"}\n            prepared.append(item)\n\n        responses: dict[str, dict[str, Any]] = {}\n        for start in range(0, len(prepared), BATCH_LIMIT):\n            chunk = prepared[start : start + BATCH_LIMIT]\n            result = await self.post(\"/$batch\", {\"requests\": chunk})\n            responses.update({str(item[\"id\"]): item for item in result.get(\"responses\", [])})\n        return [responses[item[\"id\"]] for item in prepared]\n\n    async def list_managed_tenant_ids(self) -&gt; list[str]:\n        \"\"\"List the tenants this home tenant manages through GDAP.\n\n        Call this on a client for the home tenant. It needs the delegated or application\n        permission ``DelegatedAdminRelationship.Read.All``.\n\n        Returns:\n            The managed tenant ids.\n        \"\"\"\n        managed = await self.get_all(\"/tenantRelationships/delegatedAdminCustomers\")\n        return [str(tenant[\"tenantId\"]) for tenant in managed]\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.__init__","title":"<code>__init__(auth: AuthContext, *, client_id: str | None = None, scopes: Sequence[str] | None = None, api_version: Literal['v1.0', 'beta'] = 'v1.0', timeout: float = 100.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.AsyncBaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client; see :class:<code>ResourceClient</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Delegated scopes for a user flow, such as <code>[\"User.Read.All\"]</code>.</p> <code>None</code> <code>api_version</code> <code>Literal['v1.0', 'beta']</code> <p><code>v1.0</code> (default) or <code>beta</code>.</p> <code>'v1.0'</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds.</p> <code>100.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>AsyncBaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    api_version: Literal[\"v1.0\", \"beta\"] = \"v1.0\",\n    timeout: float = 100.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.AsyncBaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        client_id: Client id for this client; see :class:`ResourceClient`.\n        scopes: Delegated scopes for a user flow, such as ``[\"User.Read.All\"]``.\n        api_version: ``v1.0`` (default) or ``beta``.\n        timeout: Timeout for each HTTP request, in seconds.\n        max_retries: How often a throttled request is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n    \"\"\"\n    super().__init__(\n        auth,\n        base_url=f\"{GRAPH_RESOURCE}/{api_version}\",\n        client_id=client_id,\n        scopes=scopes,\n        timeout=timeout,\n        max_retries=max_retries,\n        max_retry_wait=max_retry_wait,\n        transport=transport,\n    )\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.batch","title":"<code>batch(requests: Sequence[Mapping[str, Any]]) -&gt; list[dict[str, Any]]</code>  <code>async</code>","text":"<p>Send requests through <code>$batch</code>, twenty per call.</p> <p>Parameters:</p> Name Type Description Default <code>requests</code> <code>Sequence[Mapping[str, Any]]</code> <p>Batch request objects with <code>method</code> and <code>url</code> and optionally <code>id</code>, <code>body</code> and <code>headers</code>. Missing ids are filled in. A request with a body and no content type is sent as JSON.</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>One response object per request, in the order of <code>requests</code>. Failed requests</p> <code>list[dict[str, Any]]</code> <p>are returned with their error status, not raised.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def batch(self, requests: Sequence[Mapping[str, Any]]) -&gt; list[dict[str, Any]]:\n    \"\"\"Send requests through ``$batch``, twenty per call.\n\n    Args:\n        requests: Batch request objects with ``method`` and ``url`` and optionally\n            ``id``, ``body`` and ``headers``. Missing ids are filled in. A request with\n            a body and no content type is sent as JSON.\n\n    Returns:\n        One response object per request, in the order of ``requests``. Failed requests\n        are returned with their error status, not raised.\n    \"\"\"\n    prepared: list[dict[str, Any]] = []\n    for index, request in enumerate(requests):\n        item = dict(request)\n        item[\"id\"] = str(item.get(\"id\", index))\n        if \"body\" in item and \"headers\" not in item:\n            item[\"headers\"] = {\"Content-Type\": \"application/json\"}\n        prepared.append(item)\n\n    responses: dict[str, dict[str, Any]] = {}\n    for start in range(0, len(prepared), BATCH_LIMIT):\n        chunk = prepared[start : start + BATCH_LIMIT]\n        result = await self.post(\"/$batch\", {\"requests\": chunk})\n        responses.update({str(item[\"id\"]): item for item in result.get(\"responses\", [])})\n    return [responses[item[\"id\"]] for item in prepared]\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.delete","title":"<code>delete(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>  <code>async</code>","text":"<p>Send a DELETE request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the resource.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def delete(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a DELETE request.\n\n    Args:\n        path: Path of the resource.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(await self.request(\"DELETE\", path, params=params, headers=headers))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.get","title":"<code>get(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>  <code>async</code>","text":"<p>Send a GET request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path such as <code>/users</code>, or an absolute Graph URL.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters such as <code>{\"$top\": 5}</code>.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers such as <code>ConsistencyLevel</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def get(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a GET request.\n\n    Args:\n        path: Path such as ``/users``, or an absolute Graph URL.\n        params: Query parameters such as ``{\"$top\": 5}``.\n        headers: Extra request headers such as ``ConsistencyLevel``.\n\n    Returns:\n        The parsed JSON body.\n    \"\"\"\n    return self._json(await self.request(\"GET\", path, params=params, headers=headers))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.get_all","title":"<code>get_all(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; list[Any]</code>  <code>async</code>","text":"<p>Return every item of a collection, following <code>@odata.nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the collection.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters for the first page.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers, sent with every page.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Any]</code> <p>The items of all pages, in order.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def get_all(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; list[Any]:\n    \"\"\"Return every item of a collection, following ``@odata.nextLink``.\n\n    Args:\n        path: Path of the collection.\n        params: Query parameters for the first page.\n        headers: Extra request headers, sent with every page.\n\n    Returns:\n        The items of all pages, in order.\n    \"\"\"\n    items: list[Any] = []\n    async for page in self.iter_pages(path, params=params, headers=headers):\n        items.extend(page.get(\"value\", []))\n    return items\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.iter_pages","title":"<code>iter_pages(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; AsyncIterator[dict[str, Any]]</code>  <code>async</code>","text":"<p>Yield every page of a collection, following <code>@odata.nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the collection.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters for the first page; next links carry their own.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers, sent with every page.</p> <code>None</code> <p>Yields:</p> Type Description <code>AsyncIterator[dict[str, Any]]</code> <p>Each page as returned by Graph.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def iter_pages(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; AsyncIterator[dict[str, Any]]:\n    \"\"\"Yield every page of a collection, following ``@odata.nextLink``.\n\n    Args:\n        path: Path of the collection.\n        params: Query parameters for the first page; next links carry their own.\n        headers: Extra request headers, sent with every page.\n\n    Yields:\n        Each page as returned by Graph.\n    \"\"\"\n    url: str | None = path\n    while url:\n        page = await self.get(url, params=params, headers=headers)\n        yield page\n        url, params = page.get(\"@odata.nextLink\"), None\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.list_managed_tenant_ids","title":"<code>list_managed_tenant_ids() -&gt; list[str]</code>  <code>async</code>","text":"<p>List the tenants this home tenant manages through GDAP.</p> <p>Call this on a client for the home tenant. It needs the delegated or application permission <code>DelegatedAdminRelationship.Read.All</code>.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>The managed tenant ids.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def list_managed_tenant_ids(self) -&gt; list[str]:\n    \"\"\"List the tenants this home tenant manages through GDAP.\n\n    Call this on a client for the home tenant. It needs the delegated or application\n    permission ``DelegatedAdminRelationship.Read.All``.\n\n    Returns:\n        The managed tenant ids.\n    \"\"\"\n    managed = await self.get_all(\"/tenantRelationships/delegatedAdminCustomers\")\n    return [str(tenant[\"tenantId\"]) for tenant in managed]\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.patch","title":"<code>patch(path: str, json: Any = None, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>  <code>async</code>","text":"<p>Send a PATCH request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path such as <code>/users/{id}</code>.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def patch(\n    self,\n    path: str,\n    json: Any = None,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a PATCH request.\n\n    Args:\n        path: Path such as ``/users/{id}``.\n        json: Request body.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    response = await self.request(\"PATCH\", path, params=params, json=json, headers=headers)\n    return self._json(response)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.post","title":"<code>post(path: str, json: Any = None, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>  <code>async</code>","text":"<p>Send a POST request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path such as <code>/users</code>.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def post(\n    self,\n    path: str,\n    json: Any = None,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a POST request.\n\n    Args:\n        path: Path such as ``/users``.\n        json: Request body.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    response = await self.request(\"POST\", path, params=params, json=json, headers=headers)\n    return self._json(response)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.graph.GraphClient.put","title":"<code>put(path: str, json: Any = None, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>  <code>async</code>","text":"<p>Send a PUT request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the resource.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/graph.py</code> <pre><code>async def put(\n    self,\n    path: str,\n    json: Any = None,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a PUT request.\n\n    Args:\n        path: Path of the resource.\n        json: Request body.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    response = await self.request(\"PUT\", path, params=params, json=json, headers=headers)\n    return self._json(response)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient","title":"<code>AzureClient</code>","text":"<p>               Bases: <code>ResourceClient</code></p> <p>Authenticated requests to Azure Resource Manager.</p> <p>Every call needs the <code>api_version</code> of the resource provider being addressed.</p> Example <p>arm = AzureClient(auth)  # doctest: +SKIP subs = await arm.get_all(\"/subscriptions\", api_version=\"2022-12-01\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>class AzureClient(ResourceClient):\n    \"\"\"Authenticated requests to Azure Resource Manager.\n\n    Every call needs the ``api_version`` of the resource provider being addressed.\n\n    Example:\n        &gt;&gt;&gt; arm = AzureClient(auth)  # doctest: +SKIP\n        &gt;&gt;&gt; subs = await arm.get_all(\"/subscriptions\", api_version=\"2022-12-01\")  # doctest: +SKIP\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str] = AZURE_POWERSHELL_CLIENT_ID\n    RESOURCE: ClassVar[str] = ARM_RESOURCE\n    ERROR_CLASS: ClassVar[type[ResourceError]] = AzureError\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        timeout: float = 100.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.AsyncBaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            client_id: Client id for this client; see :class:`ResourceClient`.\n            scopes: Scopes to request. Defaults to ``https://management.azure.com/.default``.\n            timeout: Timeout for each HTTP request, in seconds.\n            max_retries: How often a throttled request is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n        \"\"\"\n        super().__init__(\n            auth,\n            base_url=ARM_RESOURCE,\n            client_id=client_id,\n            scopes=scopes,\n            timeout=timeout,\n            max_retries=max_retries,\n            max_retry_wait=max_retry_wait,\n            transport=transport,\n        )\n\n    async def send(\n        self,\n        method: str,\n        path: str,\n        *,\n        api_version: str,\n        params: Mapping[str, Any] | None = None,\n        json: Any = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; httpx.Response:\n        \"\"\"Send a request with its ``api-version`` and return the raw response.\n\n        Use this when the response headers matter, for example to pass the response of a\n        long-running operation to :meth:`wait`.\n\n        Args:\n            method: HTTP method.\n            path: Resource path such as ``/subscriptions/{id}/resourceGroups/{name}``.\n            api_version: API version of the resource provider.\n            params: Further query parameters.\n            json: Request body.\n            headers: Extra request headers.\n\n        Returns:\n            The successful response.\n        \"\"\"\n        query = {**(params or {}), \"api-version\": api_version}\n        return await self.request(method, path, params=query, json=json, headers=headers)\n\n    async def get(\n        self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n    ) -&gt; Any:\n        \"\"\"Send a GET request.\n\n        Args:\n            path: Resource path.\n            api_version: API version of the resource provider.\n            params: Further query parameters.\n\n        Returns:\n            The parsed JSON body.\n        \"\"\"\n        return self._json(await self.send(\"GET\", path, api_version=api_version, params=params))\n\n    async def put(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n        \"\"\"Send a PUT request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n        Args:\n            path: Resource path.\n            json: Request body.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(await self.send(\"PUT\", path, api_version=api_version, json=json))\n\n    async def post(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n        \"\"\"Send a POST request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n        Args:\n            path: Resource or action path.\n            json: Request body.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(await self.send(\"POST\", path, api_version=api_version, json=json))\n\n    async def patch(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n        \"\"\"Send a PATCH request.\n\n        Args:\n            path: Resource path.\n            json: Request body.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(await self.send(\"PATCH\", path, api_version=api_version, json=json))\n\n    async def delete(self, path: str, *, api_version: str) -&gt; Any:\n        \"\"\"Send a DELETE request. For long-running deletes use :meth:`send` and :meth:`wait`.\n\n        Args:\n            path: Resource path.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(await self.send(\"DELETE\", path, api_version=api_version))\n\n    async def iter_pages(\n        self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n    ) -&gt; AsyncIterator[dict[str, Any]]:\n        \"\"\"Yield every page of a list operation, following ``nextLink``.\n\n        Args:\n            path: Path of the list operation.\n            api_version: API version of the resource provider.\n            params: Further query parameters for the first page.\n\n        Yields:\n            Each page as returned by Azure Resource Manager.\n        \"\"\"\n        page = await self.get(path, api_version=api_version, params=params)\n        yield page\n        while page.get(\"nextLink\"):\n            # A next link already carries api-version and every other parameter.\n            page = self._json(await self.request(\"GET\", page[\"nextLink\"]))\n            yield page\n\n    async def get_all(\n        self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n    ) -&gt; list[Any]:\n        \"\"\"Return every item of a list operation, following ``nextLink``.\n\n        Args:\n            path: Path of the list operation.\n            api_version: API version of the resource provider.\n            params: Further query parameters for the first page.\n\n        Returns:\n            The items of all pages, in order.\n        \"\"\"\n        items: list[Any] = []\n        async for page in self.iter_pages(path, api_version=api_version, params=params):\n            items.extend(page.get(\"value\", []))\n        return items\n\n    async def wait(\n        self,\n        response: httpx.Response,\n        *,\n        timeout: float = 1800.0,\n        poll_interval: float | None = None,\n    ) -&gt; Any:\n        \"\"\"Wait for the long-running operation started by ``response`` to finish.\n\n        Polls the ``Azure-AsyncOperation`` URL when the response has one, else the\n        ``Location`` URL, waiting ``Retry-After`` seconds between polls.\n\n        Args:\n            response: The response of :meth:`send` that started the operation.\n            timeout: Give up after this many seconds.\n            poll_interval: Seconds between polls; overrides ``Retry-After``.\n\n        Returns:\n            The final resource for PUT and PATCH, the ``Location`` result when there is one,\n            else the final operation status. When the operation had already completed, the\n            body of ``response``.\n\n        Raises:\n            AzureError: If the operation fails or is cancelled.\n            TimeoutError: If the operation does not finish in time.\n        \"\"\"\n        deadline = time.monotonic() + timeout\n        operation_url = response.headers.get(\"Azure-AsyncOperation\")\n        location_url = response.headers.get(\"Location\")\n\n        def delay(latest: httpx.Response) -&gt; float:\n            if poll_interval is not None:\n                return poll_interval\n            if \"Retry-After\" in latest.headers:\n                return retry_delay(latest, 1, self._max_retry_wait)\n            return _DEFAULT_POLL_SECONDS\n\n        if operation_url:\n            latest = response\n            while True:\n                if time.monotonic() &gt; deadline:\n                    raise TimeoutError(f\"Operation still running after {timeout} s\")\n                await asyncio.sleep(delay(latest))\n                latest = await self.request(\"GET\", operation_url)\n                status = self._json(latest) or {}\n                state = str(status.get(\"status\", \"\")).lower()\n                if state in _TERMINAL_FAILURES:\n                    raise self._operation_error(latest, status)\n                if state == \"succeeded\":\n                    break\n            if response.request.method in {\"PUT\", \"PATCH\"}:\n                return self._json(await self.request(\"GET\", str(response.request.url)))\n            if location_url:\n                return self._json(await self.request(\"GET\", location_url))\n            return status\n\n        if response.status_code == httpx.codes.ACCEPTED and location_url:\n            latest = response\n            while latest.status_code == httpx.codes.ACCEPTED:\n                if time.monotonic() &gt; deadline:\n                    raise TimeoutError(f\"Operation still running after {timeout} s\")\n                await asyncio.sleep(delay(latest))\n                latest = await self.request(\"GET\", location_url)\n            return self._json(latest)\n\n        return self._json(response)\n\n    def _operation_error(self, response: httpx.Response, status: Mapping[str, Any]) -&gt; AzureError:\n        \"\"\"Build the exception for a failed long-running operation.\n\n        Args:\n            response: The poll response that reported the failure.\n            status: The operation status body.\n\n        Returns:\n            The exception to raise.\n        \"\"\"\n        error = status.get(\"error\") or {}\n        code = error.get(\"code\") if isinstance(error, dict) else None\n        message = error.get(\"message\") if isinstance(error, dict) else None\n        detail = \": \".join(str(part) for part in (code, message) if part)\n        return AzureError(\n            f\"Operation {status.get('status')} {detail}\".rstrip(),\n            status=response.status_code,\n            code=str(code) if code else None,\n            request_id=response.headers.get(\"x-ms-request-id\"),\n            body=dict(status),\n        )\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.__init__","title":"<code>__init__(auth: AuthContext, *, client_id: str | None = None, scopes: Sequence[str] | None = None, timeout: float = 100.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.AsyncBaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client; see :class:<code>ResourceClient</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Scopes to request. Defaults to <code>https://management.azure.com/.default</code>.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds.</p> <code>100.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>AsyncBaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    timeout: float = 100.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.AsyncBaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        client_id: Client id for this client; see :class:`ResourceClient`.\n        scopes: Scopes to request. Defaults to ``https://management.azure.com/.default``.\n        timeout: Timeout for each HTTP request, in seconds.\n        max_retries: How often a throttled request is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n    \"\"\"\n    super().__init__(\n        auth,\n        base_url=ARM_RESOURCE,\n        client_id=client_id,\n        scopes=scopes,\n        timeout=timeout,\n        max_retries=max_retries,\n        max_retry_wait=max_retry_wait,\n        transport=transport,\n    )\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.delete","title":"<code>delete(path: str, *, api_version: str) -&gt; Any</code>  <code>async</code>","text":"<p>Send a DELETE request. For long-running deletes use :meth:<code>send</code> and :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def delete(self, path: str, *, api_version: str) -&gt; Any:\n    \"\"\"Send a DELETE request. For long-running deletes use :meth:`send` and :meth:`wait`.\n\n    Args:\n        path: Resource path.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(await self.send(\"DELETE\", path, api_version=api_version))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.get","title":"<code>get(path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; Any</code>  <code>async</code>","text":"<p>Send a GET request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def get(\n    self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n) -&gt; Any:\n    \"\"\"Send a GET request.\n\n    Args:\n        path: Resource path.\n        api_version: API version of the resource provider.\n        params: Further query parameters.\n\n    Returns:\n        The parsed JSON body.\n    \"\"\"\n    return self._json(await self.send(\"GET\", path, api_version=api_version, params=params))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.get_all","title":"<code>get_all(path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; list[Any]</code>  <code>async</code>","text":"<p>Return every item of a list operation, following <code>nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the list operation.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters for the first page.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Any]</code> <p>The items of all pages, in order.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def get_all(\n    self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n) -&gt; list[Any]:\n    \"\"\"Return every item of a list operation, following ``nextLink``.\n\n    Args:\n        path: Path of the list operation.\n        api_version: API version of the resource provider.\n        params: Further query parameters for the first page.\n\n    Returns:\n        The items of all pages, in order.\n    \"\"\"\n    items: list[Any] = []\n    async for page in self.iter_pages(path, api_version=api_version, params=params):\n        items.extend(page.get(\"value\", []))\n    return items\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.iter_pages","title":"<code>iter_pages(path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; AsyncIterator[dict[str, Any]]</code>  <code>async</code>","text":"<p>Yield every page of a list operation, following <code>nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the list operation.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters for the first page.</p> <code>None</code> <p>Yields:</p> Type Description <code>AsyncIterator[dict[str, Any]]</code> <p>Each page as returned by Azure Resource Manager.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def iter_pages(\n    self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n) -&gt; AsyncIterator[dict[str, Any]]:\n    \"\"\"Yield every page of a list operation, following ``nextLink``.\n\n    Args:\n        path: Path of the list operation.\n        api_version: API version of the resource provider.\n        params: Further query parameters for the first page.\n\n    Yields:\n        Each page as returned by Azure Resource Manager.\n    \"\"\"\n    page = await self.get(path, api_version=api_version, params=params)\n    yield page\n    while page.get(\"nextLink\"):\n        # A next link already carries api-version and every other parameter.\n        page = self._json(await self.request(\"GET\", page[\"nextLink\"]))\n        yield page\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.patch","title":"<code>patch(path: str, json: Any = None, *, api_version: str) -&gt; Any</code>  <code>async</code>","text":"<p>Send a PATCH request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def patch(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n    \"\"\"Send a PATCH request.\n\n    Args:\n        path: Resource path.\n        json: Request body.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(await self.send(\"PATCH\", path, api_version=api_version, json=json))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.post","title":"<code>post(path: str, json: Any = None, *, api_version: str) -&gt; Any</code>  <code>async</code>","text":"<p>Send a POST request. For long-running operations use :meth:<code>send</code> and :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource or action path.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def post(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n    \"\"\"Send a POST request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n    Args:\n        path: Resource or action path.\n        json: Request body.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(await self.send(\"POST\", path, api_version=api_version, json=json))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.put","title":"<code>put(path: str, json: Any = None, *, api_version: str) -&gt; Any</code>  <code>async</code>","text":"<p>Send a PUT request. For long-running operations use :meth:<code>send</code> and :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def put(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n    \"\"\"Send a PUT request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n    Args:\n        path: Resource path.\n        json: Request body.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(await self.send(\"PUT\", path, api_version=api_version, json=json))\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.send","title":"<code>send(method: str, path: str, *, api_version: str, params: Mapping[str, Any] | None = None, json: Any = None, headers: Mapping[str, str] | None = None) -&gt; httpx.Response</code>  <code>async</code>","text":"<p>Send a request with its <code>api-version</code> and return the raw response.</p> <p>Use this when the response headers matter, for example to pass the response of a long-running operation to :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>HTTP method.</p> required <code>path</code> <code>str</code> <p>Resource path such as <code>/subscriptions/{id}/resourceGroups/{name}</code>.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters.</p> <code>None</code> <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Response</code> <p>The successful response.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def send(\n    self,\n    method: str,\n    path: str,\n    *,\n    api_version: str,\n    params: Mapping[str, Any] | None = None,\n    json: Any = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; httpx.Response:\n    \"\"\"Send a request with its ``api-version`` and return the raw response.\n\n    Use this when the response headers matter, for example to pass the response of a\n    long-running operation to :meth:`wait`.\n\n    Args:\n        method: HTTP method.\n        path: Resource path such as ``/subscriptions/{id}/resourceGroups/{name}``.\n        api_version: API version of the resource provider.\n        params: Further query parameters.\n        json: Request body.\n        headers: Extra request headers.\n\n    Returns:\n        The successful response.\n    \"\"\"\n    query = {**(params or {}), \"api-version\": api_version}\n    return await self.request(method, path, params=query, json=json, headers=headers)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.arm.AzureClient.wait","title":"<code>wait(response: httpx.Response, *, timeout: float = 1800.0, poll_interval: float | None = None) -&gt; Any</code>  <code>async</code>","text":"<p>Wait for the long-running operation started by <code>response</code> to finish.</p> <p>Polls the <code>Azure-AsyncOperation</code> URL when the response has one, else the <code>Location</code> URL, waiting <code>Retry-After</code> seconds between polls.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>The response of :meth:<code>send</code> that started the operation.</p> required <code>timeout</code> <code>float</code> <p>Give up after this many seconds.</p> <code>1800.0</code> <code>poll_interval</code> <code>float | None</code> <p>Seconds between polls; overrides <code>Retry-After</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The final resource for PUT and PATCH, the <code>Location</code> result when there is one,</p> <code>Any</code> <p>else the final operation status. When the operation had already completed, the</p> <code>Any</code> <p>body of <code>response</code>.</p> <p>Raises:</p> Type Description <code>AzureError</code> <p>If the operation fails or is cancelled.</p> <code>TimeoutError</code> <p>If the operation does not finish in time.</p> Source code in <code>src/azure_auth/clients/arm.py</code> <pre><code>async def wait(\n    self,\n    response: httpx.Response,\n    *,\n    timeout: float = 1800.0,\n    poll_interval: float | None = None,\n) -&gt; Any:\n    \"\"\"Wait for the long-running operation started by ``response`` to finish.\n\n    Polls the ``Azure-AsyncOperation`` URL when the response has one, else the\n    ``Location`` URL, waiting ``Retry-After`` seconds between polls.\n\n    Args:\n        response: The response of :meth:`send` that started the operation.\n        timeout: Give up after this many seconds.\n        poll_interval: Seconds between polls; overrides ``Retry-After``.\n\n    Returns:\n        The final resource for PUT and PATCH, the ``Location`` result when there is one,\n        else the final operation status. When the operation had already completed, the\n        body of ``response``.\n\n    Raises:\n        AzureError: If the operation fails or is cancelled.\n        TimeoutError: If the operation does not finish in time.\n    \"\"\"\n    deadline = time.monotonic() + timeout\n    operation_url = response.headers.get(\"Azure-AsyncOperation\")\n    location_url = response.headers.get(\"Location\")\n\n    def delay(latest: httpx.Response) -&gt; float:\n        if poll_interval is not None:\n            return poll_interval\n        if \"Retry-After\" in latest.headers:\n            return retry_delay(latest, 1, self._max_retry_wait)\n        return _DEFAULT_POLL_SECONDS\n\n    if operation_url:\n        latest = response\n        while True:\n            if time.monotonic() &gt; deadline:\n                raise TimeoutError(f\"Operation still running after {timeout} s\")\n            await asyncio.sleep(delay(latest))\n            latest = await self.request(\"GET\", operation_url)\n            status = self._json(latest) or {}\n            state = str(status.get(\"status\", \"\")).lower()\n            if state in _TERMINAL_FAILURES:\n                raise self._operation_error(latest, status)\n            if state == \"succeeded\":\n                break\n        if response.request.method in {\"PUT\", \"PATCH\"}:\n            return self._json(await self.request(\"GET\", str(response.request.url)))\n        if location_url:\n            return self._json(await self.request(\"GET\", location_url))\n        return status\n\n    if response.status_code == httpx.codes.ACCEPTED and location_url:\n        latest = response\n        while latest.status_code == httpx.codes.ACCEPTED:\n            if time.monotonic() &gt; deadline:\n                raise TimeoutError(f\"Operation still running after {timeout} s\")\n            await asyncio.sleep(delay(latest))\n            latest = await self.request(\"GET\", location_url)\n        return self._json(latest)\n\n    return self._json(response)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.invoke_command.InvokeCommandClient","title":"<code>InvokeCommandClient</code>","text":"<p>               Bases: <code>ResourceClient</code></p> <p>Base class for clients that run cmdlets through <code>InvokeCommand</code>.</p> <p>Attributes:</p> Name Type Description <code>HOST</code> <code>str</code> <p>Host name of the service.</p> <code>last_warnings</code> <code>list[str]</code> <p>Warnings the service returned for the most recent cmdlet.</p> Source code in <code>src/azure_auth/clients/invoke_command.py</code> <pre><code>class InvokeCommandClient(ResourceClient):\n    \"\"\"Base class for clients that run cmdlets through ``InvokeCommand``.\n\n    Attributes:\n        HOST: Host name of the service.\n        last_warnings: Warnings the service returned for the most recent cmdlet.\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str] = EXCHANGE_POWERSHELL_CLIENT_ID\n    ERROR_CLASS: ClassVar[type[ResourceError]] = InvokeCommandError\n    HOST: ClassVar[str]\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        anchor_mailbox: str | None = None,\n        api_version: Literal[\"beta\", \"v1.0\"] = \"beta\",\n        page_size: int = 1000,\n        timeout: float = 300.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.AsyncBaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            client_id: Client id for this client; see :class:`ResourceClient`.\n            scopes: Scopes to request. Defaults to ``https://&lt;host&gt;/.default``.\n            anchor_mailbox: Value of the ``X-AnchorMailbox`` routing header. Defaults to the\n                signed-in user for a user flow in its own tenant, and to the tenant's system\n                mailbox for app flows and for sibling (GDAP) contexts.\n            api_version: ``beta`` (what the PowerShell module uses) or ``v1.0``.\n            page_size: Preferred number of results per page.\n            timeout: Timeout for each HTTP request, in seconds. Cmdlets can be slow.\n            max_retries: How often a throttled request is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n        \"\"\"\n        super().__init__(\n            auth,\n            base_url=f\"https://{self.HOST}\",\n            client_id=client_id,\n            scopes=scopes,\n            timeout=timeout,\n            max_retries=max_retries,\n            max_retry_wait=max_retry_wait,\n            transport=transport,\n        )\n        self._anchor_mailbox = anchor_mailbox\n        self._api_version = api_version\n        self._page_size = page_size\n        self._connection_id = str(uuid.uuid4())\n        self._tenant_guid: str | None = None\n        self.last_warnings: list[str] = []\n\n    def _is_trusted_host(self, host: str) -&gt; bool:\n        \"\"\"Accept the service host and its regional sub-hosts.\n\n        Args:\n            host: Host name of the request URL.\n\n        Returns:\n            ``True`` for ``HOST`` and for ``&lt;region&gt;.HOST``.\n        \"\"\"\n        lowered = host.lower()\n        return lowered == self.HOST or lowered.endswith(f\".{self.HOST}\")\n\n    async def _tenant(self) -&gt; str:\n        \"\"\"Return the tenant GUID for the request path.\n\n        The service wants the GUID, and the context may have been given a domain name, so\n        the GUID is read from the ``tid`` claim of the access token.\n\n        Returns:\n            The tenant GUID, or the context's tenant id when the token cannot be decoded.\n        \"\"\"\n        if self._tenant_guid is None:\n            token = await self._tokens.acquire_token(self._scopes, client_id=self._client_id)\n            self._tenant_guid = tenant_id_from_token(token.token) or self._auth.tenant_id\n        return self._tenant_guid\n\n    def _anchor(self, tenant: str) -&gt; str:\n        \"\"\"Build the ``X-AnchorMailbox`` routing header.\n\n        Args:\n            tenant: Tenant GUID.\n\n        Returns:\n            The configured anchor, else the user's mailbox for a user flow in its own tenant,\n            else the tenant's system mailbox.\n        \"\"\"\n        if self._anchor_mailbox:\n            return self._anchor_mailbox\n        if self._auth.is_app_flow or self._auth.is_sibling:\n            return f\"APP:{SYSTEM_MAILBOX}@{tenant}\"\n        return f\"UPN:{self._auth.username}\"\n\n    async def _post(self, url: str, payload: dict[str, Any], cmdlet: str) -&gt; httpx.Response:\n        \"\"\"Send one ``InvokeCommand`` request, following a regional redirect by hand.\n\n        ``httpx`` drops the ``Authorization`` header when a redirect changes the host, and\n        Security &amp; Compliance answers the first call with a redirect to a regional host. So\n        the redirect is read here, the regional host becomes the new base URL, and the\n        request is sent again with its token.\n\n        Args:\n            url: Request URL or path.\n            payload: The request body.\n            cmdlet: Name of the cmdlet, for the diagnostic header.\n\n        Returns:\n            The successful response.\n        \"\"\"\n        tenant = await self._tenant()\n        headers = {\n            \"Accept\": \"application/json\",\n            \"Content-Type\": \"application/json;odata.metadata=minimal\",\n            \"Prefer\": f\"odata.maxpagesize={self._page_size}\",\n            \"X-AnchorMailbox\": self._anchor(tenant),\n            \"X-CmdletName\": cmdlet,\n            \"X-ResponseFormat\": \"json\",\n            \"connection-id\": self._connection_id,\n            \"client-request-id\": str(uuid.uuid4()),\n        }\n        response = await self.request(\"POST\", url, json=payload, headers=headers)\n        location = response.headers.get(\"Location\")\n        if response.status_code == httpx.codes.FOUND and location:\n            region = httpx.URL(location).host.split(\".\", 1)[0]\n            if region and not self._base_url.host.startswith(f\"{region}.\"):\n                self._base_url = httpx.URL(f\"https://{region}.{self.HOST}/\")\n                target = httpx.URL(url) if \"://\" in url else None\n                retry_url = str(target.copy_with(host=self._base_url.host)) if target else url\n                response = await self.request(\"POST\", retry_url, json=payload, headers=headers)\n        if response.is_redirect:\n            # A redirect that could not be followed must not look like an empty result.\n            raise self._error(response)\n        return response\n\n    async def iter_pages(self, cmdlet: str, **parameters: Any) -&gt; AsyncIterator[dict[str, Any]]:\n        \"\"\"Run a cmdlet and yield each page of its output.\n\n        Args:\n            cmdlet: Cmdlet name such as ``Get-Mailbox``.\n            **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n        Yields:\n            Each response page; the results are under ``value``.\n        \"\"\"\n        payload = {\"CmdletInput\": {\"CmdletName\": cmdlet, \"Parameters\": parameters}}\n        tenant = await self._tenant()\n        url: str | None = f\"/adminapi/{self._api_version}/{tenant}/InvokeCommand\"\n        self.last_warnings = []\n        while url:\n            response = await self._post(url, payload, cmdlet)\n            warnings = response.headers.get(\"X-Warnings\")\n            if warnings:\n                self.last_warnings.append(warnings)\n            page = self._json(response) or {}\n            yield page\n            url = page.get(\"@odata.nextLink\")\n\n    async def run(self, cmdlet: str, **parameters: Any) -&gt; list[dict[str, Any]]:\n        \"\"\"Run a cmdlet and return all of its output.\n\n        Args:\n            cmdlet: Cmdlet name such as ``Get-Mailbox``.\n            **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n        Returns:\n            Every output object, as dictionaries. There is no server-side limit: a cmdlet\n            with a large result set is paged through in full. ``ResultSize`` does not change\n            this, because the PowerShell module applies it client side rather than sending\n            it, so ``InvokeCommand`` ignores it. To stop early, use :meth:`iter_pages`.\n\n        Raises:\n            InvokeCommandError: If the service rejects the cmdlet.\n        \"\"\"\n        results: list[dict[str, Any]] = []\n        async for page in self.iter_pages(cmdlet, **parameters):\n            results.extend(page.get(\"value\", []))\n        return results\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.invoke_command.InvokeCommandClient.__init__","title":"<code>__init__(auth: AuthContext, *, client_id: str | None = None, scopes: Sequence[str] | None = None, anchor_mailbox: str | None = None, api_version: Literal['beta', 'v1.0'] = 'beta', page_size: int = 1000, timeout: float = 300.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.AsyncBaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client; see :class:<code>ResourceClient</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Scopes to request. Defaults to <code>https://&lt;host&gt;/.default</code>.</p> <code>None</code> <code>anchor_mailbox</code> <code>str | None</code> <p>Value of the <code>X-AnchorMailbox</code> routing header. Defaults to the signed-in user for a user flow in its own tenant, and to the tenant's system mailbox for app flows and for sibling (GDAP) contexts.</p> <code>None</code> <code>api_version</code> <code>Literal['beta', 'v1.0']</code> <p><code>beta</code> (what the PowerShell module uses) or <code>v1.0</code>.</p> <code>'beta'</code> <code>page_size</code> <code>int</code> <p>Preferred number of results per page.</p> <code>1000</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds. Cmdlets can be slow.</p> <code>300.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>AsyncBaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> Source code in <code>src/azure_auth/clients/invoke_command.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    anchor_mailbox: str | None = None,\n    api_version: Literal[\"beta\", \"v1.0\"] = \"beta\",\n    page_size: int = 1000,\n    timeout: float = 300.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.AsyncBaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        client_id: Client id for this client; see :class:`ResourceClient`.\n        scopes: Scopes to request. Defaults to ``https://&lt;host&gt;/.default``.\n        anchor_mailbox: Value of the ``X-AnchorMailbox`` routing header. Defaults to the\n            signed-in user for a user flow in its own tenant, and to the tenant's system\n            mailbox for app flows and for sibling (GDAP) contexts.\n        api_version: ``beta`` (what the PowerShell module uses) or ``v1.0``.\n        page_size: Preferred number of results per page.\n        timeout: Timeout for each HTTP request, in seconds. Cmdlets can be slow.\n        max_retries: How often a throttled request is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n    \"\"\"\n    super().__init__(\n        auth,\n        base_url=f\"https://{self.HOST}\",\n        client_id=client_id,\n        scopes=scopes,\n        timeout=timeout,\n        max_retries=max_retries,\n        max_retry_wait=max_retry_wait,\n        transport=transport,\n    )\n    self._anchor_mailbox = anchor_mailbox\n    self._api_version = api_version\n    self._page_size = page_size\n    self._connection_id = str(uuid.uuid4())\n    self._tenant_guid: str | None = None\n    self.last_warnings: list[str] = []\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.invoke_command.InvokeCommandClient.iter_pages","title":"<code>iter_pages(cmdlet: str, **parameters: Any) -&gt; AsyncIterator[dict[str, Any]]</code>  <code>async</code>","text":"<p>Run a cmdlet and yield each page of its output.</p> <p>Parameters:</p> Name Type Description Default <code>cmdlet</code> <code>str</code> <p>Cmdlet name such as <code>Get-Mailbox</code>.</p> required <code>**parameters</code> <code>Any</code> <p>Cmdlet parameters. Use <code>True</code> for switch parameters.</p> <code>{}</code> <p>Yields:</p> Type Description <code>AsyncIterator[dict[str, Any]]</code> <p>Each response page; the results are under <code>value</code>.</p> Source code in <code>src/azure_auth/clients/invoke_command.py</code> <pre><code>async def iter_pages(self, cmdlet: str, **parameters: Any) -&gt; AsyncIterator[dict[str, Any]]:\n    \"\"\"Run a cmdlet and yield each page of its output.\n\n    Args:\n        cmdlet: Cmdlet name such as ``Get-Mailbox``.\n        **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n    Yields:\n        Each response page; the results are under ``value``.\n    \"\"\"\n    payload = {\"CmdletInput\": {\"CmdletName\": cmdlet, \"Parameters\": parameters}}\n    tenant = await self._tenant()\n    url: str | None = f\"/adminapi/{self._api_version}/{tenant}/InvokeCommand\"\n    self.last_warnings = []\n    while url:\n        response = await self._post(url, payload, cmdlet)\n        warnings = response.headers.get(\"X-Warnings\")\n        if warnings:\n            self.last_warnings.append(warnings)\n        page = self._json(response) or {}\n        yield page\n        url = page.get(\"@odata.nextLink\")\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.invoke_command.InvokeCommandClient.run","title":"<code>run(cmdlet: str, **parameters: Any) -&gt; list[dict[str, Any]]</code>  <code>async</code>","text":"<p>Run a cmdlet and return all of its output.</p> <p>Parameters:</p> Name Type Description Default <code>cmdlet</code> <code>str</code> <p>Cmdlet name such as <code>Get-Mailbox</code>.</p> required <code>**parameters</code> <code>Any</code> <p>Cmdlet parameters. Use <code>True</code> for switch parameters.</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>Every output object, as dictionaries. There is no server-side limit: a cmdlet</p> <code>list[dict[str, Any]]</code> <p>with a large result set is paged through in full. <code>ResultSize</code> does not change</p> <code>list[dict[str, Any]]</code> <p>this, because the PowerShell module applies it client side rather than sending</p> <code>list[dict[str, Any]]</code> <p>it, so <code>InvokeCommand</code> ignores it. To stop early, use :meth:<code>iter_pages</code>.</p> <p>Raises:</p> Type Description <code>InvokeCommandError</code> <p>If the service rejects the cmdlet.</p> Source code in <code>src/azure_auth/clients/invoke_command.py</code> <pre><code>async def run(self, cmdlet: str, **parameters: Any) -&gt; list[dict[str, Any]]:\n    \"\"\"Run a cmdlet and return all of its output.\n\n    Args:\n        cmdlet: Cmdlet name such as ``Get-Mailbox``.\n        **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n    Returns:\n        Every output object, as dictionaries. There is no server-side limit: a cmdlet\n        with a large result set is paged through in full. ``ResultSize`` does not change\n        this, because the PowerShell module applies it client side rather than sending\n        it, so ``InvokeCommand`` ignores it. To stop early, use :meth:`iter_pages`.\n\n    Raises:\n        InvokeCommandError: If the service rejects the cmdlet.\n    \"\"\"\n    results: list[dict[str, Any]] = []\n    async for page in self.iter_pages(cmdlet, **parameters):\n        results.extend(page.get(\"value\", []))\n    return results\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.invoke_command.tenant_id_from_token","title":"<code>tenant_id_from_token(token: str) -&gt; str | None</code>","text":"<p>Read the <code>tid</code> claim of an access token, without validating the token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>A JWT access token.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>The tenant id (GUID), or <code>None</code> when the token cannot be decoded.</p> Source code in <code>src/azure_auth/clients/invoke_command.py</code> <pre><code>def tenant_id_from_token(token: str) -&gt; str | None:\n    \"\"\"Read the ``tid`` claim of an access token, without validating the token.\n\n    Args:\n        token: A JWT access token.\n\n    Returns:\n        The tenant id (GUID), or ``None`` when the token cannot be decoded.\n    \"\"\"\n    parts = token.split(\".\")\n    if len(parts) &lt; 2:  # a JWT is header.payload[.signature]\n        return None\n    try:\n        payload = json_module.loads(base64.urlsafe_b64decode(parts[1] + \"=\" * (-len(parts[1]) % 4)))\n    except binascii.Error, ValueError:\n        return None\n    tenant = payload.get(\"tid\") if isinstance(payload, dict) else None\n    return str(tenant) if tenant else None\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.exchange.ExchangeClient","title":"<code>ExchangeClient</code>","text":"<p>               Bases: <code>InvokeCommandClient</code></p> <p>Run Exchange Online cmdlets without PowerShell.</p> <p>User flows default to the Exchange Online PowerShell client id, which is pre-authorised for Exchange. App flows need the <code>Exchange.ManageAsApp</code> application permission and an Exchange administrator role assigned to the service principal.</p> Example <p>exchange = ExchangeClient(auth)  # doctest: +SKIP mailboxes = await exchange.run(\"Get-Mailbox\")  # doctest: +SKIP</p> Note <p><code>ResultSize</code> is not a cmdlet parameter on this endpoint. The PowerShell module applies it client side while consuming pages, so <code>InvokeCommand</code> ignores it and <code>run</code> returns every result. Use :meth:<code>~InvokeCommandClient.iter_pages</code> and stop when you have enough.</p> Source code in <code>src/azure_auth/clients/exchange.py</code> <pre><code>class ExchangeClient(InvokeCommandClient):\n    \"\"\"Run Exchange Online cmdlets without PowerShell.\n\n    User flows default to the Exchange Online PowerShell client id, which is pre-authorised\n    for Exchange. App flows need the ``Exchange.ManageAsApp`` application permission and an\n    Exchange administrator role assigned to the service principal.\n\n    Example:\n        &gt;&gt;&gt; exchange = ExchangeClient(auth)  # doctest: +SKIP\n        &gt;&gt;&gt; mailboxes = await exchange.run(\"Get-Mailbox\")  # doctest: +SKIP\n\n    Note:\n        ``ResultSize`` is not a cmdlet parameter on this endpoint. The PowerShell module\n        applies it client side while consuming pages, so ``InvokeCommand`` ignores it and\n        ``run`` returns every result. Use :meth:`~InvokeCommandClient.iter_pages` and stop\n        when you have enough.\n    \"\"\"\n\n    RESOURCE: ClassVar[str] = EXCHANGE_RESOURCE\n    HOST: ClassVar[str] = \"outlook.office365.com\"\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.ipps.IppsClient","title":"<code>IppsClient</code>","text":"<p>               Bases: <code>InvokeCommandClient</code></p> <p>Run Security &amp; Compliance cmdlets without PowerShell.</p> <p>The service redirects the first call to a regional host. The client follows that redirect itself and keeps using the regional host, while tokens stay scoped to the global host.</p> Example <p>ipps = IppsClient(auth)  # doctest: +SKIP labels = await ipps.run(\"Get-Label\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/clients/ipps.py</code> <pre><code>class IppsClient(InvokeCommandClient):\n    \"\"\"Run Security &amp; Compliance cmdlets without PowerShell.\n\n    The service redirects the first call to a regional host. The client follows that\n    redirect itself and keeps using the regional host, while tokens stay scoped to the\n    global host.\n\n    Example:\n        &gt;&gt;&gt; ipps = IppsClient(auth)  # doctest: +SKIP\n        &gt;&gt;&gt; labels = await ipps.run(\"Get-Label\")  # doctest: +SKIP\n    \"\"\"\n\n    RESOURCE: ClassVar[str] = IPPS_RESOURCE\n    HOST: ClassVar[str] = \"ps.compliance.protection.outlook.com\"\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient","title":"<code>KeyVaultClient</code>","text":"<p>Secrets, certificates and signing keys of one vault.</p> Example <p>vault = KeyVaultClient(auth, \"https://contoso.vault.azure.net\")  # doctest: +SKIP password = await vault.get_secret(\"service-password\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>class KeyVaultClient:\n    \"\"\"Secrets, certificates and signing keys of one vault.\n\n    Example:\n        &gt;&gt;&gt; vault = KeyVaultClient(auth, \"https://contoso.vault.azure.net\")  # doctest: +SKIP\n        &gt;&gt;&gt; password = await vault.get_secret(\"service-password\")  # doctest: +SKIP\n    \"\"\"\n\n    def __init__(self, auth: AuthContext, vault_url: str) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from. User flows use the Azure\n                PowerShell client id unless the context names another.\n            vault_url: URL of the vault, such as ``https://contoso.vault.azure.net``.\n        \"\"\"\n        self._vault_url = vault_url.rstrip(\"/\")\n        self._credential = auth.aio\n        self._secrets = SecretClient(self._vault_url, self._credential)\n        self._certificates = CertificateClient(self._vault_url, self._credential)\n        self._crypto: dict[str, CryptographyClient] = {}\n\n    @property\n    def vault_url(self) -&gt; str:\n        \"\"\"URL of the vault.\"\"\"\n        return self._vault_url\n\n    async def get_secret(self, name: str, version: str | None = None) -&gt; str:\n        \"\"\"Read a secret value.\n\n        Args:\n            name: Name of the secret.\n            version: Version to read. Defaults to the latest.\n\n        Returns:\n            The secret value.\n\n        Raises:\n            ValueError: If the secret has no value.\n        \"\"\"\n        secret = await self._secrets.get_secret(name, version)\n        if secret.value is None:\n            raise ValueError(f\"Secret {name!r} has no value\")\n        return secret.value\n\n    async def get_certificate(self, name: str, version: str | None = None) -&gt; bytes:\n        \"\"\"Read the public part of a certificate.\n\n        Args:\n            name: Name of the certificate.\n            version: Version to read. Defaults to the latest.\n\n        Returns:\n            The DER-encoded certificate, without its private key.\n\n        Raises:\n            ValueError: If the certificate has no content.\n        \"\"\"\n        if version:\n            certificate = await self._certificates.get_certificate_version(name, version)\n        else:\n            certificate = await self._certificates.get_certificate(name)\n        if certificate.cer is None:\n            raise ValueError(f\"Certificate {name!r} has no content\")\n        return bytes(certificate.cer)\n\n    def _crypto_client(self, key_name: str, version: str | None) -&gt; CryptographyClient:\n        \"\"\"Return the cryptography client for one key.\n\n        Args:\n            key_name: Name of the key.\n            version: Key version, or ``None`` for the latest.\n\n        Returns:\n            A memoised ``CryptographyClient``.\n        \"\"\"\n        key_id = f\"{self._vault_url}/keys/{key_name}\" + (f\"/{version}\" if version else \"\")\n        if key_id not in self._crypto:\n            self._crypto[key_id] = CryptographyClient(key_id, self._credential)\n        return self._crypto[key_id]\n\n    async def sign(\n        self,\n        key_name: str,\n        algorithm: SignatureAlgorithm | str,\n        digest: bytes,\n        *,\n        version: str | None = None,\n    ) -&gt; bytes:\n        \"\"\"Sign a digest with a key that stays in the vault.\n\n        Args:\n            key_name: Name of the key.\n            algorithm: Signature algorithm, such as ``PS256`` or ``RS256``.\n            digest: The digest to sign, hashed to match ``algorithm``.\n            version: Key version. Defaults to the latest.\n\n        Returns:\n            The signature.\n        \"\"\"\n        client = self._crypto_client(key_name, version)\n        result = await client.sign(SignatureAlgorithm(algorithm), digest)\n        return bytes(result.signature)\n\n    async def verify(\n        self,\n        key_name: str,\n        algorithm: SignatureAlgorithm | str,\n        digest: bytes,\n        signature: bytes,\n        *,\n        version: str | None = None,\n    ) -&gt; bool:\n        \"\"\"Verify a signature with a key in the vault.\n\n        Args:\n            key_name: Name of the key.\n            algorithm: Signature algorithm, such as ``PS256`` or ``RS256``.\n            digest: The digest that was signed.\n            signature: The signature to check.\n            version: Key version. Defaults to the latest.\n\n        Returns:\n            Whether the signature is valid.\n        \"\"\"\n        client = self._crypto_client(key_name, version)\n        result = await client.verify(SignatureAlgorithm(algorithm), digest, signature)\n        return bool(result.is_valid)\n\n    async def aclose(self) -&gt; None:\n        \"\"\"Close the underlying SDK clients.\"\"\"\n        await self._secrets.close()\n        await self._certificates.close()\n        for client in self._crypto.values():\n            await client.close()\n\n    async def __aenter__(self) -&gt; Self:\n        \"\"\"Enter the context manager.\n\n        Returns:\n            This client.\n        \"\"\"\n        return self\n\n    async def __aexit__(\n        self,\n        exc_type: type[BaseException] | None = None,\n        exc_value: BaseException | None = None,\n        traceback: TracebackType | None = None,\n    ) -&gt; None:\n        \"\"\"Close the client when leaving the context manager.\"\"\"\n        await self.aclose()\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.vault_url","title":"<code>vault_url: str</code>  <code>property</code>","text":"<p>URL of the vault.</p>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.__aenter__","title":"<code>__aenter__() -&gt; Self</code>  <code>async</code>","text":"<p>Enter the context manager.</p> <p>Returns:</p> Type Description <code>Self</code> <p>This client.</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>async def __aenter__(self) -&gt; Self:\n    \"\"\"Enter the context manager.\n\n    Returns:\n        This client.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.__aexit__","title":"<code>__aexit__(exc_type: type[BaseException] | None = None, exc_value: BaseException | None = None, traceback: TracebackType | None = None) -&gt; None</code>  <code>async</code>","text":"<p>Close the client when leaving the context manager.</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>async def __aexit__(\n    self,\n    exc_type: type[BaseException] | None = None,\n    exc_value: BaseException | None = None,\n    traceback: TracebackType | None = None,\n) -&gt; None:\n    \"\"\"Close the client when leaving the context manager.\"\"\"\n    await self.aclose()\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.__init__","title":"<code>__init__(auth: AuthContext, vault_url: str) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from. User flows use the Azure PowerShell client id unless the context names another.</p> required <code>vault_url</code> <code>str</code> <p>URL of the vault, such as <code>https://contoso.vault.azure.net</code>.</p> required Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>def __init__(self, auth: AuthContext, vault_url: str) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from. User flows use the Azure\n            PowerShell client id unless the context names another.\n        vault_url: URL of the vault, such as ``https://contoso.vault.azure.net``.\n    \"\"\"\n    self._vault_url = vault_url.rstrip(\"/\")\n    self._credential = auth.aio\n    self._secrets = SecretClient(self._vault_url, self._credential)\n    self._certificates = CertificateClient(self._vault_url, self._credential)\n    self._crypto: dict[str, CryptographyClient] = {}\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.aclose","title":"<code>aclose() -&gt; None</code>  <code>async</code>","text":"<p>Close the underlying SDK clients.</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying SDK clients.\"\"\"\n    await self._secrets.close()\n    await self._certificates.close()\n    for client in self._crypto.values():\n        await client.close()\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.get_certificate","title":"<code>get_certificate(name: str, version: str | None = None) -&gt; bytes</code>  <code>async</code>","text":"<p>Read the public part of a certificate.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the certificate.</p> required <code>version</code> <code>str | None</code> <p>Version to read. Defaults to the latest.</p> <code>None</code> <p>Returns:</p> Type Description <code>bytes</code> <p>The DER-encoded certificate, without its private key.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the certificate has no content.</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>async def get_certificate(self, name: str, version: str | None = None) -&gt; bytes:\n    \"\"\"Read the public part of a certificate.\n\n    Args:\n        name: Name of the certificate.\n        version: Version to read. Defaults to the latest.\n\n    Returns:\n        The DER-encoded certificate, without its private key.\n\n    Raises:\n        ValueError: If the certificate has no content.\n    \"\"\"\n    if version:\n        certificate = await self._certificates.get_certificate_version(name, version)\n    else:\n        certificate = await self._certificates.get_certificate(name)\n    if certificate.cer is None:\n        raise ValueError(f\"Certificate {name!r} has no content\")\n    return bytes(certificate.cer)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.get_secret","title":"<code>get_secret(name: str, version: str | None = None) -&gt; str</code>  <code>async</code>","text":"<p>Read a secret value.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the secret.</p> required <code>version</code> <code>str | None</code> <p>Version to read. Defaults to the latest.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>The secret value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the secret has no value.</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>async def get_secret(self, name: str, version: str | None = None) -&gt; str:\n    \"\"\"Read a secret value.\n\n    Args:\n        name: Name of the secret.\n        version: Version to read. Defaults to the latest.\n\n    Returns:\n        The secret value.\n\n    Raises:\n        ValueError: If the secret has no value.\n    \"\"\"\n    secret = await self._secrets.get_secret(name, version)\n    if secret.value is None:\n        raise ValueError(f\"Secret {name!r} has no value\")\n    return secret.value\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.sign","title":"<code>sign(key_name: str, algorithm: SignatureAlgorithm | str, digest: bytes, *, version: str | None = None) -&gt; bytes</code>  <code>async</code>","text":"<p>Sign a digest with a key that stays in the vault.</p> <p>Parameters:</p> Name Type Description Default <code>key_name</code> <code>str</code> <p>Name of the key.</p> required <code>algorithm</code> <code>SignatureAlgorithm | str</code> <p>Signature algorithm, such as <code>PS256</code> or <code>RS256</code>.</p> required <code>digest</code> <code>bytes</code> <p>The digest to sign, hashed to match <code>algorithm</code>.</p> required <code>version</code> <code>str | None</code> <p>Key version. Defaults to the latest.</p> <code>None</code> <p>Returns:</p> Type Description <code>bytes</code> <p>The signature.</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>async def sign(\n    self,\n    key_name: str,\n    algorithm: SignatureAlgorithm | str,\n    digest: bytes,\n    *,\n    version: str | None = None,\n) -&gt; bytes:\n    \"\"\"Sign a digest with a key that stays in the vault.\n\n    Args:\n        key_name: Name of the key.\n        algorithm: Signature algorithm, such as ``PS256`` or ``RS256``.\n        digest: The digest to sign, hashed to match ``algorithm``.\n        version: Key version. Defaults to the latest.\n\n    Returns:\n        The signature.\n    \"\"\"\n    client = self._crypto_client(key_name, version)\n    result = await client.sign(SignatureAlgorithm(algorithm), digest)\n    return bytes(result.signature)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.keyvault.KeyVaultClient.verify","title":"<code>verify(key_name: str, algorithm: SignatureAlgorithm | str, digest: bytes, signature: bytes, *, version: str | None = None) -&gt; bool</code>  <code>async</code>","text":"<p>Verify a signature with a key in the vault.</p> <p>Parameters:</p> Name Type Description Default <code>key_name</code> <code>str</code> <p>Name of the key.</p> required <code>algorithm</code> <code>SignatureAlgorithm | str</code> <p>Signature algorithm, such as <code>PS256</code> or <code>RS256</code>.</p> required <code>digest</code> <code>bytes</code> <p>The digest that was signed.</p> required <code>signature</code> <code>bytes</code> <p>The signature to check.</p> required <code>version</code> <code>str | None</code> <p>Key version. Defaults to the latest.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether the signature is valid.</p> Source code in <code>src/azure_auth/clients/keyvault.py</code> <pre><code>async def verify(\n    self,\n    key_name: str,\n    algorithm: SignatureAlgorithm | str,\n    digest: bytes,\n    signature: bytes,\n    *,\n    version: str | None = None,\n) -&gt; bool:\n    \"\"\"Verify a signature with a key in the vault.\n\n    Args:\n        key_name: Name of the key.\n        algorithm: Signature algorithm, such as ``PS256`` or ``RS256``.\n        digest: The digest that was signed.\n        signature: The signature to check.\n        version: Key version. Defaults to the latest.\n\n    Returns:\n        Whether the signature is valid.\n    \"\"\"\n    client = self._crypto_client(key_name, version)\n    result = await client.verify(SignatureAlgorithm(algorithm), digest, signature)\n    return bool(result.is_valid)\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.errors.AzureError","title":"<code>AzureError</code>","text":"<p>               Bases: <code>ResourceError</code></p> <p>Azure Resource Manager rejected a request or a long-running operation failed.</p> Source code in <code>src/azure_auth/clients/errors.py</code> <pre><code>class AzureError(ResourceError):\n    \"\"\"Azure Resource Manager rejected a request or a long-running operation failed.\"\"\"\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.errors.GraphError","title":"<code>GraphError</code>","text":"<p>               Bases: <code>ResourceError</code></p> <p>Microsoft Graph rejected a request.</p> Source code in <code>src/azure_auth/clients/errors.py</code> <pre><code>class GraphError(ResourceError):\n    \"\"\"Microsoft Graph rejected a request.\"\"\"\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.errors.InvokeCommandError","title":"<code>InvokeCommandError</code>","text":"<p>               Bases: <code>ResourceError</code></p> <p>Exchange Online or Security &amp; Compliance rejected a cmdlet invocation.</p> Source code in <code>src/azure_auth/clients/errors.py</code> <pre><code>class InvokeCommandError(ResourceError):\n    \"\"\"Exchange Online or Security &amp; Compliance rejected a cmdlet invocation.\"\"\"\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.errors.ResourceError","title":"<code>ResourceError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>A resource answered with an error status.</p> <p>Attributes:</p> Name Type Description <code>status</code> <p>HTTP status code.</p> <code>code</code> <p>Service error code, such as <code>Request_ResourceNotFound</code>, when one was given.</p> <code>request_id</code> <p>Service request id for support cases, when one was given.</p> <code>body</code> <p>The decoded response body: parsed JSON when possible, else text.</p> Source code in <code>src/azure_auth/clients/errors.py</code> <pre><code>class ResourceError(Exception):\n    \"\"\"A resource answered with an error status.\n\n    Attributes:\n        status: HTTP status code.\n        code: Service error code, such as ``Request_ResourceNotFound``, when one was given.\n        request_id: Service request id for support cases, when one was given.\n        body: The decoded response body: parsed JSON when possible, else text.\n    \"\"\"\n\n    def __init__(\n        self,\n        message: str,\n        *,\n        status: int,\n        code: str | None = None,\n        request_id: str | None = None,\n        body: Any = None,\n    ) -&gt; None:\n        \"\"\"Create the error.\n\n        Args:\n            message: Human readable description.\n            status: HTTP status code.\n            code: Service error code.\n            request_id: Service request id.\n            body: The decoded response body.\n        \"\"\"\n        super().__init__(message)\n        self.status = status\n        self.code = code\n        self.request_id = request_id\n        self.body = body\n</code></pre>"},{"location":"reference/clients/#azure_auth.clients.errors.ResourceError.__init__","title":"<code>__init__(message: str, *, status: int, code: str | None = None, request_id: str | None = None, body: Any = None) -&gt; None</code>","text":"<p>Create the error.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Human readable description.</p> required <code>status</code> <code>int</code> <p>HTTP status code.</p> required <code>code</code> <code>str | None</code> <p>Service error code.</p> <code>None</code> <code>request_id</code> <code>str | None</code> <p>Service request id.</p> <code>None</code> <code>body</code> <code>Any</code> <p>The decoded response body.</p> <code>None</code> Source code in <code>src/azure_auth/clients/errors.py</code> <pre><code>def __init__(\n    self,\n    message: str,\n    *,\n    status: int,\n    code: str | None = None,\n    request_id: str | None = None,\n    body: Any = None,\n) -&gt; None:\n    \"\"\"Create the error.\n\n    Args:\n        message: Human readable description.\n        status: HTTP status code.\n        code: Service error code.\n        request_id: Service request id.\n        body: The decoded response body.\n    \"\"\"\n    super().__init__(message)\n    self.status = status\n    self.code = code\n    self.request_id = request_id\n    self.body = body\n</code></pre>"},{"location":"reference/sync/","title":"Blocking clients","text":"<p>Blocking versions of the resource clients.</p> <p>These mirror :mod:<code>azure_auth.clients</code> one to one: same classes, same methods, no <code>await</code>. They are generated from the asynchronous clients, so behaviour is identical.</p> Example <p>from azure_auth import AuthContext from azure_auth.sync import GraphClient auth = AuthContext(\"contoso.onmicrosoft.com\", username=\"admin@contoso.com\") with GraphClient(auth, scopes=[\"User.Read.All\"]) as graph:  # doctest: +SKIP ...     users = graph.get_all(\"/users\")</p> <p><code>KeyVaultClient</code> needs the optional <code>keyvault</code> extra; import it from :mod:<code>azure_auth.sync.keyvault</code>.</p> <p>The blocking clients are generated from the asynchronous ones and have the same methods without <code>await</code>. See Clients for the full documentation.</p>"},{"location":"reference/sync/#azure_auth.sync.AzureClient","title":"<code>AzureClient</code>","text":"<p>               Bases: <code>ResourceClient</code></p> <p>Authenticated requests to Azure Resource Manager.</p> <p>Every call needs the <code>api_version</code> of the resource provider being addressed.</p> Example <p>arm = AzureClient(auth)  # doctest: +SKIP subs = arm.get_all(\"/subscriptions\", api_version=\"2022-12-01\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>class AzureClient(ResourceClient):\n    \"\"\"Authenticated requests to Azure Resource Manager.\n\n    Every call needs the ``api_version`` of the resource provider being addressed.\n\n    Example:\n        &gt;&gt;&gt; arm = AzureClient(auth)  # doctest: +SKIP\n        &gt;&gt;&gt; subs = arm.get_all(\"/subscriptions\", api_version=\"2022-12-01\")  # doctest: +SKIP\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str] = AZURE_POWERSHELL_CLIENT_ID\n    RESOURCE: ClassVar[str] = ARM_RESOURCE\n    ERROR_CLASS: ClassVar[type[ResourceError]] = AzureError\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        timeout: float = 100.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.BaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            client_id: Client id for this client; see :class:`ResourceClient`.\n            scopes: Scopes to request. Defaults to ``https://management.azure.com/.default``.\n            timeout: Timeout for each HTTP request, in seconds.\n            max_retries: How often a throttled request is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n        \"\"\"\n        super().__init__(\n            auth,\n            base_url=ARM_RESOURCE,\n            client_id=client_id,\n            scopes=scopes,\n            timeout=timeout,\n            max_retries=max_retries,\n            max_retry_wait=max_retry_wait,\n            transport=transport,\n        )\n\n    def send(\n        self,\n        method: str,\n        path: str,\n        *,\n        api_version: str,\n        params: Mapping[str, Any] | None = None,\n        json: Any = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; httpx.Response:\n        \"\"\"Send a request with its ``api-version`` and return the raw response.\n\n        Use this when the response headers matter, for example to pass the response of a\n        long-running operation to :meth:`wait`.\n\n        Args:\n            method: HTTP method.\n            path: Resource path such as ``/subscriptions/{id}/resourceGroups/{name}``.\n            api_version: API version of the resource provider.\n            params: Further query parameters.\n            json: Request body.\n            headers: Extra request headers.\n\n        Returns:\n            The successful response.\n        \"\"\"\n        query = {**(params or {}), \"api-version\": api_version}\n        return self.request(method, path, params=query, json=json, headers=headers)\n\n    def get(self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; Any:\n        \"\"\"Send a GET request.\n\n        Args:\n            path: Resource path.\n            api_version: API version of the resource provider.\n            params: Further query parameters.\n\n        Returns:\n            The parsed JSON body.\n        \"\"\"\n        return self._json(self.send(\"GET\", path, api_version=api_version, params=params))\n\n    def put(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n        \"\"\"Send a PUT request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n        Args:\n            path: Resource path.\n            json: Request body.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(self.send(\"PUT\", path, api_version=api_version, json=json))\n\n    def post(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n        \"\"\"Send a POST request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n        Args:\n            path: Resource or action path.\n            json: Request body.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(self.send(\"POST\", path, api_version=api_version, json=json))\n\n    def patch(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n        \"\"\"Send a PATCH request.\n\n        Args:\n            path: Resource path.\n            json: Request body.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(self.send(\"PATCH\", path, api_version=api_version, json=json))\n\n    def delete(self, path: str, *, api_version: str) -&gt; Any:\n        \"\"\"Send a DELETE request. For long-running deletes use :meth:`send` and :meth:`wait`.\n\n        Args:\n            path: Resource path.\n            api_version: API version of the resource provider.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(self.send(\"DELETE\", path, api_version=api_version))\n\n    def iter_pages(\n        self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n    ) -&gt; Iterator[dict[str, Any]]:\n        \"\"\"Yield every page of a list operation, following ``nextLink``.\n\n        Args:\n            path: Path of the list operation.\n            api_version: API version of the resource provider.\n            params: Further query parameters for the first page.\n\n        Yields:\n            Each page as returned by Azure Resource Manager.\n        \"\"\"\n        page = self.get(path, api_version=api_version, params=params)\n        yield page\n        while page.get(\"nextLink\"):\n            # A next link already carries api-version and every other parameter.\n            page = self._json(self.request(\"GET\", page[\"nextLink\"]))\n            yield page\n\n    def get_all(\n        self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n    ) -&gt; list[Any]:\n        \"\"\"Return every item of a list operation, following ``nextLink``.\n\n        Args:\n            path: Path of the list operation.\n            api_version: API version of the resource provider.\n            params: Further query parameters for the first page.\n\n        Returns:\n            The items of all pages, in order.\n        \"\"\"\n        items: list[Any] = []\n        for page in self.iter_pages(path, api_version=api_version, params=params):\n            items.extend(page.get(\"value\", []))\n        return items\n\n    def wait(\n        self,\n        response: httpx.Response,\n        *,\n        timeout: float = 1800.0,\n        poll_interval: float | None = None,\n    ) -&gt; Any:\n        \"\"\"Wait for the long-running operation started by ``response`` to finish.\n\n        Polls the ``Azure-AsyncOperation`` URL when the response has one, else the\n        ``Location`` URL, waiting ``Retry-After`` seconds between polls.\n\n        Args:\n            response: The response of :meth:`send` that started the operation.\n            timeout: Give up after this many seconds.\n            poll_interval: Seconds between polls; overrides ``Retry-After``.\n\n        Returns:\n            The final resource for PUT and PATCH, the ``Location`` result when there is one,\n            else the final operation status. When the operation had already completed, the\n            body of ``response``.\n\n        Raises:\n            AzureError: If the operation fails or is cancelled.\n            TimeoutError: If the operation does not finish in time.\n        \"\"\"\n        deadline = time.monotonic() + timeout\n        operation_url = response.headers.get(\"Azure-AsyncOperation\")\n        location_url = response.headers.get(\"Location\")\n\n        def delay(latest: httpx.Response) -&gt; float:\n            if poll_interval is not None:\n                return poll_interval\n            if \"Retry-After\" in latest.headers:\n                return retry_delay(latest, 1, self._max_retry_wait)\n            return _DEFAULT_POLL_SECONDS\n\n        if operation_url:\n            latest = response\n            while True:\n                if time.monotonic() &gt; deadline:\n                    raise TimeoutError(f\"Operation still running after {timeout} s\")\n                time.sleep(delay(latest))\n                latest = self.request(\"GET\", operation_url)\n                status = self._json(latest) or {}\n                state = str(status.get(\"status\", \"\")).lower()\n                if state in _TERMINAL_FAILURES:\n                    raise self._operation_error(latest, status)\n                if state == \"succeeded\":\n                    break\n            if response.request.method in {\"PUT\", \"PATCH\"}:\n                return self._json(self.request(\"GET\", str(response.request.url)))\n            if location_url:\n                return self._json(self.request(\"GET\", location_url))\n            return status\n\n        if response.status_code == httpx.codes.ACCEPTED and location_url:\n            latest = response\n            while latest.status_code == httpx.codes.ACCEPTED:\n                if time.monotonic() &gt; deadline:\n                    raise TimeoutError(f\"Operation still running after {timeout} s\")\n                time.sleep(delay(latest))\n                latest = self.request(\"GET\", location_url)\n            return self._json(latest)\n\n        return self._json(response)\n\n    def _operation_error(self, response: httpx.Response, status: Mapping[str, Any]) -&gt; AzureError:\n        \"\"\"Build the exception for a failed long-running operation.\n\n        Args:\n            response: The poll response that reported the failure.\n            status: The operation status body.\n\n        Returns:\n            The exception to raise.\n        \"\"\"\n        error = status.get(\"error\") or {}\n        code = error.get(\"code\") if isinstance(error, dict) else None\n        message = error.get(\"message\") if isinstance(error, dict) else None\n        detail = \": \".join(str(part) for part in (code, message) if part)\n        return AzureError(\n            f\"Operation {status.get('status')} {detail}\".rstrip(),\n            status=response.status_code,\n            code=str(code) if code else None,\n            request_id=response.headers.get(\"x-ms-request-id\"),\n            body=dict(status),\n        )\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.__init__","title":"<code>__init__(auth: AuthContext, *, client_id: str | None = None, scopes: Sequence[str] | None = None, timeout: float = 100.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.BaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client; see :class:<code>ResourceClient</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Scopes to request. Defaults to <code>https://management.azure.com/.default</code>.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds.</p> <code>100.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>BaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    timeout: float = 100.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.BaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        client_id: Client id for this client; see :class:`ResourceClient`.\n        scopes: Scopes to request. Defaults to ``https://management.azure.com/.default``.\n        timeout: Timeout for each HTTP request, in seconds.\n        max_retries: How often a throttled request is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n    \"\"\"\n    super().__init__(\n        auth,\n        base_url=ARM_RESOURCE,\n        client_id=client_id,\n        scopes=scopes,\n        timeout=timeout,\n        max_retries=max_retries,\n        max_retry_wait=max_retry_wait,\n        transport=transport,\n    )\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.delete","title":"<code>delete(path: str, *, api_version: str) -&gt; Any</code>","text":"<p>Send a DELETE request. For long-running deletes use :meth:<code>send</code> and :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def delete(self, path: str, *, api_version: str) -&gt; Any:\n    \"\"\"Send a DELETE request. For long-running deletes use :meth:`send` and :meth:`wait`.\n\n    Args:\n        path: Resource path.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(self.send(\"DELETE\", path, api_version=api_version))\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.get","title":"<code>get(path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; Any</code>","text":"<p>Send a GET request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def get(self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; Any:\n    \"\"\"Send a GET request.\n\n    Args:\n        path: Resource path.\n        api_version: API version of the resource provider.\n        params: Further query parameters.\n\n    Returns:\n        The parsed JSON body.\n    \"\"\"\n    return self._json(self.send(\"GET\", path, api_version=api_version, params=params))\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.get_all","title":"<code>get_all(path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; list[Any]</code>","text":"<p>Return every item of a list operation, following <code>nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the list operation.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters for the first page.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Any]</code> <p>The items of all pages, in order.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def get_all(\n    self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n) -&gt; list[Any]:\n    \"\"\"Return every item of a list operation, following ``nextLink``.\n\n    Args:\n        path: Path of the list operation.\n        api_version: API version of the resource provider.\n        params: Further query parameters for the first page.\n\n    Returns:\n        The items of all pages, in order.\n    \"\"\"\n    items: list[Any] = []\n    for page in self.iter_pages(path, api_version=api_version, params=params):\n        items.extend(page.get(\"value\", []))\n    return items\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.iter_pages","title":"<code>iter_pages(path: str, *, api_version: str, params: Mapping[str, Any] | None = None) -&gt; Iterator[dict[str, Any]]</code>","text":"<p>Yield every page of a list operation, following <code>nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the list operation.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters for the first page.</p> <code>None</code> <p>Yields:</p> Type Description <code>dict[str, Any]</code> <p>Each page as returned by Azure Resource Manager.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def iter_pages(\n    self, path: str, *, api_version: str, params: Mapping[str, Any] | None = None\n) -&gt; Iterator[dict[str, Any]]:\n    \"\"\"Yield every page of a list operation, following ``nextLink``.\n\n    Args:\n        path: Path of the list operation.\n        api_version: API version of the resource provider.\n        params: Further query parameters for the first page.\n\n    Yields:\n        Each page as returned by Azure Resource Manager.\n    \"\"\"\n    page = self.get(path, api_version=api_version, params=params)\n    yield page\n    while page.get(\"nextLink\"):\n        # A next link already carries api-version and every other parameter.\n        page = self._json(self.request(\"GET\", page[\"nextLink\"]))\n        yield page\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.patch","title":"<code>patch(path: str, json: Any = None, *, api_version: str) -&gt; Any</code>","text":"<p>Send a PATCH request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def patch(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n    \"\"\"Send a PATCH request.\n\n    Args:\n        path: Resource path.\n        json: Request body.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(self.send(\"PATCH\", path, api_version=api_version, json=json))\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.post","title":"<code>post(path: str, json: Any = None, *, api_version: str) -&gt; Any</code>","text":"<p>Send a POST request. For long-running operations use :meth:<code>send</code> and :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource or action path.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def post(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n    \"\"\"Send a POST request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n    Args:\n        path: Resource or action path.\n        json: Request body.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(self.send(\"POST\", path, api_version=api_version, json=json))\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.put","title":"<code>put(path: str, json: Any = None, *, api_version: str) -&gt; Any</code>","text":"<p>Send a PUT request. For long-running operations use :meth:<code>send</code> and :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Resource path.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def put(self, path: str, json: Any = None, *, api_version: str) -&gt; Any:\n    \"\"\"Send a PUT request. For long-running operations use :meth:`send` and :meth:`wait`.\n\n    Args:\n        path: Resource path.\n        json: Request body.\n        api_version: API version of the resource provider.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(self.send(\"PUT\", path, api_version=api_version, json=json))\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.send","title":"<code>send(method: str, path: str, *, api_version: str, params: Mapping[str, Any] | None = None, json: Any = None, headers: Mapping[str, str] | None = None) -&gt; httpx.Response</code>","text":"<p>Send a request with its <code>api-version</code> and return the raw response.</p> <p>Use this when the response headers matter, for example to pass the response of a long-running operation to :meth:<code>wait</code>.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>HTTP method.</p> required <code>path</code> <code>str</code> <p>Resource path such as <code>/subscriptions/{id}/resourceGroups/{name}</code>.</p> required <code>api_version</code> <code>str</code> <p>API version of the resource provider.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Further query parameters.</p> <code>None</code> <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Response</code> <p>The successful response.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def send(\n    self,\n    method: str,\n    path: str,\n    *,\n    api_version: str,\n    params: Mapping[str, Any] | None = None,\n    json: Any = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; httpx.Response:\n    \"\"\"Send a request with its ``api-version`` and return the raw response.\n\n    Use this when the response headers matter, for example to pass the response of a\n    long-running operation to :meth:`wait`.\n\n    Args:\n        method: HTTP method.\n        path: Resource path such as ``/subscriptions/{id}/resourceGroups/{name}``.\n        api_version: API version of the resource provider.\n        params: Further query parameters.\n        json: Request body.\n        headers: Extra request headers.\n\n    Returns:\n        The successful response.\n    \"\"\"\n    query = {**(params or {}), \"api-version\": api_version}\n    return self.request(method, path, params=query, json=json, headers=headers)\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.AzureClient.wait","title":"<code>wait(response: httpx.Response, *, timeout: float = 1800.0, poll_interval: float | None = None) -&gt; Any</code>","text":"<p>Wait for the long-running operation started by <code>response</code> to finish.</p> <p>Polls the <code>Azure-AsyncOperation</code> URL when the response has one, else the <code>Location</code> URL, waiting <code>Retry-After</code> seconds between polls.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>The response of :meth:<code>send</code> that started the operation.</p> required <code>timeout</code> <code>float</code> <p>Give up after this many seconds.</p> <code>1800.0</code> <code>poll_interval</code> <code>float | None</code> <p>Seconds between polls; overrides <code>Retry-After</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The final resource for PUT and PATCH, the <code>Location</code> result when there is one,</p> <code>Any</code> <p>else the final operation status. When the operation had already completed, the</p> <code>Any</code> <p>body of <code>response</code>.</p> <p>Raises:</p> Type Description <code>AzureError</code> <p>If the operation fails or is cancelled.</p> <code>TimeoutError</code> <p>If the operation does not finish in time.</p> Source code in <code>src/azure_auth/_sync/arm.py</code> <pre><code>def wait(\n    self,\n    response: httpx.Response,\n    *,\n    timeout: float = 1800.0,\n    poll_interval: float | None = None,\n) -&gt; Any:\n    \"\"\"Wait for the long-running operation started by ``response`` to finish.\n\n    Polls the ``Azure-AsyncOperation`` URL when the response has one, else the\n    ``Location`` URL, waiting ``Retry-After`` seconds between polls.\n\n    Args:\n        response: The response of :meth:`send` that started the operation.\n        timeout: Give up after this many seconds.\n        poll_interval: Seconds between polls; overrides ``Retry-After``.\n\n    Returns:\n        The final resource for PUT and PATCH, the ``Location`` result when there is one,\n        else the final operation status. When the operation had already completed, the\n        body of ``response``.\n\n    Raises:\n        AzureError: If the operation fails or is cancelled.\n        TimeoutError: If the operation does not finish in time.\n    \"\"\"\n    deadline = time.monotonic() + timeout\n    operation_url = response.headers.get(\"Azure-AsyncOperation\")\n    location_url = response.headers.get(\"Location\")\n\n    def delay(latest: httpx.Response) -&gt; float:\n        if poll_interval is not None:\n            return poll_interval\n        if \"Retry-After\" in latest.headers:\n            return retry_delay(latest, 1, self._max_retry_wait)\n        return _DEFAULT_POLL_SECONDS\n\n    if operation_url:\n        latest = response\n        while True:\n            if time.monotonic() &gt; deadline:\n                raise TimeoutError(f\"Operation still running after {timeout} s\")\n            time.sleep(delay(latest))\n            latest = self.request(\"GET\", operation_url)\n            status = self._json(latest) or {}\n            state = str(status.get(\"status\", \"\")).lower()\n            if state in _TERMINAL_FAILURES:\n                raise self._operation_error(latest, status)\n            if state == \"succeeded\":\n                break\n        if response.request.method in {\"PUT\", \"PATCH\"}:\n            return self._json(self.request(\"GET\", str(response.request.url)))\n        if location_url:\n            return self._json(self.request(\"GET\", location_url))\n        return status\n\n    if response.status_code == httpx.codes.ACCEPTED and location_url:\n        latest = response\n        while latest.status_code == httpx.codes.ACCEPTED:\n            if time.monotonic() &gt; deadline:\n                raise TimeoutError(f\"Operation still running after {timeout} s\")\n            time.sleep(delay(latest))\n            latest = self.request(\"GET\", location_url)\n        return self._json(latest)\n\n    return self._json(response)\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ExchangeClient","title":"<code>ExchangeClient</code>","text":"<p>               Bases: <code>InvokeCommandClient</code></p> <p>Run Exchange Online cmdlets without PowerShell.</p> <p>User flows default to the Exchange Online PowerShell client id, which is pre-authorised for Exchange. App flows need the <code>Exchange.ManageAsApp</code> application permission and an Exchange administrator role assigned to the service principal.</p> Example <p>exchange = ExchangeClient(auth)  # doctest: +SKIP mailboxes = exchange.run(\"Get-Mailbox\")  # doctest: +SKIP</p> Note <p><code>ResultSize</code> is not a cmdlet parameter on this endpoint. The PowerShell module applies it client side while consuming pages, so <code>InvokeCommand</code> ignores it and <code>run</code> returns every result. Use :meth:<code>~InvokeCommandClient.iter_pages</code> and stop when you have enough.</p> Source code in <code>src/azure_auth/_sync/exchange.py</code> <pre><code>class ExchangeClient(InvokeCommandClient):\n    \"\"\"Run Exchange Online cmdlets without PowerShell.\n\n    User flows default to the Exchange Online PowerShell client id, which is pre-authorised\n    for Exchange. App flows need the ``Exchange.ManageAsApp`` application permission and an\n    Exchange administrator role assigned to the service principal.\n\n    Example:\n        &gt;&gt;&gt; exchange = ExchangeClient(auth)  # doctest: +SKIP\n        &gt;&gt;&gt; mailboxes = exchange.run(\"Get-Mailbox\")  # doctest: +SKIP\n\n    Note:\n        ``ResultSize`` is not a cmdlet parameter on this endpoint. The PowerShell module\n        applies it client side while consuming pages, so ``InvokeCommand`` ignores it and\n        ``run`` returns every result. Use :meth:`~InvokeCommandClient.iter_pages` and stop\n        when you have enough.\n    \"\"\"\n\n    RESOURCE: ClassVar[str] = EXCHANGE_RESOURCE\n    HOST: ClassVar[str] = \"outlook.office365.com\"\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient","title":"<code>GraphClient</code>","text":"<p>               Bases: <code>ResourceClient</code></p> <p>Authenticated requests to Microsoft Graph.</p> <p>User flows default to the Microsoft Graph PowerShell client id. That application works by dynamic consent, so pass the delegated <code>scopes</code> you need; without them the token only carries what was consented to in the tenant before. App flows always use <code>https://graph.microsoft.com/.default</code>.</p> Example <p>graph = GraphClient(auth, scopes=[\"User.Read.All\"])  # doctest: +SKIP users = graph.get_all(\"/users\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>class GraphClient(ResourceClient):\n    \"\"\"Authenticated requests to Microsoft Graph.\n\n    User flows default to the Microsoft Graph PowerShell client id. That application works\n    by dynamic consent, so pass the delegated ``scopes`` you need; without them the token\n    only carries what was consented to in the tenant before. App flows always use\n    ``https://graph.microsoft.com/.default``.\n\n    Example:\n        &gt;&gt;&gt; graph = GraphClient(auth, scopes=[\"User.Read.All\"])  # doctest: +SKIP\n        &gt;&gt;&gt; users = graph.get_all(\"/users\")  # doctest: +SKIP\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str] = GRAPH_CLI_CLIENT_ID\n    RESOURCE: ClassVar[str] = GRAPH_RESOURCE\n    ERROR_CLASS: ClassVar[type[ResourceError]] = GraphError\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        api_version: Literal[\"v1.0\", \"beta\"] = \"v1.0\",\n        timeout: float = 100.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.BaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            client_id: Client id for this client; see :class:`ResourceClient`.\n            scopes: Delegated scopes for a user flow, such as ``[\"User.Read.All\"]``.\n            api_version: ``v1.0`` (default) or ``beta``.\n            timeout: Timeout for each HTTP request, in seconds.\n            max_retries: How often a throttled request is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n        \"\"\"\n        super().__init__(\n            auth,\n            base_url=f\"{GRAPH_RESOURCE}/{api_version}\",\n            client_id=client_id,\n            scopes=scopes,\n            timeout=timeout,\n            max_retries=max_retries,\n            max_retry_wait=max_retry_wait,\n            transport=transport,\n        )\n\n    def get(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a GET request.\n\n        Args:\n            path: Path such as ``/users``, or an absolute Graph URL.\n            params: Query parameters such as ``{\"$top\": 5}``.\n            headers: Extra request headers such as ``ConsistencyLevel``.\n\n        Returns:\n            The parsed JSON body.\n        \"\"\"\n        return self._json(self.request(\"GET\", path, params=params, headers=headers))\n\n    def post(\n        self,\n        path: str,\n        json: Any = None,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a POST request.\n\n        Args:\n            path: Path such as ``/users``.\n            json: Request body.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        response = self.request(\"POST\", path, params=params, json=json, headers=headers)\n        return self._json(response)\n\n    def patch(\n        self,\n        path: str,\n        json: Any = None,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a PATCH request.\n\n        Args:\n            path: Path such as ``/users/{id}``.\n            json: Request body.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        response = self.request(\"PATCH\", path, params=params, json=json, headers=headers)\n        return self._json(response)\n\n    def put(\n        self,\n        path: str,\n        json: Any = None,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a PUT request.\n\n        Args:\n            path: Path of the resource.\n            json: Request body.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        response = self.request(\"PUT\", path, params=params, json=json, headers=headers)\n        return self._json(response)\n\n    def delete(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Any:\n        \"\"\"Send a DELETE request.\n\n        Args:\n            path: Path of the resource.\n            params: Query parameters.\n            headers: Extra request headers.\n\n        Returns:\n            The parsed JSON body, or ``None`` when the response has none.\n        \"\"\"\n        return self._json(self.request(\"DELETE\", path, params=params, headers=headers))\n\n    def iter_pages(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; Iterator[dict[str, Any]]:\n        \"\"\"Yield every page of a collection, following ``@odata.nextLink``.\n\n        Args:\n            path: Path of the collection.\n            params: Query parameters for the first page; next links carry their own.\n            headers: Extra request headers, sent with every page.\n\n        Yields:\n            Each page as returned by Graph.\n        \"\"\"\n        url: str | None = path\n        while url:\n            page = self.get(url, params=params, headers=headers)\n            yield page\n            url, params = page.get(\"@odata.nextLink\"), None\n\n    def get_all(\n        self,\n        path: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; list[Any]:\n        \"\"\"Return every item of a collection, following ``@odata.nextLink``.\n\n        Args:\n            path: Path of the collection.\n            params: Query parameters for the first page.\n            headers: Extra request headers, sent with every page.\n\n        Returns:\n            The items of all pages, in order.\n        \"\"\"\n        items: list[Any] = []\n        for page in self.iter_pages(path, params=params, headers=headers):\n            items.extend(page.get(\"value\", []))\n        return items\n\n    def batch(self, requests: Sequence[Mapping[str, Any]]) -&gt; list[dict[str, Any]]:\n        \"\"\"Send requests through ``$batch``, twenty per call.\n\n        Args:\n            requests: Batch request objects with ``method`` and ``url`` and optionally\n                ``id``, ``body`` and ``headers``. Missing ids are filled in. A request with\n                a body and no content type is sent as JSON.\n\n        Returns:\n            One response object per request, in the order of ``requests``. Failed requests\n            are returned with their error status, not raised.\n        \"\"\"\n        prepared: list[dict[str, Any]] = []\n        for index, request in enumerate(requests):\n            item = dict(request)\n            item[\"id\"] = str(item.get(\"id\", index))\n            if \"body\" in item and \"headers\" not in item:\n                item[\"headers\"] = {\"Content-Type\": \"application/json\"}\n            prepared.append(item)\n\n        responses: dict[str, dict[str, Any]] = {}\n        for start in range(0, len(prepared), BATCH_LIMIT):\n            chunk = prepared[start : start + BATCH_LIMIT]\n            result = self.post(\"/$batch\", {\"requests\": chunk})\n            responses.update({str(item[\"id\"]): item for item in result.get(\"responses\", [])})\n        return [responses[item[\"id\"]] for item in prepared]\n\n    def list_managed_tenant_ids(self) -&gt; list[str]:\n        \"\"\"List the tenants this home tenant manages through GDAP.\n\n        Call this on a client for the home tenant. It needs the delegated or application\n        permission ``DelegatedAdminRelationship.Read.All``.\n\n        Returns:\n            The managed tenant ids.\n        \"\"\"\n        managed = self.get_all(\"/tenantRelationships/delegatedAdminCustomers\")\n        return [str(tenant[\"tenantId\"]) for tenant in managed]\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.__init__","title":"<code>__init__(auth: AuthContext, *, client_id: str | None = None, scopes: Sequence[str] | None = None, api_version: Literal['v1.0', 'beta'] = 'v1.0', timeout: float = 100.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.BaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client; see :class:<code>ResourceClient</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Delegated scopes for a user flow, such as <code>[\"User.Read.All\"]</code>.</p> <code>None</code> <code>api_version</code> <code>Literal['v1.0', 'beta']</code> <p><code>v1.0</code> (default) or <code>beta</code>.</p> <code>'v1.0'</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds.</p> <code>100.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>BaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    api_version: Literal[\"v1.0\", \"beta\"] = \"v1.0\",\n    timeout: float = 100.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.BaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        client_id: Client id for this client; see :class:`ResourceClient`.\n        scopes: Delegated scopes for a user flow, such as ``[\"User.Read.All\"]``.\n        api_version: ``v1.0`` (default) or ``beta``.\n        timeout: Timeout for each HTTP request, in seconds.\n        max_retries: How often a throttled request is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n    \"\"\"\n    super().__init__(\n        auth,\n        base_url=f\"{GRAPH_RESOURCE}/{api_version}\",\n        client_id=client_id,\n        scopes=scopes,\n        timeout=timeout,\n        max_retries=max_retries,\n        max_retry_wait=max_retry_wait,\n        transport=transport,\n    )\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.batch","title":"<code>batch(requests: Sequence[Mapping[str, Any]]) -&gt; list[dict[str, Any]]</code>","text":"<p>Send requests through <code>$batch</code>, twenty per call.</p> <p>Parameters:</p> Name Type Description Default <code>requests</code> <code>Sequence[Mapping[str, Any]]</code> <p>Batch request objects with <code>method</code> and <code>url</code> and optionally <code>id</code>, <code>body</code> and <code>headers</code>. Missing ids are filled in. A request with a body and no content type is sent as JSON.</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>One response object per request, in the order of <code>requests</code>. Failed requests</p> <code>list[dict[str, Any]]</code> <p>are returned with their error status, not raised.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def batch(self, requests: Sequence[Mapping[str, Any]]) -&gt; list[dict[str, Any]]:\n    \"\"\"Send requests through ``$batch``, twenty per call.\n\n    Args:\n        requests: Batch request objects with ``method`` and ``url`` and optionally\n            ``id``, ``body`` and ``headers``. Missing ids are filled in. A request with\n            a body and no content type is sent as JSON.\n\n    Returns:\n        One response object per request, in the order of ``requests``. Failed requests\n        are returned with their error status, not raised.\n    \"\"\"\n    prepared: list[dict[str, Any]] = []\n    for index, request in enumerate(requests):\n        item = dict(request)\n        item[\"id\"] = str(item.get(\"id\", index))\n        if \"body\" in item and \"headers\" not in item:\n            item[\"headers\"] = {\"Content-Type\": \"application/json\"}\n        prepared.append(item)\n\n    responses: dict[str, dict[str, Any]] = {}\n    for start in range(0, len(prepared), BATCH_LIMIT):\n        chunk = prepared[start : start + BATCH_LIMIT]\n        result = self.post(\"/$batch\", {\"requests\": chunk})\n        responses.update({str(item[\"id\"]): item for item in result.get(\"responses\", [])})\n    return [responses[item[\"id\"]] for item in prepared]\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.delete","title":"<code>delete(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>","text":"<p>Send a DELETE request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the resource.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def delete(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a DELETE request.\n\n    Args:\n        path: Path of the resource.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    return self._json(self.request(\"DELETE\", path, params=params, headers=headers))\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.get","title":"<code>get(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>","text":"<p>Send a GET request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path such as <code>/users</code>, or an absolute Graph URL.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters such as <code>{\"$top\": 5}</code>.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers such as <code>ConsistencyLevel</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def get(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a GET request.\n\n    Args:\n        path: Path such as ``/users``, or an absolute Graph URL.\n        params: Query parameters such as ``{\"$top\": 5}``.\n        headers: Extra request headers such as ``ConsistencyLevel``.\n\n    Returns:\n        The parsed JSON body.\n    \"\"\"\n    return self._json(self.request(\"GET\", path, params=params, headers=headers))\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.get_all","title":"<code>get_all(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; list[Any]</code>","text":"<p>Return every item of a collection, following <code>@odata.nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the collection.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters for the first page.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers, sent with every page.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Any]</code> <p>The items of all pages, in order.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def get_all(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; list[Any]:\n    \"\"\"Return every item of a collection, following ``@odata.nextLink``.\n\n    Args:\n        path: Path of the collection.\n        params: Query parameters for the first page.\n        headers: Extra request headers, sent with every page.\n\n    Returns:\n        The items of all pages, in order.\n    \"\"\"\n    items: list[Any] = []\n    for page in self.iter_pages(path, params=params, headers=headers):\n        items.extend(page.get(\"value\", []))\n    return items\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.iter_pages","title":"<code>iter_pages(path: str, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Iterator[dict[str, Any]]</code>","text":"<p>Yield every page of a collection, following <code>@odata.nextLink</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the collection.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters for the first page; next links carry their own.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers, sent with every page.</p> <code>None</code> <p>Yields:</p> Type Description <code>dict[str, Any]</code> <p>Each page as returned by Graph.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def iter_pages(\n    self,\n    path: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Iterator[dict[str, Any]]:\n    \"\"\"Yield every page of a collection, following ``@odata.nextLink``.\n\n    Args:\n        path: Path of the collection.\n        params: Query parameters for the first page; next links carry their own.\n        headers: Extra request headers, sent with every page.\n\n    Yields:\n        Each page as returned by Graph.\n    \"\"\"\n    url: str | None = path\n    while url:\n        page = self.get(url, params=params, headers=headers)\n        yield page\n        url, params = page.get(\"@odata.nextLink\"), None\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.list_managed_tenant_ids","title":"<code>list_managed_tenant_ids() -&gt; list[str]</code>","text":"<p>List the tenants this home tenant manages through GDAP.</p> <p>Call this on a client for the home tenant. It needs the delegated or application permission <code>DelegatedAdminRelationship.Read.All</code>.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>The managed tenant ids.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def list_managed_tenant_ids(self) -&gt; list[str]:\n    \"\"\"List the tenants this home tenant manages through GDAP.\n\n    Call this on a client for the home tenant. It needs the delegated or application\n    permission ``DelegatedAdminRelationship.Read.All``.\n\n    Returns:\n        The managed tenant ids.\n    \"\"\"\n    managed = self.get_all(\"/tenantRelationships/delegatedAdminCustomers\")\n    return [str(tenant[\"tenantId\"]) for tenant in managed]\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.patch","title":"<code>patch(path: str, json: Any = None, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>","text":"<p>Send a PATCH request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path such as <code>/users/{id}</code>.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def patch(\n    self,\n    path: str,\n    json: Any = None,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a PATCH request.\n\n    Args:\n        path: Path such as ``/users/{id}``.\n        json: Request body.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    response = self.request(\"PATCH\", path, params=params, json=json, headers=headers)\n    return self._json(response)\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.post","title":"<code>post(path: str, json: Any = None, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>","text":"<p>Send a POST request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path such as <code>/users</code>.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def post(\n    self,\n    path: str,\n    json: Any = None,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a POST request.\n\n    Args:\n        path: Path such as ``/users``.\n        json: Request body.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    response = self.request(\"POST\", path, params=params, json=json, headers=headers)\n    return self._json(response)\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.GraphClient.put","title":"<code>put(path: str, json: Any = None, *, params: Mapping[str, Any] | None = None, headers: Mapping[str, str] | None = None) -&gt; Any</code>","text":"<p>Send a PUT request.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path of the resource.</p> required <code>json</code> <code>Any</code> <p>Request body.</p> <code>None</code> <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The parsed JSON body, or <code>None</code> when the response has none.</p> Source code in <code>src/azure_auth/_sync/graph.py</code> <pre><code>def put(\n    self,\n    path: str,\n    json: Any = None,\n    *,\n    params: Mapping[str, Any] | None = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; Any:\n    \"\"\"Send a PUT request.\n\n    Args:\n        path: Path of the resource.\n        json: Request body.\n        params: Query parameters.\n        headers: Extra request headers.\n\n    Returns:\n        The parsed JSON body, or ``None`` when the response has none.\n    \"\"\"\n    response = self.request(\"PUT\", path, params=params, json=json, headers=headers)\n    return self._json(response)\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.InvokeCommandClient","title":"<code>InvokeCommandClient</code>","text":"<p>               Bases: <code>ResourceClient</code></p> <p>Base class for clients that run cmdlets through <code>InvokeCommand</code>.</p> <p>Attributes:</p> Name Type Description <code>HOST</code> <code>str</code> <p>Host name of the service.</p> <code>last_warnings</code> <code>list[str]</code> <p>Warnings the service returned for the most recent cmdlet.</p> Source code in <code>src/azure_auth/_sync/invoke_command.py</code> <pre><code>class InvokeCommandClient(ResourceClient):\n    \"\"\"Base class for clients that run cmdlets through ``InvokeCommand``.\n\n    Attributes:\n        HOST: Host name of the service.\n        last_warnings: Warnings the service returned for the most recent cmdlet.\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str] = EXCHANGE_POWERSHELL_CLIENT_ID\n    ERROR_CLASS: ClassVar[type[ResourceError]] = InvokeCommandError\n    HOST: ClassVar[str]\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        anchor_mailbox: str | None = None,\n        api_version: Literal[\"beta\", \"v1.0\"] = \"beta\",\n        page_size: int = 1000,\n        timeout: float = 300.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.BaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            client_id: Client id for this client; see :class:`ResourceClient`.\n            scopes: Scopes to request. Defaults to ``https://&lt;host&gt;/.default``.\n            anchor_mailbox: Value of the ``X-AnchorMailbox`` routing header. Defaults to the\n                signed-in user for a user flow in its own tenant, and to the tenant's system\n                mailbox for app flows and for sibling (GDAP) contexts.\n            api_version: ``beta`` (what the PowerShell module uses) or ``v1.0``.\n            page_size: Preferred number of results per page.\n            timeout: Timeout for each HTTP request, in seconds. Cmdlets can be slow.\n            max_retries: How often a throttled request is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n        \"\"\"\n        super().__init__(\n            auth,\n            base_url=f\"https://{self.HOST}\",\n            client_id=client_id,\n            scopes=scopes,\n            timeout=timeout,\n            max_retries=max_retries,\n            max_retry_wait=max_retry_wait,\n            transport=transport,\n        )\n        self._anchor_mailbox = anchor_mailbox\n        self._api_version = api_version\n        self._page_size = page_size\n        self._connection_id = str(uuid.uuid4())\n        self._tenant_guid: str | None = None\n        self.last_warnings: list[str] = []\n\n    def _is_trusted_host(self, host: str) -&gt; bool:\n        \"\"\"Accept the service host and its regional sub-hosts.\n\n        Args:\n            host: Host name of the request URL.\n\n        Returns:\n            ``True`` for ``HOST`` and for ``&lt;region&gt;.HOST``.\n        \"\"\"\n        lowered = host.lower()\n        return lowered == self.HOST or lowered.endswith(f\".{self.HOST}\")\n\n    def _tenant(self) -&gt; str:\n        \"\"\"Return the tenant GUID for the request path.\n\n        The service wants the GUID, and the context may have been given a domain name, so\n        the GUID is read from the ``tid`` claim of the access token.\n\n        Returns:\n            The tenant GUID, or the context's tenant id when the token cannot be decoded.\n        \"\"\"\n        if self._tenant_guid is None:\n            token = self._tokens.acquire_token(self._scopes, client_id=self._client_id)\n            self._tenant_guid = tenant_id_from_token(token.token) or self._auth.tenant_id\n        return self._tenant_guid\n\n    def _anchor(self, tenant: str) -&gt; str:\n        \"\"\"Build the ``X-AnchorMailbox`` routing header.\n\n        Args:\n            tenant: Tenant GUID.\n\n        Returns:\n            The configured anchor, else the user's mailbox for a user flow in its own tenant,\n            else the tenant's system mailbox.\n        \"\"\"\n        if self._anchor_mailbox:\n            return self._anchor_mailbox\n        if self._auth.is_app_flow or self._auth.is_sibling:\n            return f\"APP:{SYSTEM_MAILBOX}@{tenant}\"\n        return f\"UPN:{self._auth.username}\"\n\n    def _post(self, url: str, payload: dict[str, Any], cmdlet: str) -&gt; httpx.Response:\n        \"\"\"Send one ``InvokeCommand`` request, following a regional redirect by hand.\n\n        ``httpx`` drops the ``Authorization`` header when a redirect changes the host, and\n        Security &amp; Compliance answers the first call with a redirect to a regional host. So\n        the redirect is read here, the regional host becomes the new base URL, and the\n        request is sent again with its token.\n\n        Args:\n            url: Request URL or path.\n            payload: The request body.\n            cmdlet: Name of the cmdlet, for the diagnostic header.\n\n        Returns:\n            The successful response.\n        \"\"\"\n        tenant = self._tenant()\n        headers = {\n            \"Accept\": \"application/json\",\n            \"Content-Type\": \"application/json;odata.metadata=minimal\",\n            \"Prefer\": f\"odata.maxpagesize={self._page_size}\",\n            \"X-AnchorMailbox\": self._anchor(tenant),\n            \"X-CmdletName\": cmdlet,\n            \"X-ResponseFormat\": \"json\",\n            \"connection-id\": self._connection_id,\n            \"client-request-id\": str(uuid.uuid4()),\n        }\n        response = self.request(\"POST\", url, json=payload, headers=headers)\n        location = response.headers.get(\"Location\")\n        if response.status_code == httpx.codes.FOUND and location:\n            region = httpx.URL(location).host.split(\".\", 1)[0]\n            if region and not self._base_url.host.startswith(f\"{region}.\"):\n                self._base_url = httpx.URL(f\"https://{region}.{self.HOST}/\")\n                target = httpx.URL(url) if \"://\" in url else None\n                retry_url = str(target.copy_with(host=self._base_url.host)) if target else url\n                response = self.request(\"POST\", retry_url, json=payload, headers=headers)\n        if response.is_redirect:\n            # A redirect that could not be followed must not look like an empty result.\n            raise self._error(response)\n        return response\n\n    def iter_pages(self, cmdlet: str, **parameters: Any) -&gt; Iterator[dict[str, Any]]:\n        \"\"\"Run a cmdlet and yield each page of its output.\n\n        Args:\n            cmdlet: Cmdlet name such as ``Get-Mailbox``.\n            **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n        Yields:\n            Each response page; the results are under ``value``.\n        \"\"\"\n        payload = {\"CmdletInput\": {\"CmdletName\": cmdlet, \"Parameters\": parameters}}\n        tenant = self._tenant()\n        url: str | None = f\"/adminapi/{self._api_version}/{tenant}/InvokeCommand\"\n        self.last_warnings = []\n        while url:\n            response = self._post(url, payload, cmdlet)\n            warnings = response.headers.get(\"X-Warnings\")\n            if warnings:\n                self.last_warnings.append(warnings)\n            page = self._json(response) or {}\n            yield page\n            url = page.get(\"@odata.nextLink\")\n\n    def run(self, cmdlet: str, **parameters: Any) -&gt; list[dict[str, Any]]:\n        \"\"\"Run a cmdlet and return all of its output.\n\n        Args:\n            cmdlet: Cmdlet name such as ``Get-Mailbox``.\n            **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n        Returns:\n            Every output object, as dictionaries. There is no server-side limit: a cmdlet\n            with a large result set is paged through in full. ``ResultSize`` does not change\n            this, because the PowerShell module applies it client side rather than sending\n            it, so ``InvokeCommand`` ignores it. To stop early, use :meth:`iter_pages`.\n\n        Raises:\n            InvokeCommandError: If the service rejects the cmdlet.\n        \"\"\"\n        results: list[dict[str, Any]] = []\n        for page in self.iter_pages(cmdlet, **parameters):\n            results.extend(page.get(\"value\", []))\n        return results\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.InvokeCommandClient.__init__","title":"<code>__init__(auth: AuthContext, *, client_id: str | None = None, scopes: Sequence[str] | None = None, anchor_mailbox: str | None = None, api_version: Literal['beta', 'v1.0'] = 'beta', page_size: int = 1000, timeout: float = 300.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.BaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client; see :class:<code>ResourceClient</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Scopes to request. Defaults to <code>https://&lt;host&gt;/.default</code>.</p> <code>None</code> <code>anchor_mailbox</code> <code>str | None</code> <p>Value of the <code>X-AnchorMailbox</code> routing header. Defaults to the signed-in user for a user flow in its own tenant, and to the tenant's system mailbox for app flows and for sibling (GDAP) contexts.</p> <code>None</code> <code>api_version</code> <code>Literal['beta', 'v1.0']</code> <p><code>beta</code> (what the PowerShell module uses) or <code>v1.0</code>.</p> <code>'beta'</code> <code>page_size</code> <code>int</code> <p>Preferred number of results per page.</p> <code>1000</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds. Cmdlets can be slow.</p> <code>300.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>BaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> Source code in <code>src/azure_auth/_sync/invoke_command.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    anchor_mailbox: str | None = None,\n    api_version: Literal[\"beta\", \"v1.0\"] = \"beta\",\n    page_size: int = 1000,\n    timeout: float = 300.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.BaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        client_id: Client id for this client; see :class:`ResourceClient`.\n        scopes: Scopes to request. Defaults to ``https://&lt;host&gt;/.default``.\n        anchor_mailbox: Value of the ``X-AnchorMailbox`` routing header. Defaults to the\n            signed-in user for a user flow in its own tenant, and to the tenant's system\n            mailbox for app flows and for sibling (GDAP) contexts.\n        api_version: ``beta`` (what the PowerShell module uses) or ``v1.0``.\n        page_size: Preferred number of results per page.\n        timeout: Timeout for each HTTP request, in seconds. Cmdlets can be slow.\n        max_retries: How often a throttled request is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n    \"\"\"\n    super().__init__(\n        auth,\n        base_url=f\"https://{self.HOST}\",\n        client_id=client_id,\n        scopes=scopes,\n        timeout=timeout,\n        max_retries=max_retries,\n        max_retry_wait=max_retry_wait,\n        transport=transport,\n    )\n    self._anchor_mailbox = anchor_mailbox\n    self._api_version = api_version\n    self._page_size = page_size\n    self._connection_id = str(uuid.uuid4())\n    self._tenant_guid: str | None = None\n    self.last_warnings: list[str] = []\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.InvokeCommandClient.iter_pages","title":"<code>iter_pages(cmdlet: str, **parameters: Any) -&gt; Iterator[dict[str, Any]]</code>","text":"<p>Run a cmdlet and yield each page of its output.</p> <p>Parameters:</p> Name Type Description Default <code>cmdlet</code> <code>str</code> <p>Cmdlet name such as <code>Get-Mailbox</code>.</p> required <code>**parameters</code> <code>Any</code> <p>Cmdlet parameters. Use <code>True</code> for switch parameters.</p> <code>{}</code> <p>Yields:</p> Type Description <code>dict[str, Any]</code> <p>Each response page; the results are under <code>value</code>.</p> Source code in <code>src/azure_auth/_sync/invoke_command.py</code> <pre><code>def iter_pages(self, cmdlet: str, **parameters: Any) -&gt; Iterator[dict[str, Any]]:\n    \"\"\"Run a cmdlet and yield each page of its output.\n\n    Args:\n        cmdlet: Cmdlet name such as ``Get-Mailbox``.\n        **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n    Yields:\n        Each response page; the results are under ``value``.\n    \"\"\"\n    payload = {\"CmdletInput\": {\"CmdletName\": cmdlet, \"Parameters\": parameters}}\n    tenant = self._tenant()\n    url: str | None = f\"/adminapi/{self._api_version}/{tenant}/InvokeCommand\"\n    self.last_warnings = []\n    while url:\n        response = self._post(url, payload, cmdlet)\n        warnings = response.headers.get(\"X-Warnings\")\n        if warnings:\n            self.last_warnings.append(warnings)\n        page = self._json(response) or {}\n        yield page\n        url = page.get(\"@odata.nextLink\")\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.InvokeCommandClient.run","title":"<code>run(cmdlet: str, **parameters: Any) -&gt; list[dict[str, Any]]</code>","text":"<p>Run a cmdlet and return all of its output.</p> <p>Parameters:</p> Name Type Description Default <code>cmdlet</code> <code>str</code> <p>Cmdlet name such as <code>Get-Mailbox</code>.</p> required <code>**parameters</code> <code>Any</code> <p>Cmdlet parameters. Use <code>True</code> for switch parameters.</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>Every output object, as dictionaries. There is no server-side limit: a cmdlet</p> <code>list[dict[str, Any]]</code> <p>with a large result set is paged through in full. <code>ResultSize</code> does not change</p> <code>list[dict[str, Any]]</code> <p>this, because the PowerShell module applies it client side rather than sending</p> <code>list[dict[str, Any]]</code> <p>it, so <code>InvokeCommand</code> ignores it. To stop early, use :meth:<code>iter_pages</code>.</p> <p>Raises:</p> Type Description <code>InvokeCommandError</code> <p>If the service rejects the cmdlet.</p> Source code in <code>src/azure_auth/_sync/invoke_command.py</code> <pre><code>def run(self, cmdlet: str, **parameters: Any) -&gt; list[dict[str, Any]]:\n    \"\"\"Run a cmdlet and return all of its output.\n\n    Args:\n        cmdlet: Cmdlet name such as ``Get-Mailbox``.\n        **parameters: Cmdlet parameters. Use ``True`` for switch parameters.\n\n    Returns:\n        Every output object, as dictionaries. There is no server-side limit: a cmdlet\n        with a large result set is paged through in full. ``ResultSize`` does not change\n        this, because the PowerShell module applies it client side rather than sending\n        it, so ``InvokeCommand`` ignores it. To stop early, use :meth:`iter_pages`.\n\n    Raises:\n        InvokeCommandError: If the service rejects the cmdlet.\n    \"\"\"\n    results: list[dict[str, Any]] = []\n    for page in self.iter_pages(cmdlet, **parameters):\n        results.extend(page.get(\"value\", []))\n    return results\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.IppsClient","title":"<code>IppsClient</code>","text":"<p>               Bases: <code>InvokeCommandClient</code></p> <p>Run Security &amp; Compliance cmdlets without PowerShell.</p> <p>The service redirects the first call to a regional host. The client follows that redirect itself and keeps using the regional host, while tokens stay scoped to the global host.</p> Example <p>ipps = IppsClient(auth)  # doctest: +SKIP labels = ipps.run(\"Get-Label\")  # doctest: +SKIP</p> Source code in <code>src/azure_auth/_sync/ipps.py</code> <pre><code>class IppsClient(InvokeCommandClient):\n    \"\"\"Run Security &amp; Compliance cmdlets without PowerShell.\n\n    The service redirects the first call to a regional host. The client follows that\n    redirect itself and keeps using the regional host, while tokens stay scoped to the\n    global host.\n\n    Example:\n        &gt;&gt;&gt; ipps = IppsClient(auth)  # doctest: +SKIP\n        &gt;&gt;&gt; labels = ipps.run(\"Get-Label\")  # doctest: +SKIP\n    \"\"\"\n\n    RESOURCE: ClassVar[str] = IPPS_RESOURCE\n    HOST: ClassVar[str] = \"ps.compliance.protection.outlook.com\"\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient","title":"<code>ResourceClient</code>","text":"<p>Base class: an authenticated HTTP client for one resource.</p> <p>Subclasses set the class attributes and add resource-specific verbs.</p> <p>Attributes:</p> Name Type Description <code>DEFAULT_CLIENT_ID</code> <code>str</code> <p>First-party client id used for user flows when neither the client nor the context names one.</p> <code>RESOURCE</code> <code>str</code> <p>Resource identifier; <code>&lt;RESOURCE&gt;/.default</code> is the default scope.</p> <code>ERROR_CLASS</code> <code>type[ResourceError]</code> <p>Exception type raised for error responses.</p> Source code in <code>src/azure_auth/_sync/base.py</code> <pre><code>class ResourceClient:\n    \"\"\"Base class: an authenticated HTTP client for one resource.\n\n    Subclasses set the class attributes and add resource-specific verbs.\n\n    Attributes:\n        DEFAULT_CLIENT_ID: First-party client id used for user flows when neither the client\n            nor the context names one.\n        RESOURCE: Resource identifier; ``&lt;RESOURCE&gt;/.default`` is the default scope.\n        ERROR_CLASS: Exception type raised for error responses.\n    \"\"\"\n\n    DEFAULT_CLIENT_ID: ClassVar[str]\n    RESOURCE: ClassVar[str]\n    ERROR_CLASS: ClassVar[type[ResourceError]] = ResourceError\n\n    def __init__(\n        self,\n        auth: AuthContext,\n        *,\n        base_url: str,\n        client_id: str | None = None,\n        scopes: Sequence[str] | None = None,\n        timeout: float = 100.0,\n        max_retries: int = 3,\n        max_retry_wait: float = 120.0,\n        transport: httpx.BaseTransport | None = None,\n    ) -&gt; None:\n        \"\"\"Create the client. No network call is made.\n\n        Args:\n            auth: The authentication context to get tokens from.\n            base_url: Base URL that relative request paths are joined to.\n            client_id: Client id for this client. Defaults to the context's client id, then\n                (user flows only) to ``DEFAULT_CLIENT_ID``.\n            scopes: Delegated scopes to request in a user flow; short names are qualified\n                with the resource. Defaults to ``&lt;RESOURCE&gt;/.default``. App flows always use\n                ``.default``.\n            timeout: Timeout for each HTTP request, in seconds.\n            max_retries: How often a throttled request (429, 503) is retried.\n            max_retry_wait: Longest wait before one retry, in seconds.\n            transport: Custom ``httpx`` transport, mainly for tests.\n\n        Raises:\n            ValueError: If an app flow has no client id, or is given delegated scopes.\n        \"\"\"\n        self._auth = auth\n        self._tokens = auth\n        self._client_id = self._resolve_client_id(client_id)\n        self._scopes = self._resolve_scopes(scopes)\n        self._base_url = httpx.URL(base_url.rstrip(\"/\") + \"/\")\n        self._max_retries = max_retries\n        self._max_retry_wait = max_retry_wait\n        self._http = httpx.Client(timeout=timeout, transport=transport)\n\n    # ------------------------------------------------------------------ configuration\n\n    @property\n    def auth(self) -&gt; AuthContext:\n        \"\"\"The authentication context behind this client.\"\"\"\n        return self._auth\n\n    @property\n    def client_id(self) -&gt; str:\n        \"\"\"The client id this client requests tokens with.\"\"\"\n        return self._client_id\n\n    @property\n    def scopes(self) -&gt; tuple[str, ...]:\n        \"\"\"The scopes this client requests.\"\"\"\n        return tuple(self._scopes)\n\n    @property\n    def base_url(self) -&gt; str:\n        \"\"\"The base URL that relative request paths are joined to.\"\"\"\n        return str(self._base_url).rstrip(\"/\")\n\n    def _resolve_client_id(self, client_id: str | None) -&gt; str:\n        \"\"\"Apply the client id precedence rule.\n\n        Args:\n            client_id: Client id given to this client, if any.\n\n        Returns:\n            The client's id, else the context's, else (user flows) the resource default.\n        \"\"\"\n        resolved = client_id or self._auth.client_id\n        if resolved:\n            return resolved\n        if self._auth.is_app_flow:\n            raise ValueError(\n                \"App flows need a client_id on the AuthContext or on the client; \"\n                \"first-party client ids cannot be used with a secret or certificate\"\n            )\n        return self.DEFAULT_CLIENT_ID\n\n    def _resolve_scopes(self, scopes: Sequence[str] | None) -&gt; list[str]:\n        \"\"\"Work out which scopes to request.\n\n        Args:\n            scopes: Scopes given to this client, if any.\n\n        Returns:\n            Fully qualified scopes.\n        \"\"\"\n        default = [f\"{self.RESOURCE}/.default\"]\n        # MSAL adds the OpenID Connect scopes itself and rejects them when they are passed in.\n        wanted = [scope for scope in scopes or () if scope not in _RESERVED_SCOPES]\n        if not wanted:\n            return default\n        qualified = [\n            scope if \"://\" in scope or scope == \"email\" else f\"{self.RESOURCE}/{scope}\"\n            for scope in wanted\n        ]\n        if self._auth.is_app_flow and qualified != default:\n            raise ValueError(f\"App flows always request {default[0]}; do not pass scopes\")\n        return qualified\n\n    # ------------------------------------------------------------------ lifecycle\n\n    def login(self, *, force: bool = False) -&gt; None:\n        \"\"\"Sign in now instead of at the first request.\n\n        Args:\n            force: Prompt even when a cached token or account exists.\n        \"\"\"\n        self._tokens.login(client_id=self._client_id, scopes=self._scopes, force=force)\n\n    def close(self) -&gt; None:\n        \"\"\"Close the underlying HTTP connections.\"\"\"\n        self._http.close()\n\n    def __enter__(self) -&gt; Self:\n        \"\"\"Enter the context manager.\n\n        Returns:\n            This client.\n        \"\"\"\n        return self\n\n    def __exit__(\n        self,\n        exc_type: type[BaseException] | None = None,\n        exc_value: BaseException | None = None,\n        traceback: TracebackType | None = None,\n    ) -&gt; None:\n        \"\"\"Close the client when leaving the context manager.\"\"\"\n        self.close()\n\n    # ------------------------------------------------------------------ requests\n\n    def _url(self, url: str) -&gt; httpx.URL:\n        \"\"\"Resolve a request URL and make sure the token may be sent to it.\n\n        Args:\n            url: A path relative to the base URL, or an absolute URL such as a next link.\n\n        Returns:\n            The absolute URL.\n\n        Raises:\n            ValueError: If the URL points at a host this client does not trust.\n        \"\"\"\n        target = self._base_url.join(url.lstrip(\"/\") if \"://\" not in url else url)\n        trusted = target.scheme == \"https\" and self._is_trusted_host(target.host)\n        # Credentials in the URL would make httpx replace the bearer token with basic auth.\n        if not trusted or target.userinfo:\n            raise ValueError(f\"Refusing to send a token for {self.RESOURCE} to {target}\")\n        return target\n\n    def _is_trusted_host(self, host: str) -&gt; bool:\n        \"\"\"Decide whether a host may receive this client's token.\n\n        Args:\n            host: Host name of the request URL.\n\n        Returns:\n            ``True`` when the host is the base URL's host.\n        \"\"\"\n        return host.lower() == self._base_url.host.lower()\n\n    def request(\n        self,\n        method: str,\n        url: str,\n        *,\n        params: Mapping[str, Any] | None = None,\n        json: Any = None,\n        headers: Mapping[str, str] | None = None,\n    ) -&gt; httpx.Response:\n        \"\"\"Send an authenticated request and return the successful response.\n\n        Throttled requests (429, 503) are retried, honouring ``Retry-After``. A 401 is\n        retried once: with the claims from the challenge when the resource sent one, else\n        with a freshly acquired token.\n\n        Args:\n            method: HTTP method.\n            url: Path relative to the base URL, or an absolute URL on the same host.\n            params: Query parameters.\n            json: Request body, serialised as JSON.\n            headers: Extra request headers.\n\n        Returns:\n            The response, whose status is below 400.\n\n        Raises:\n            ResourceError: (the subclass in ``ERROR_CLASS``) for an error response.\n        \"\"\"\n        target = self._url(url)\n        retries = 0\n        challenged = False\n        claims: str | None = None\n        force_refresh = False\n        while True:\n            token = self._tokens.acquire_token(\n                self._scopes, client_id=self._client_id, claims=claims, force_refresh=force_refresh\n            )\n            claims, force_refresh = None, False\n            response = self._http.request(\n                method,\n                target,\n                params=params,\n                json=json,\n                headers={**(headers or {}), \"Authorization\": f\"Bearer {token.token}\"},\n            )\n            if response.status_code == httpx.codes.UNAUTHORIZED and not challenged:\n                challenged = True\n                claims = claims_from_challenge(response.headers.get(\"WWW-Authenticate\"))\n                force_refresh = claims is None\n                continue\n            if response.status_code in _RETRY_STATUSES and retries &lt; self._max_retries:\n                retries += 1\n                time.sleep(retry_delay(response, retries, self._max_retry_wait))\n                continue\n            if response.status_code &gt;= httpx.codes.BAD_REQUEST:\n                raise self._error(response)\n            return response\n\n    def _error(self, response: httpx.Response) -&gt; ResourceError:\n        \"\"\"Build the exception for an error response.\n\n        Args:\n            response: The error response.\n\n        Returns:\n            An instance of ``ERROR_CLASS``.\n        \"\"\"\n        body: Any\n        try:\n            body = response.json()\n        except ValueError:\n            body = response.text\n        error = body.get(\"error\") if isinstance(body, dict) else None\n        code = message = None\n        if isinstance(error, dict):\n            code, message = error.get(\"code\"), error.get(\"message\")\n        elif isinstance(error, str):\n            code, message = error, body.get(\"error_description\")\n        request_id = next(\n            (response.headers[name] for name in _REQUEST_ID_HEADERS if name in response.headers),\n            None,\n        )\n        summary = f\"{response.request.method} {response.request.url} -&gt; {response.status_code}\"\n        detail = \": \".join(str(part) for part in (code, message) if part)\n        return self.ERROR_CLASS(\n            f\"{summary} {detail}\".rstrip(),\n            status=response.status_code,\n            code=str(code) if code else None,\n            request_id=request_id,\n            body=body,\n        )\n\n    @staticmethod\n    def _json(response: httpx.Response) -&gt; Any:\n        \"\"\"Decode a response body.\n\n        Args:\n            response: A successful response.\n\n        Returns:\n            The parsed JSON, or ``None`` when the response has no body.\n        \"\"\"\n        return response.json() if response.content else None\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.auth","title":"<code>auth: AuthContext</code>  <code>property</code>","text":"<p>The authentication context behind this client.</p>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.base_url","title":"<code>base_url: str</code>  <code>property</code>","text":"<p>The base URL that relative request paths are joined to.</p>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.client_id","title":"<code>client_id: str</code>  <code>property</code>","text":"<p>The client id this client requests tokens with.</p>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.scopes","title":"<code>scopes: tuple[str, ...]</code>  <code>property</code>","text":"<p>The scopes this client requests.</p>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.__enter__","title":"<code>__enter__() -&gt; Self</code>","text":"<p>Enter the context manager.</p> <p>Returns:</p> Type Description <code>Self</code> <p>This client.</p> Source code in <code>src/azure_auth/_sync/base.py</code> <pre><code>def __enter__(self) -&gt; Self:\n    \"\"\"Enter the context manager.\n\n    Returns:\n        This client.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.__exit__","title":"<code>__exit__(exc_type: type[BaseException] | None = None, exc_value: BaseException | None = None, traceback: TracebackType | None = None) -&gt; None</code>","text":"<p>Close the client when leaving the context manager.</p> Source code in <code>src/azure_auth/_sync/base.py</code> <pre><code>def __exit__(\n    self,\n    exc_type: type[BaseException] | None = None,\n    exc_value: BaseException | None = None,\n    traceback: TracebackType | None = None,\n) -&gt; None:\n    \"\"\"Close the client when leaving the context manager.\"\"\"\n    self.close()\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.__init__","title":"<code>__init__(auth: AuthContext, *, base_url: str, client_id: str | None = None, scopes: Sequence[str] | None = None, timeout: float = 100.0, max_retries: int = 3, max_retry_wait: float = 120.0, transport: httpx.BaseTransport | None = None) -&gt; None</code>","text":"<p>Create the client. No network call is made.</p> <p>Parameters:</p> Name Type Description Default <code>auth</code> <code>AuthContext</code> <p>The authentication context to get tokens from.</p> required <code>base_url</code> <code>str</code> <p>Base URL that relative request paths are joined to.</p> required <code>client_id</code> <code>str | None</code> <p>Client id for this client. Defaults to the context's client id, then (user flows only) to <code>DEFAULT_CLIENT_ID</code>.</p> <code>None</code> <code>scopes</code> <code>Sequence[str] | None</code> <p>Delegated scopes to request in a user flow; short names are qualified with the resource. Defaults to <code>&lt;RESOURCE&gt;/.default</code>. App flows always use <code>.default</code>.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Timeout for each HTTP request, in seconds.</p> <code>100.0</code> <code>max_retries</code> <code>int</code> <p>How often a throttled request (429, 503) is retried.</p> <code>3</code> <code>max_retry_wait</code> <code>float</code> <p>Longest wait before one retry, in seconds.</p> <code>120.0</code> <code>transport</code> <code>BaseTransport | None</code> <p>Custom <code>httpx</code> transport, mainly for tests.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an app flow has no client id, or is given delegated scopes.</p> Source code in <code>src/azure_auth/_sync/base.py</code> <pre><code>def __init__(\n    self,\n    auth: AuthContext,\n    *,\n    base_url: str,\n    client_id: str | None = None,\n    scopes: Sequence[str] | None = None,\n    timeout: float = 100.0,\n    max_retries: int = 3,\n    max_retry_wait: float = 120.0,\n    transport: httpx.BaseTransport | None = None,\n) -&gt; None:\n    \"\"\"Create the client. No network call is made.\n\n    Args:\n        auth: The authentication context to get tokens from.\n        base_url: Base URL that relative request paths are joined to.\n        client_id: Client id for this client. Defaults to the context's client id, then\n            (user flows only) to ``DEFAULT_CLIENT_ID``.\n        scopes: Delegated scopes to request in a user flow; short names are qualified\n            with the resource. Defaults to ``&lt;RESOURCE&gt;/.default``. App flows always use\n            ``.default``.\n        timeout: Timeout for each HTTP request, in seconds.\n        max_retries: How often a throttled request (429, 503) is retried.\n        max_retry_wait: Longest wait before one retry, in seconds.\n        transport: Custom ``httpx`` transport, mainly for tests.\n\n    Raises:\n        ValueError: If an app flow has no client id, or is given delegated scopes.\n    \"\"\"\n    self._auth = auth\n    self._tokens = auth\n    self._client_id = self._resolve_client_id(client_id)\n    self._scopes = self._resolve_scopes(scopes)\n    self._base_url = httpx.URL(base_url.rstrip(\"/\") + \"/\")\n    self._max_retries = max_retries\n    self._max_retry_wait = max_retry_wait\n    self._http = httpx.Client(timeout=timeout, transport=transport)\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.close","title":"<code>close() -&gt; None</code>","text":"<p>Close the underlying HTTP connections.</p> Source code in <code>src/azure_auth/_sync/base.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying HTTP connections.\"\"\"\n    self._http.close()\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.login","title":"<code>login(*, force: bool = False) -&gt; None</code>","text":"<p>Sign in now instead of at the first request.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>Prompt even when a cached token or account exists.</p> <code>False</code> Source code in <code>src/azure_auth/_sync/base.py</code> <pre><code>def login(self, *, force: bool = False) -&gt; None:\n    \"\"\"Sign in now instead of at the first request.\n\n    Args:\n        force: Prompt even when a cached token or account exists.\n    \"\"\"\n    self._tokens.login(client_id=self._client_id, scopes=self._scopes, force=force)\n</code></pre>"},{"location":"reference/sync/#azure_auth.sync.ResourceClient.request","title":"<code>request(method: str, url: str, *, params: Mapping[str, Any] | None = None, json: Any = None, headers: Mapping[str, str] | None = None) -&gt; httpx.Response</code>","text":"<p>Send an authenticated request and return the successful response.</p> <p>Throttled requests (429, 503) are retried, honouring <code>Retry-After</code>. A 401 is retried once: with the claims from the challenge when the resource sent one, else with a freshly acquired token.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>HTTP method.</p> required <code>url</code> <code>str</code> <p>Path relative to the base URL, or an absolute URL on the same host.</p> required <code>params</code> <code>Mapping[str, Any] | None</code> <p>Query parameters.</p> <code>None</code> <code>json</code> <code>Any</code> <p>Request body, serialised as JSON.</p> <code>None</code> <code>headers</code> <code>Mapping[str, str] | None</code> <p>Extra request headers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Response</code> <p>The response, whose status is below 400.</p> <p>Raises:</p> Type Description <code>ResourceError</code> <p>(the subclass in <code>ERROR_CLASS</code>) for an error response.</p> Source code in <code>src/azure_auth/_sync/base.py</code> <pre><code>def request(\n    self,\n    method: str,\n    url: str,\n    *,\n    params: Mapping[str, Any] | None = None,\n    json: Any = None,\n    headers: Mapping[str, str] | None = None,\n) -&gt; httpx.Response:\n    \"\"\"Send an authenticated request and return the successful response.\n\n    Throttled requests (429, 503) are retried, honouring ``Retry-After``. A 401 is\n    retried once: with the claims from the challenge when the resource sent one, else\n    with a freshly acquired token.\n\n    Args:\n        method: HTTP method.\n        url: Path relative to the base URL, or an absolute URL on the same host.\n        params: Query parameters.\n        json: Request body, serialised as JSON.\n        headers: Extra request headers.\n\n    Returns:\n        The response, whose status is below 400.\n\n    Raises:\n        ResourceError: (the subclass in ``ERROR_CLASS``) for an error response.\n    \"\"\"\n    target = self._url(url)\n    retries = 0\n    challenged = False\n    claims: str | None = None\n    force_refresh = False\n    while True:\n        token = self._tokens.acquire_token(\n            self._scopes, client_id=self._client_id, claims=claims, force_refresh=force_refresh\n        )\n        claims, force_refresh = None, False\n        response = self._http.request(\n            method,\n            target,\n            params=params,\n            json=json,\n            headers={**(headers or {}), \"Authorization\": f\"Bearer {token.token}\"},\n        )\n        if response.status_code == httpx.codes.UNAUTHORIZED and not challenged:\n            challenged = True\n            claims = claims_from_challenge(response.headers.get(\"WWW-Authenticate\"))\n            force_refresh = claims is None\n            continue\n        if response.status_code in _RETRY_STATUSES and retries &lt; self._max_retries:\n            retries += 1\n            time.sleep(retry_delay(response, retries, self._max_retry_wait))\n            continue\n        if response.status_code &gt;= httpx.codes.BAD_REQUEST:\n            raise self._error(response)\n        return response\n</code></pre>"}]}