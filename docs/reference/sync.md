# Blocking clients

::: azure_auth.sync

The blocking clients are generated from the asynchronous ones and have the same methods
without `await`. See [Clients](clients.md) for the full documentation.
